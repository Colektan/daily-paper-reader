---
title: Online KL-Regularized Reinforcement Learning with Function Approximation under Misspecification
title_zh: 基于函数近似的在线KL正则化强化学习在模型错误设定下的研究
authors: "Haoyang Hong, Zichen Wang, Quanquan Gu, Huazheng Wang"
date: 2026-06-04
pdf: "https://arxiv.org/pdf/2606.06053v1"
tags: ["query:rl-vla-world"]
score: 9.0
evidence: KL正则化强化学习在模型误设定下的保证
tldr: 论文针对模型误设下KL正则化强化学习问题，提出KL误设框架并分析基于回归的算法。现有理论依赖可实现性，适用于误设场景时可能失效。本文采用Gibbs策略更新，导出带显式误设项的高概率KL遗憾界，在可实现情形下恢复经典结果。
source: arxiv
selection_source: fresh_fetch
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.06053v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 985, \"height\": 380, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.06053v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1430, \"height\": 1374, \"label\": \"Table\"}]"
motivation: 现有KL正则化RL理论依赖函数类可实现性，无法处理模型误设，需要建立适用于误设场景的遗憾保证。
method: 定义KL误设度量，提出回归式算法并采用Gibbs策略更新，在上下文赌博机和episodic RL中分析。
result: 获得包含显式误设项的高概率KL遗憾界，实现时可恢复标准界，误设影响被显式量化。
conclusion: 首次为KL正则化RL在函数近似误设下提供理论保证，扩展了可应用范围。
---

## 摘要
我们研究了在一般函数近似下，具有模型错误设定的KL正则化情境赌博机与情节强化学习（RL）。现有保证依赖于可实现性，因此无法推广到错误设定的模型，经典遗憾界可能失效。本文引入了情境赌博机和情节强化学习的KL错误设定公式，并分析了基于回归的Gibbs策略更新算法。建立了具有显式错误设定项的高概率KL遗憾界，将标准可实现的KL正则化设定作为特例恢复。

## Abstract
We study KL-regularized contextual bandits and episodic reinforcement learning (RL) under general function approximation with model misspecification. Existing guarantees rely on realizability and therefore do not extend to misspecified models, where classical regret bounds may fail. This work introduces KL misspecification formulations for contextual bandits and episodic RL and analyzes regression-based algorithms with Gibbs policy updates. High-probability KL-regret guarantees with explicit misspecification terms are established, recovering the standard realizable KL-regularized setting as a special case.

---

## 论文详细总结（自动生成）

## 论文详细中文总结

### 1. 核心问题与整体含义（研究动机和背景）
- **研究动机**：现代 RLHF（基于人类反馈的强化学习）和 LLM（大语言模型）后训练流程中，常使用 KL 正则化来约束策略更新相对于参考策略的偏差（如 PPO、DPO 等）。然而现有理论分析大多依赖于**可实现性**（realizability）假设，即假设真实奖励或价值函数完全属于学习算法使用的函数类。在实际应用中，模型可能过于简单（如仅用线性、低秩函数类），导致模型**错误设定**（misspecification）：即使有无限数据，函数类内最优预测器仍存在非零近似偏差。
- **核心问题**：在 KL 正则化的情境赌博机和情节强化学习中，如何在不假定可实现性的情况下获得可证明的高概率遗憾界？错误设定如何影响算法性能？现有错误设定理论（如 Foster et al., 2021b）主要针对标准奖励遗憾，不直接适用于 KL 正则化目标。
- **整体含义**：本文是首个将 KL 正则化强化学习在一般函数近似下的错误设定理论系统化的研究，填补了理论空白，为实际中模型不完美的情况提供了理论保障。

### 2. 方法论
- **核心思想**：
  - 定义与 KL 正则化目标一致的**点式错误设定度量**：对于情境赌博机（ζ_Bandit）和情节 RL（ζ_RL），均采用点式超限误差定义（式 (6) 和式 (16)）。
  - 提出基于回归的乐观算法，采用 **Gibbs 策略更新**：算法维护一个乐观的奖励或 Q 函数估计，并通过 KL 正则化的 Gibbs 分布采样动作。
  - 通过**二次自界不等式**（Gibbs quadratic self-bounding inequality）将 KL 散度与平方 Q 间隙联系起来，再通过 Bellman 残差转换将平方 Q 间隙转化为平方 Bellman 残差，最终控制为平方奖励项的和。
- **关键技术细节**：
  - **MR-KL-UCB**（算法1）：适用于情境赌博机。每轮用过去数据拟合 ERM 奖励估计，加上基于 **Eluder 维数** 的置信度奖励，形成乐观估计，再根据 Gibbs 策略采样动作。
  - **MR-KL-LSVI**（算法2）：适用于情节 RL。每集反向传播：利用上一期数据拟合 Q 函数，加上阶段式乐观奖励，更新 Gibbs 策略和软价值函数（通过 log-partition 操作符）。
  - **理论参数设定**：置信度缩放因子 β = Θ(√log(NRT/δ))；局部化参数 λ 包含 Eluder 复杂度、log 因子和 Tζ² 项。在 RL 中，β = Θ(√Λ_RL)，其中 Λ_RL 包含函数类大小的对数。
  - 未指明时，错误设定参数 ζ 假定已知；未知时可通过模型选择包装器（几何网格搜索）处理，本文聚焦于已知 ζ 的基础算法分析。
- **重要公式**：
  - 点式错误设定：ζ²_Bandit = inf_{R∈R} sup_{x,a} |R(x,a) - R⋆(x,a)|²
  - KL-正则化 Bellman 操作符： (T_{η,h} f_{h+1})(s,a) = r_h(s,a) + E_{s'} [V_{h+1}(f_{h+1}; s')]
  - 理论遗憾界（赌博机）：Reg(T) = Õ(η [ log(NR T/δ) d(R,λ,T) + T ζ²_Bandit ])
  - 理论遗憾界（RL）：Reg(T) = Õ(η H² β² d_RL(λ,T) + η H³ T ζ²_RL)

### 3. 实验设计
- 文中**没有进行任何实验**。本文是完全的理论论文，不包含数据集、基准方法或对比实验。

### 4. 资源与算力
- 文中**未提及**任何 GPU 型号、数量或训练时长等算力资源。作为理论论文，不需要实验运行。

### 5. 实验数量与充分性
- 不适用。本文无实验。

### 6. 主要结论与发现
- 成功建立了 KL 正则化情境赌博机和情节 RL 在函数近似错误设定下的高概率遗憾界，且遗憾界显式包含错误设定项（T ζ²）。
- **可实现性（ζ=0）作为特例被恢复**，与现有可实现 KL 正则化理论（如 Zhao et al. 2025）一致。
- 分析揭示了 KL 正则化带来的特有项（二次自界不等式、Bellman 残差转换）在错误设定分析中的关键作用。
- 错误设定的影响被量化：赌博机中额外项为 Õ(η T ζ²)；RL 中额外项为 Õ(η H³ T ζ²)，其中 H 为情节长度。
- 证明依赖于 Eluder 维数复杂度、Freedman 不等式对齐、乐观置信度等标准技术。

### 7. 优点
- **理论创新**：首次将错误设定分析扩展到 KL 正则化强化学习，填补了重要理论空白。
- **方法透明**：算法结构简洁（标准回归 + 乐观奖励 + Gibbs 更新），易于理解。
- **通用性**：错误设定条件采用点式超限形式，与 Foster et al. (2021b) 一致但适应 KL 目标；后续可推广到更复杂的设定（如无限函数类、未知错误设定水平）。
- **模块化证明**：RL 分析中分离了置信度条件、不确定性可和性、可预测-实际对齐等模块，便于验证其他算法。
- **恢复可实现情形**：展示了理论的一致性。

### 8. 不足与局限
- **缺乏实验验证**：作为纯理论论文，没有用任何合成或真实数据集验证理论界的紧性或算法的实际性能，实际应用中的行为未知。
- **假设较强**：要求点式错误设定（uniform pointwise misspecification），在实际中可能过于严格；现实错误可能更结构化（如仅在某些分布上大）。
- **未知错误设定水平**：基础算法需要已知 ζ，虽然作者提到可通过模型选择包装器处理，但未给出具体分析，实际实现可能需要额外调参。
- **RL 分析中 horizon 依赖**：遗憾界中有 η H³ T ζ² 项，H 的立方因子可能较松；作者也指出来自 Bellman 残差传播的 H² 因子，但 H³ 是否最优尚待下界验证。
- **仅考虑轻尾噪声**：假设奖励噪声为 1-次高斯，实际中可能遇到重尾或对抗噪声，理论不覆盖。
- **未考虑偏好反馈**：仅基于奖励，而 RLHF 常用偏好反馈（Bradley-Terry 模型等），作者在结论中提及作为未来方向。
- **计算复杂度**：算法使用 ERM 和 Eluder 维数定义，实际中有限函数类可能很大，ERM 计算成本高，Eluder 维数可能难以计算。

（完）
