---
title: Post-Hoc Robustness for Model-Based Reinforcement Learning
title_zh: 基于模型的强化学习的后验鲁棒性
authors: "Siemen Herremans, Ali Anwar, Siegfried Mercelis"
date: 2026-06-02
pdf: "https://arxiv.org/pdf/2606.03521v1"
tags: ["query:rl-vla-world"]
score: 9.0
evidence: 模型基强化学习的后验鲁棒性
tldr: 在基于模型的强化学习中，环境扰动影响策略鲁棒性。本文提出一种事后鲁棒化方法，利用学习到的模型和名义策略，通过模型预测控制结合对抗性 rollout 进行策略改进，其中 rollout 通过投影梯度下降在界内不确定性集中近似并缓解分布外问题。在 Gymnasium MuJoCo 环境中验证，该方法在不额外训练神经网络的情况下显著提升了策略对扰动的鲁棒性。该工作增强了模型基强化学习的实际部署能力。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.03521v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1440, \"height\": 443, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.03521v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 641, \"height\": 472, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.03521v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1223, \"height\": 438, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.03521v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1223, \"height\": 440, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.03521v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1414, \"height\": 441, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.03521v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1429, \"height\": 574, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.03521v1/fig-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 1426, \"height\": 573, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.03521v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1421, \"height\": 247, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.03521v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 815, \"height\": 243, \"label\": \"Table\"}]"
motivation: 提升基于模型的强化学习在推理时对对抗扰动的鲁棒性，避免重新训练网络。
method: 使用模型预测控制与对抗性 rollout，通过投影梯度下降在界内不确定性集中近似扰动，并处理分布外问题。
result: 在扰动后的 MuJoCo 环境中，算法鲁棒性显著优于基线，且计算开销低。
conclusion: 提出的事后鲁棒化方法有效且高效，无需额外训练即可增强模型基强化学习的鲁棒性。
---

## 摘要
为了提升强化学习（RL）在现实世界中的适用性，对抗鲁棒RL领域研究如何在对抗环境扰动下训练智能体。在该设定中，主角智能体在对手的环境扰动下优化策略，形成零和马尔可夫博弈。当对抗鲁棒RL与基于模型的RL结合时，对手可以针对学习到的转移模型而非训练环境。扩展这一思路，本文引入了深度RL智能体在推理时的后验鲁棒化方法。通过将学习到的模型与训练好的基线策略结合使用，我们的方法执行鲁棒策略改进步骤，旨在不额外训练神经网络的情况下提升鲁棒性。具体而言，我们利用基于对抗展开的模型预测控制，该展开通过有界不确定性集内的投影梯度下降近似。此外，这些离线展开在考虑并缓解分布外问题的同时进行。通过在扰动后的Gymnasium MuJoCo环境中评估算法，同时考虑后验推理设定的计算限制，验证了所提方法能够显著提升鲁棒性。

## Abstract
To improve the real-world applicability of reinforcement learning (RL), the field of adversarially robust RL studies how to train agents under adversarial environment perturbations. In this setting, a protagonist agent optimizes a policy under environmental perturbations from an adversary, resulting in a zero-sum Markov game. When adversarially robust RL is combined with model-based RL, the adversary can target a learned transition model instead of the training environment. Extending this idea, this work introduces post-hoc robustification of deep RL agents at inference time. By using the learned model in combination with a trained nominal policy, our approach performs a robust policy improvement step. The goal is to improve robustness without any additional training of neural networks. Specifically, we utilize model-predictive control under adversarial rollouts, which are approximated via projected gradient descent within a bounded uncertainty set. Furthermore, these offline rollouts are performed while considering and mitigating out-of-distribution issues. The proposed methodology is validated by demonstrating significant improvements in robustness when the algorithm is evaluated in perturbed Gymnasium MuJoCo environments, while considering the computational limitations of the post-hoc inference setting.

---

## 论文详细总结（自动生成）

## 详细中文总结

### 1. 论文的核心问题与整体含义（研究动机和背景）
- **核心问题**：在基于模型的强化学习（MBRL）中，智能体在部署时面对环境扰动（如物理参数变化）时性能显著下降。现有方法多在训练阶段引入对抗训练（如鲁棒MDP），但会带来训练不稳定、计算开销大等问题。
- **研究动机**：探索能否在不修改训练过程的前提下，在推理时（inference time）直接提升已训练智能体的鲁棒性——即“后验鲁棒化”（post-hoc robustness）。这样可以在训练后按需定义不确定性集，并避免对抗训练对名义性能的负面影响。
- **整体含义**：提出了一种实用的轻量级解决方案，使得 MBRL 智能体在无重训练的情况下应对环境变化，提高了 RL 在 sim2real 等现实场景中的部署能力。

### 2. 论文提出的方法论：核心思想、关键技术细节、公式或算法流程
- **核心思想**：在推理时，利用预训练的名义转移模型 pθ 和名义值函数 Vπ，通过**带对抗扰动的模型预测控制（MPC）** 执行鲁棒策略改进。即在模型展开时人为扰动下一个状态以最小化值函数（悲观估计），从而得到对扰动鲁棒的局部决策。
- **关键技术细节**：
    - **MPC 策略**：使用模型预测路径积分（MPPI）优化局部策略 π_MPC，初始化为预训练策略 πψ。
    - **对抗扰动**：对模型预测的下一个状态 s' 执行投影梯度下降（PGD），使其趋向于低值区域，扰动 δ 被限制在 L2 球内（半径 r2）。迭代公式：
        ```
        δ ← Proj_{∥δ∥2≤r2}[ δ - α ∇(s'+δ)Vψ(s'+δ) / ∥∇(s'+δ)Vψ(s'+δ)∥2 ]
        ```
        其中 Proj 为投影函数，确保 δ 不超过半径 r2。
    - **分布外（OOD）缓解**：使用集成模型（bootstrap ensemble）的方差估计作为 epistemic uncertainty 指标，设定阈值 h，当展开过程中 ensemble discrepancy 超过 h 时截断 rollout，避免对学习模型在未知区域过度利用。
    - **Rollout 过程**：递归定义悲观值估计 V_rollout_k(s_k) = r(s_k, π(s_k)) + γ * V_rollout_{k+1}(s_{k+1})，其中 s_{k+1} 为经过 PGD 扰动后的状态。最终步使用 Vπ(s_N)。
- **算法流程简要**：
    1. 加载预训练的 MBPO 模型（包括转移模型集成、策略、值函数）。
    2. 在每个环境步骤，使用 MPPI 对局部策略进行迭代优化（7 次迭代），每次迭代中：
        - 对当前状态及动作，利用模型展开 N 步轨迹，每步对预测状态施加 PGD 扰动（1~3 步）。
        - 根据 ensemble 方差判断是否截断展开。
        - 计算累积悲观回报作为轨迹评分。
    3. 选择最优动作序列的第一个动作执行于环境。

### 3. 实验设计
- **数据集/场景**：Gymnasium MuJoCo 的 **Reacher-v4**（简单2D机械臂）和 **Hopper-v4**（单腿跳跃机器人）。
- **Benchmark/基线**：
    - **基线（Baseline）**：未修改的 MBPO（论文 Janner et al., 2019）——直接使用预训练策略 πψ 输出动作。
    - **非鲁棒 MPC 消融（Non-Robust MPC）**：使用 MPC 但不对模型展开施加对抗扰动（即 r2=0），但仍使用 OOD 截断。
    - **提出方法（Robust MPC）**：完整方法。
- **对比的设置**：
    - 单参数扰动：分别扰动 Reacher-v4 的阻尼系数、齿轮比；Hopper-v4 的躯干质量、摩擦系数。
    - 双参数同时扰动：躯干质量 × 摩擦系数。
- **消融实验**：
    - 扰动半径 r2 的敏感性（0.0, 0.03, 0.045, 0.06, 0.075, 0.12）。
    - Rollout 深度 N 的影响（1, 10, 20, 40）。
    - 有无 OOD 截断的效果（在 Reacher-v4 上展示）。
- **评估指标**：每个种子下 20 个 episode 的平均回报，共 5 个随机种子，报告 95% 自助法置信区间。

### 4. 资源与算力
- 论文 **未明确说明训练 MBRL 智能体所使用的计算资源**（只提及预训练 MBPO，但未给出 GPU 型号、训练时间等）。
- 提供了**推理时**每次环境步骤的计算时间（表1），在多种 GPU 上使用 Jax 实现：
    - Hopper-v4（N=20）：RTX 4090 0.09s, RTX 2080 Ti 0.12s, A6000 0.15s, H200 0.09s。
    - Reacher-v4（N=5）：对应约为 0.015~0.024s。
- 未报告训练耗时或能耗。

### 5. 实验数量与充分性
- **主要实验组数**：两个环境 × 两种单参数扰动 + 双参数扰动每种一个图（共5幅主要结果图）。消融实验：Reacher-v4 OOD 消融（1 图），Hopper-v4 的 N 和 r 消融（各1图，另在附录给出摩擦扰动下的类似消融）。总实验图约 10 幅。
- **充分性评价**：
    - **充分**：覆盖了不同环境、单双扰动、关键超参数消融、OOD 必要性验证。使用 5 个随机种子和 bootstrap 置信区间，统计上较为可靠。
    - **客观**：对比基线采用标准 MBPO，非鲁棒 MPC 消融能区分对抗扰动的贡献。超参数手动调优但报告了调优过程，未过度优化。
    - **局限**：仅验证了低维连续控制任务（Reacher 和 Hopper），缺乏更高维任务（如 Humanoid, Ant）的验证；扰动类型偏少（仅质量、摩擦、阻尼、齿轮）；未与更复杂的鲁棒训练方法（如 RARL, R-MDP 在线算法）对比——但本工作初衷是后验方式，可比性有限。

### 6. 论文的主要结论与发现
1. **提出的 Robust MPC 方法显著提升鲁棒性**：在两种环境下，面对单参数和双参数扰动，方法均能保持比基线更高的回报，且性能下降更平缓。
2. **对抗扰动（PGD）是鲁棒性提升的核心**：非鲁棒 MPC 消融实验表明，不施加扰动时鲁棒性提升有限甚至下降（Hopper 中非鲁棒 MPC 与基线接近）。
3. **OOD 截断至关重要**：若无 OOD 缓解，方法性能甚至低于基线（图2），说明充分抑制模型误差是成功关键。
4. **Rollout 深度 N 和扰动半径 r 有稳定工作区间**：增大 N 可提升鲁棒性，但过长会因复合模型误差而下降；适当 r 值（如 0.045~0.075）可提升鲁棒性，过大则会失效。
5. **计算开销可接受**：在主流 GPU 上每步推理时间在 0.1~0.2 秒，适合实时性要求不高的场景。

### 7. 优点
- **创新性的后验范式**：无需重新训练或修改训练过程，直接利用已训练模型实现鲁棒性提升，实用性强。
- **理论与工程结合**：利用 OTC 不确定性集与 PGD 扰动建立与 RMDP 的联系，同时考虑 OOD 问题，使得方法在经验上有效。
- **彻底的消融实验**：清晰解耦了对抗扰动、OOD 截断、 rollout 深度、不确定性半径各自的影响，验证了每个组件的必要性。
- **计算效率讨论**：提供了不同 GPU 的推理时间，有助于评估部署可行性。
- **代码开放性提及**：基于 Jax 实现（参考 TD-MPC2 与 MBPO），有助于复现。

### 8. 不足与局限
- **理论保证不足**：使用名义值函数最小化代替鲁棒值函数，仅在 1 步 rollout 时与鲁棒贝尔曼算子严格对应；多步后无理论保障。作者虽通过直觉（终止状态使两者一致）解释，但缺乏严谨分析。
- **环境复杂度和通用性有限**：仅测试了 Reacher 和 Hopper 两个低维环境，未在 Humanoid、Ant 等高维或更复杂场景（如带有视觉输入的任务）验证，方法的可扩展性未知。
- **扰动类型范围有限**：仅修改物理参数，未考虑观测噪声、动作扰动等常见对抗攻击形式。
- **超参数敏感性**：r 和 N 需手动调谐（如通过迭代调整），且不同环境差异大（例如 Hopper N=40 而 Reacher N=5），缺乏自适应机制。
- **计算开销仍属中等**：即使使用 GPU，每步约 0.1s 对于高频控制（如四足机器人）可能仍不足以实时运行。
- **未与在线鲁棒训练方法公平对比**：虽然设定不同，但读者可能期望了解相对于在训练阶段加入对抗训练的方法（如 RARL）在最终鲁棒性和名义性能上的权衡。

（完）
