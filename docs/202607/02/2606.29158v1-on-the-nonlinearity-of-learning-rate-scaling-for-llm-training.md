---
title: On the Nonlinearity of Learning Rate Scaling for LLM Training
title_zh: 关于大语言模型训练中学习率缩放的非线性
authors: "Zaiwen Yang, Huaqing Zhang, Jing Xu, Jingzhao Zhang"
date: 2026-06-28
pdf: "https://arxiv.org/pdf/2606.29158v1"
tags: ["query:rep-learning"]
score: 9.0
evidence: 训练优化策略以改善模型收敛
tldr: 大规模语言模型训练中，学习率缩放通常假设对数线性规律以从小规模外推。本文实验发现，在GPT-2模型从22M到707M参数、5B到100B tokens的范围内，最优学习率随规模增大出现向上弯曲，导致外推不准。改用有效学习率（权重重归一化后的步长）和数据规模（D）外推可消除该弯曲。解释是权重范数收敛到平衡点慢，需更大步长。实验采用AdamH优化器进一步验证。
source: arxiv
selection_source: fresh_fetch
motivation: 现有学习率缩放假设对数线性规律，但在大规模时出现非线性，导致外推不准确，需探究原因和改进方法。
method: 通过训练GPT-2系列模型（22M-707M参数）在5B-100B tokens上，系统比较不同学习率缩放方式，并分析权重范数收敛动态。
result: 发现最优学习率在大规模时有向上弯曲，改用有效学习率和数据规模外推可消除弯曲，且该现象与权重范数收敛慢有关。
conclusion: 学习率缩放的非线性源于权重范数动态，采用有效学习率可改善外推，AdamH实验支持此解释。
---

## 摘要
学习率迁移可以降低训练大语言模型的成本：实践者不是直接在目标规模下进行学习率搜索，而是从小规模运行中外推得到。现有方法通常假设最优学习率在数据规模和模型规模上遵循对数线性缩放定律。我们仔细考察并评估了这一缩放定律。在我们对从22M到707M参数的GPT-2风格模型（在5B到100B token上训练）的实证研究中，最优学习率在较大规模时出现向上的曲率，导致外推不准确。我们发现，当用有效学习率（归一化权重空间中的步长）代替学习率，以及使用数据D外推而非模型规模N外推时，这种曲率基本消失。接下来，我们解释了缩放中的非线性：当最优学习率较小时，权重范数收敛到平衡态更慢，需要更大的步长来减少过渡阶段。使用直接控制有效学习率的AdamH进行的实验进一步支持了这一解释。

## Abstract
Learning-rate transfer can reduce the cost of training large language models: instead of sweeping learning rates at target scale, practitioners extrapolate from smaller runs. Existing approaches often assume that the optimal learning rate follows a log-linear scaling law in data scale and model size. We carefully examine and evaluate this scaling law. In our empirical study of GPT-2--style models from 22M to 707M parameters trained on 5B to 100B tokens, the optimal learning rate develops upward curvature at larger scales, leading to inaccurate extrapolation. We find that this curvature largely disappears when learning rates are replaced by effective learning rate (the step size in normalized weight space), and when data $D$ extrapolation is used instead of model size $N$ extrapolation. Next, we explain nonlinearity in scaling: weight-norm converges to equilibrium slower when optimal learning is small, requiring a larger step size to reduce the transient phase. Experiments with AdamH, which directly controls the effective learning rate, further support this explanation.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）
- **研究动机**：训练大语言模型（LLM）时，学习率的调优至关重要，但在目标规模下直接进行学习率搜索成本极高。因此，研究者希望从小规模运行中通过缩放定律外推得到最优学习率（学习率迁移）。
- **现有假设**：主流假设最优学习率 $\eta^*$ 在数据规模 $D$ 和模型规模 $N$ 上满足**对数线性（幂律）关系**：$\log \eta^* = a \log D + b \log N + c$。
- **论文发现**：通过对 GPT-2 风格模型（22M~707M 参数，5B~100B tokens 训练）的系统实证，论文发现该对数线性缩放仅在局部近似成立，在大规模时出现**系统性向上弯曲**，导致外推预测偏低，不准确。
- **整体含义**：揭示了传统学习率缩放定律的非线性本质，并指出**有效学习率**（归一化权重空间的步长）和**数据规模轴外推**能更准确地迁移学习率，从而降低大规模训练的超参数调优成本。

### 2. 论文提出的方法论：核心思想、关键技术细节、公式或算法流程
- **核心思想**：
  - 重新审视 $\eta^*$ 的缩放规律，发现其非线性；引入**有效学习率** $\eta_{\text{eff}}(t) = \|\hat{\mathbf{w}}_{t+1} - \hat{\mathbf{w}}_t\|_2$（$\hat{\mathbf{w}}=\mathbf{w}/\|\mathbf{w}\|_2$），认为 $\eta_{\text{eff}}$ 才是控制优化动力学的真正量。
  - 通过分析 AdamW 隐式动力学，解释 $\eta^*$ 的非线性：权重范数 $\|\mathbf{w}_t\|_2$ 的平衡收敛速率与 $\eta$ 有关，小 $\eta$ 导致过渡期更长，使 $\eta_{\text{eff}} \propto \eta$（预平衡区）而非 $\eta_{\text{eff}} \propto \sqrt{\eta}$（平衡区），从而引起 $\log\eta^*$ 向上弯曲。
- **关键技术细节**：
  - 定义最优学习率 $\eta^*(D,N)$：在给定 $(D,N)$ 下最小化验证损失的学习率。
  - 定义最优有效学习率 $\eta^*_{\text{eff}}(D,N)$：由 $\eta^*(D,N)$ 诱导的平均有效学习率。
  - 提出两种外推策略：沿数据规模轴（$D$-dim）和沿模型规模轴（$N$-dim）。
  - 推导 AdamW 下有效学习率与学习率的关系公式（公式 14~17），证明存在两个渐近区间，小 $\eta$ 对应预平衡区。
  - 引入 **AdamH 优化器**（算法1）：直接控制权重范数和更新范数为固定值，从而显式控制 $\eta_{\text{eff}}$，验证其线性缩放行为。
- **算法流程（文字说明）**：AdamH 在每一步计算 Adam 更新方向，然后归一化更新向量并乘以固定半径 $R$（初始权重范数），再对权重进行归一化，使 $\eta_{\text{eff}}$ 直接等于学习率 $\eta$。

### 3. 实验设计：数据集、Benchmark、对比方法
- **数据集**：FineWeb-100B（开源文本语料库），用于预训练。
- **模型架构**：GPT-2 风格，12 层 Transformer，注意力头维度 128，通过改变隐藏维度（256~2048）产生 6 种参数规模（22M, 64M, 124M, 203M, 417M, 707M）。
- **训练配置**：批次大小 512，序列长度 1024，约 0.52M tokens/步。使用 AdamW 优化器（$\beta_1=\beta_2=0.95$，权重衰减 0.1），学习率对数量级网格：$\log_2\eta\in[-16,-7]$ 步长 1。采用 WSD（warmup-steady-decay）学习率调度。
- **对比方法**：
  - 外推轴对比：数据规模轴（$D$-dim）vs. 模型规模轴（$N$-dim）。
  - 参数化对比：原始学习率 $\eta$ vs. 有效学习率 $\eta_{\text{eff}}$。
  - 优化器对比：AdamW vs. AdamH（仅验证 $\eta_{\text{eff}}$ 线性缩放）。
- **评估指标**：
  - $R^2_{\text{OOD}}$：外推对数学习率的解释方差（可负值）。
  - **额外计算比率（ECR）**：使用预测学习率与最优学习率达到相同损失所需的额外训练计算量占比。
- **测试集**：固定 $N_{\text{test}}=707$M，$D_{\text{test}}\in[50\text{B},100\text{B}]$ 的元组。训练集根据计算预算 $\beta$ 从较小规模构建（$D$-dim 使用 $5\text{B},(\beta-5)\text{B}$ 的 $D$，$N$-dim 使用 $N_{\text{test}}$ 的分数）。

### 4. 资源与算力
- 文中明确说明：**所有实验在 16 张 H200 GPU 上运行**，由于中断等中间问题，总耗时约**两个月**（含多次重复与稳定性测试）。不包括 AdamH 等额外实验时，主实验本身也占相当比重。

### 5. 实验数量与充分性
- **实验规模**：
  - 模型规模：6 种（22M~707M）。
  - 数据预算：5B~100B tokens，以 2.5B 为增量，共约 39 个点。
  - 学习率网格：$\log_2\eta\in[-16,-7]$ 共 10 个值，密集覆盖。
  - 共产生数百个训练轨迹，并记录每个配置的验证损失、有效学习率、权重范数等统计量。
  - 利用 WSD 调度和检查点，可在不重新训练的情况下评估不同训练步数（对应不同 $D$）的最优学习率。
- **消融与验证**：
  - 对比 $D$-dim 与 $N$-dim 外推；对比 $\eta$ 与 $\eta_{\text{eff}}$。
  - 进行三次拟合与二次拟合对比（附录），确认三次拟合更合适。
  - 对 Adam 无权重衰减的情况也进行了验证（图5），以支持理论推导。
  - 使用 AdamH 单独验证 $\eta_{\text{eff}}$ 的线性缩放（图7）。
- **充分性评价**：实验非常系统，覆盖了不同规模、不同数据量、不同学习率，并针对理论解释提供了独立验证。但作者承认仅使用 GPT-2 架构和 FineWeb 数据集，优化器局限于 AdamW（除 AdamH 外），且外推上限为 707M 参数/100B tokens，可能限制泛化性。

### 6. 论文的主要结论与发现
1. **最优学习率缩放的非线性**：$\log\eta^*$ 与 $\log D$ 或 $\log N$ 并非严格线性，在大规模时出现向上弯曲，导致小规模外推偏低。
2. **有效学习率更稳定**：$\log\eta^*_{\text{eff}}$ 在 $D$ 和 $N$ 上显示出更好的线性（Pearson 相关系数更高），外推更准确。
3. **数据轴外推优于模型轴外推**：在同等计算预算下，沿 $D$ 轴拟合缩放定律比沿 $N$ 轴带来更高的 $R^2_{\text{OOD}}$ 和更低的 ECR。
4. **非线性来源解释**：小学习率下，权重范数收敛到平衡态慢，导致 $\eta_{\text{eff}}$ 与 $\eta$ 呈线性关系（预平衡区）；大学习率下呈平方根关系（平衡区）。这种从平方根到线性的转变引起 $\eta^*$ 向上弯曲。
5. **AdamH 验证**：直接控制 $\eta_{\text{eff}}$ 的 AdamH 展现出清晰的线性缩放 $\log\eta^*_{\text{eff}} \propto \log D$，进一步支持理论分析。

### 7. 优点：方法或实验设计上的亮点
- **系统性实证**：覆盖多个模型规模、数据规模和学习率，并公开发布完整的训练轨迹数据集（LR-Transfer-Trajectory），促进可重复研究。
- **公平比较框架**：提出“相同计算预算下比较外推性能”的范式，使用 $R^2_{\text{OOD}}$ 和 ECR 两个互补指标，避免传统仅拟合曲线的偏差。
- **机制解释深入**：从 AdamW 隐式动力学推导出有效学习率的两阶段行为，并用亚当无权重衰减实验（图5）和 AdamH 实验（图7）独立验证。
- **实用建议明确**：指出实际训练中应优先采用“数据轴外推 + 有效学习率”策略，可将额外计算开销降至 2% 以内（表1）。

### 8. 不足与局限
- **模型与架构覆盖有限**：仅使用 GPT-2 风格 Transformer，未验证其他架构（如 LLaMA、Mamba 等）或不同归一化方式。
- **优化器局限**：主实验仅用 AdamW，除 AdamH 外未测试 SGD、Adam 无权重衰减等，尽管部分分析涉及无权重衰减情况。
- **最优学习率估计偏差**：采用三次多项式拟合 $\log\eta$ 与验证损失得到 $\eta^*$，当真实最优值在搜索边界时可能引入偏差。
- **观测尺度有限**：参数最大 707M，数据最大 100B tokens，未探索千亿参数或万亿 tokens 的极端场景，外推结论可能无法直接推广。
- **计算预算约束下的外推假设**：文中固定批次大小和动量参数，未考虑批次大小或动量等超参数联合调优的交互影响。

（完）
