---
title: Training Vision-Language-Action Models with Dense Embodied Chain-of-Thought Supervision
title_zh: 使用密集具身思维链监督训练视觉-语言-动作模型
authors: "Haoyang Li, Guanlin Li, Youhe Feng, Chen Zhao, Zhuoran Wang, Yang Li, Qizhe Wei, Shifeng Bao, Haitao Shen, Yihan Zhao, Tong Yang, Jing Zhang"
date: 2026-06-29
pdf: "https://arxiv.org/pdf/2606.30552v2"
tags: ["query:rl-vla-world"]
score: 9.0
evidence: 使用具身思维链监督的VLA模型实现跨实体迁移
tldr: 跨实体迁移在视觉-语言-动作（VLA）模型中面临挑战，因低级动作空间差异。ZR-0模型利用密集的具身思维链（ECoT）监督，通过预训练VLM生成结构化推理，并耦合扩散Transformer动作专家，在训练时对齐跨实体表示。在包含60M帧、超40万轨迹的大规模数据集上预训练后，ZR-0在单臂、双臂和人形三个仿真基准及真实机器人上均表现优异，且推理时可跳过ECoT不损失性能。该工作为跨实体VLA模型的训练提供了新范式。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.30552v2/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1417, \"height\": 646, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.30552v2/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1415, \"height\": 546, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.30552v2/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1401, \"height\": 449, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.30552v2/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1336, \"height\": 1048, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.30552v2/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1444, \"height\": 844, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.30552v2/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1158, \"height\": 186, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.30552v2/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1434, \"height\": 185, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.30552v2/table-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1443, \"height\": 2009, \"label\": \"Table\"}]"
motivation: 现有VLA模型跨实体迁移困难，但高级认知过程在实体间共享，需对齐表示以提升泛化性。
method: 提出ZR-0，双流架构：VLM生成ECoT推理，扩散Transformer动作专家通过流匹配输出连续动作，交叉注意力耦合且推理时可跳过ECoT。
result: 在LIBERO、RoboTwin 2.0、RoboCasa GR-1三个仿真基准和真实xArm平台取得强性能，ECoT监督显著提升零样本泛化。
conclusion: 密集ECoT监督有效对齐跨实体表示，为构建通用VLA模型提供了可行方案。
---

## 摘要
跨实体迁移在视觉-语言-动作（VLA）模型中仍然具有挑战性，因为不同机器人平台的低级状态和动作空间存在根本差异。我们观察到，操作背后的高级认知过程，包括场景感知、物体识别、任务规划和子任务分解，在不同实体间很大程度上是共享的。基于这一观察，我们提出了ZR-0，一个拥有26亿参数的端到端VLA模型，该模型使用密集的具身思维链（ECoT）监督来对齐视觉-语言模型（VLM）中的跨实体表示。ZR-0采用双流架构：预训练的VLM（系统2）在训练期间生成结构化的ECoT推理，而基于扩散Transformer的动作专家（系统1）通过流匹配生成连续的动作块。两个组件通过交叉注意力耦合，并带有一个注意力掩码，该掩码将动作专家限制为仅输入提示特征，从而可以在推理时完全跳过ECoT生成，而不损失任何性能。ZR-0在ProcCorpus-60M上进行了预训练，这是一个大规模数据集，包含来自超过40万条轨迹的大约6000万帧（约1000小时），密集的ECoT注释覆盖了所有帧的96.8%。我们在三个模拟基准上评估了ZR-0，涵盖单臂（LIBERO）、双臂（RoboTwin 2.0）和人形（RoboCasa GR-1 Tabletop）实体，以及在xArm平台上的真实世界实验，展示了在所有设置中的强大性能。代码和模型检查点可在https://github.com/RUCKBReasoning/ZR-0获取。

## Abstract
Cross-embodiment transfer in vision-language-action (VLA) models remains challenging because low-level state and action spaces differ fundamentally across robot platforms. We observe that the high-level cognitive process underlying manipulation, including scene perception, object identification, task planning, and sub-task decomposition, is largely shared across embodiments. Based on this observation, we present ZR-0, a 2.6 billion parameter end-to-end VLA model that uses dense Embodied Chain-of-Thought (ECoT) supervision to align cross-embodiment representations within the vision-language model (VLM). ZR-0 adopts a dual-stream architecture: a pre-trained VLM (System 2) generates structured ECoT reasoning during training, while a Diffusion Transformer-based action expert (System 1) produces continuous action chunks via flow matching. The two components are coupled through cross-attention, with an attention mask that restricts the action expert to input prompt features only, enabling ECoT generation to be entirely skipped at inference without any performance loss. ZR-0 is pre-trained on ProcCorpus-60M, a large-scale dataset comprising approximately 60 million frames (approximately 1,000 hours) from over 400K trajectories, with dense ECoT annotations covering 96.8% of all frames. We evaluate ZR-0 on three simulation benchmarks spanning single-arm (LIBERO), bimanual (RoboTwin 2.0), and humanoid (RoboCasa GR-1 Tabletop) embodiments, as well as real-world experiments on the xArm platform, demonstrating strong performance across all settings. Code and model checkpoints are available at https://github.com/RUCKBReasoning/ZR-0.

---

## 论文详细总结（自动生成）

# 论文《Training Vision-Language-Action Models with Dense Embodied Chain-of-Thought Supervision》详细中文总结

## 1. 核心问题与整体含义（研究动机和背景）

- **核心问题**：跨实体（cross-embodiment）迁移在视觉‑语言‑动作（VLA）模型中面临根本挑战——不同机器人平台的低级状态和动作空间（如自由度、控制接口、传感器配置）差异巨大，导致语义对齐困难。现有方法主要采用格式层面的处理（如零填充、归一化或固定语义维度的统一动作空间），无法真正学习共享、可迁移的表示。
- **整体含义**：作者观察到，操作背后的高级认知过程（场景感知、物体识别、任务规划、子任务分解）在不同实体间是共享的。因此，如果能让模型在训练时学习到这种与实体无关的推理能力，就能实现有效的跨实体迁移。本文通过引入密集的“具身思维链（ECoT）”监督信号来对齐 VLM 的跨实体表示，从而提升 VLA 模型的泛化能力。

## 2. 方法论：核心思想、关键技术细节

- **核心思想**：基于系统 1/系统 2 认知框架，设计双流架构：
  - **系统 2**：预训练的视觉‑语言模型（VLM，从 Qwen3‑VL‑2B‑Instruct 初始化），接收任务指令和图像观测，在训练时生成结构化的 ECoT 推理序列。
  - **系统 1**：基于扩散 Transformer（DiT）的动作专家，通过流匹配（flow matching）预测连续动作块。
- **关键技术细节**：
  - **ECoT 组成**：场景描述、进度评估、未来计划、原子子任务列表（To‑Do Actions）、目标物体边界框（视觉定位）、离散动作 token。这些组件覆盖 96.8% 的帧，提供密集监督。
  - **耦合方式**：VLM 的最后隐层特征 `ft` 通过交叉注意力传递给动作专家。动作专家的 DiT 块采用 1 层自注意力 + 3 层交叉注意力（对 VLM 特征）的模式。
  - **关键设计**：交叉注意力掩码限制动作专家仅能关注 VLM 输入提示（任务指令和图像）的特征，排除 ECoT token。这使得推理时可以完全跳过 ECoT 自回归生成，仅需一次 VLM 前向传递即可获得动作专家所需特征，不牺牲性能。
  - **训练目标**：两个损失加权求和：
    - 下一个 token 预测损失 `L_ntp`（仅更新 VLM），用于 ECoT 推理；
    - 流匹配去噪向量场损失 `L_fm`（更新整个模型），用于连续动作预测。
  - **动作表示**：状态和动作均零填充至 64 维，并进行分位数归一化；动作块长度 `H` 可配置（预训练 32，微调时根据任务调整）。
  - **推理流程**：从高斯噪声初始化动作块，通过前向欧拉迭代去噪（通常 `N` 步），不生成文本 ECoT，推理延迟约 90ms（单张 A6000 GPU）。

## 3. 实验设计：数据集、基准、对比方法

- **数据集**：
  - 预训练：ProcCorpus‑60M，约 6000 万帧（1000 小时），来自 40 万+轨迹，涵盖 DROID、Bridge、Fractal、RH20T、Open X‑Embodiment 子集等，包含密集 ECoT 注释。
  - 训练时混合通用视觉‑语言数据（CapsFusion、Pixmo）以保持 VLM 通用能力。
  - 微调：使用各基准的公开训练演示（无 ECoT 或 VL 数据辅助）。
- **基准与实体**：
  - **LIBERO**（单臂）：40 个任务，4 个评估套件（Spatial、Object、Goal、Long）。评价指标：成功率（50 episodes/任务）。
  - **RoboTwin 2.0**（双臂 ALOHA）：50 个任务，包含清洁场景和域随机化场景（500 个演示/任务）。评价指标：成功率（100 episodes/任务）。
  - **RoboCasa GR‑1 Tabletop**（人形）：24 个任务。评价指标：成功率（100 episodes/任务）。
  - **真实世界 xArm**：4 个任务（Push Blocks、Clean Table、Pick & Place、Hang Cups），每个任务 10 个 trials，使用任务进度分数 S ∈ [0,100]。
- **对比方法**：
  - LIBERO：OpenVLA、CoT‑VLA、π0、NORA‑1.5、π0.5、GR00T‑N1.7、DeepThinkVLA、MolmoAct2。
  - RoboCasa GR‑1：GR00T‑N1.6、Qwen3PI、VP‑VLA、ABot‑M0、JoyAI‑RA、StarVLA。
  - RoboTwin 2.0：π0、π0.5、X‑VLA、Motus、LingBot‑VLA。
  - 真实 xArm：π0.5。
- **消融实验**：在 LIBERO 上对比有/无 ECoT 预训练（w/o ECoT PT）。

## 4. 资源与算力

- 论文明确提到：ZR‑0 包含约 26 亿参数（VLM 21 亿 + 动作专家 5 亿）。
- 训练使用 **DeepSpeed ZeRO**、**Flash‑Attention 2**、梯度检查点技术，采用 bfloat16 混合精度。
- 预训练时全局 batch size 为 1024，使用 AdamW 优化器，学习率余弦衰减（峰值 3×10⁻⁵，最终 3×10⁻⁶），线性预热 5% 步数。
- 推理时在单张 **NVIDIA A6000**（bfloat16）上约 90ms 生成一个动作块；在单张 H100 上约 100ms。
- **未明确说明**：详细 GPU 数量、总训练时长、预训练步数等具体算力消耗。论文仅表示使用高效分布式训练，但未提供定量数据。

## 5. 实验数量与充分性

- **实验数量**：总共涉及 4 个主要基准（3 模拟 + 1 真实），每个基准包含多个任务（LIBERO 40 任务、RoboTwin 2.0 50 任务、RoboCasa 24 任务、xArm 4 任务），各任务均报告成功率或进度分数。此外包含一组消融实验（有/无 ECoT 预训练）。
- **充分性**：
  - 覆盖了单臂、双臂、人形三种实体，以及真实机器人，跨实体验证充分。
  - 对比了 10+ 种最新的 VLA 方法，包括 π0 系列、GR00T 系列、OpenVLA 等，对比全面。
  - 消融实验直接验证 ECoT 预训练的效果，但仅在一个基准（LIBERO）上进行，未在其他基准上重复消融，略显不足。
  - 未报告方差（如多次随机种子结果），仅给出单次评估的平均值，统计可靠性一般。
  - 真实世界实验仅对比 π0.5，且每个任务仅 10 个 trials，样本量偏小。

## 6. 主要结论与发现

- ZR‑0 在三个模拟基准上均达到或超过当前最强模型：LIBERO 97.8%、RoboCasa GR‑1 69.3%、RoboTwin 2.0 Clean/Randomized 88.70%/87.98%。
- ECoT 预训练带来显著提升：消融实验显示，去除 ECoT 预训练后 LIBERO 平均成功率从 97.8% 降至 95.7%，尤其在长时域任务 LIBERO‑10 上差距更大（96.4% vs 92.6%）。
- 真实世界中，ZR‑0 在需要 OCR、长程规划、空间推理的任务上优于 π0.5（平均 76.0 vs 67.8），但在需要精细电机控制的 Hang Cups 任务上落后。
- 密集 ECoT 监督主要受益于预训练数据中常见的操作原语（如拾取‑放置），对于不常出现的操作（如关闭柜门）迁移效果较弱。
- 推理时跳过 ECoT 生成不损失性能，实际控制频率满足实时部署要求。

## 7. 优点

- **方法创新**：将具身思维链（ECoT）作为密集监督信号，有效对齐跨实体表示；通过注意力掩码巧妙设计，实现训练时利用推理、推理时跳过文本生成，兼顾性能与效率。
- **架构设计**：双流系统 1/系统 2 架构灵活，VLM 与 DiT 动作专家通过交叉注意力耦合，易于扩展不同 VLM 骨干和动作专家。
- **数据规模与覆盖**：ProcCorpus‑60M 是目前最大的带密集 ECoT 注释的机器人数据集，覆盖多种实体、场景和任务。
- **实验覆盖广泛**：跨单臂、双臂、人形三种实体，并在真实机器人上验证，对比了多种基线方法，消融实验直接验证核心贡献。
- **代码与模型开源**：便于复现和社区进一步研究。

## 8. 不足与局限

- **实验覆盖的局限性**：
  - 消融实验仅在一个基准（LIBERO）上进行，未在其他基准或真实机器人上验证 ECoT 预训练的必要性。
  - 真实实验样本量小（每任务 10 trials），且仅对比了 π0.5，缺乏与其他最新方法的比较。
  - 未报告多次运行的方差或置信区间，统计可靠性待加强。
- **数据与技能覆盖不足**：
  - 预训练数据仅约 1000 小时，远小于当前领先模型（如 π0 超过 10000 小时），导致某些操作（如关闭柜门、精细挂钩）迁移效果不佳。
  - ECoT 注释生成依赖 VLM 自动管道，可能引入标注噪声（虽覆盖率高但质量未分析）。
- **应用限制**：
  - 推理延迟 90‑100ms 虽满足大部分场景，但对于需要极高控制频率的任务（如高速操作）可能仍有差距。
  - 模型 2.6B 参数，算力要求较高，不利于资源受限的部署。
  - 未在更复杂、更接近真实的硬件平台（如双手臂无人干预环境）上验证。

（完）
