---
title: "Qantara: Bridge-Flow Training for Multi-Paradigm JEPA Control"
title_zh: "Qantara: 多范式JEPA控制的桥流训练"
authors: "Ruslan Rakhimov, George Bredis, Yuriy Maksyuta, Daniil Gavrilov"
date: 2026-07-06
pdf: "https://arxiv.org/pdf/2607.04978v1"
tags: ["query:rl-vla-world"]
score: 9.0
evidence: 潜在世界模型用于控制
tldr: 现有JEPA世界模型训练时只能选择单一推理范式（轨迹优化或行为克隆），无法在推理时灵活切换。Qantara提出联合训练目标，通过在状态轴使用布朗桥插值与在动作轴使用噪声到数据流匹配，使同一checkpoint支持潜在规划、行为克隆和逆动态三种范式。在LeWM控制套件上平均成功率91.2，在OGBench-Cube上超越之前方法7.7 SR。这推动了JEPA从单范式规划器向多范式控制器的发展。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.04978v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1441, \"height\": 541, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.04978v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1376, \"height\": 1249, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.04978v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1381, \"height\": 1241, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.04978v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1085, \"height\": 300, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.04978v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1453, \"height\": 267, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.04978v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1376, \"height\": 531, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.04978v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1270, \"height\": 291, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.04978v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1143, \"height\": 206, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.04978v1/table-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1018, \"height\": 423, \"label\": \"Table\"}]"
motivation: 现有JEPA世界模型训练时承诺单一推理范式，限制了部署时的灵活性。
method: 在状态轴使用布朗桥插值连接连续潜变量，在动作轴使用噪声到数据流匹配，联合训练使模型支持多种推理范式。
result: 在LeWM套件平均91.2 SR，OGBench-Cube提升7.7 SR，行为克隆和逆动态路径也达82-83 SR。
conclusion: Qantara将JEPA从单范式规划器扩展为多范式控制器，提升部署灵活性。
---

## 摘要
联合嵌入预测架构（JEPAs）支撑了一个日益庞大的潜在世界模型家族，用于从原始像素进行控制，但每个现有的JEPA世界模型在训练时都承诺采用单一推理范式：要么在学习的动力学模型中进行轨迹优化，要么进行直接行为克隆。如果同一检查点能够同时服务于两者，则可以将这种选择推迟到推理时，此时部署约束（推出成本、观测可访问性）决定了哪条路径更优。我们提出了Qantara，一种端到端的JEPA，其联合训练目标将状态轴上连续干净潜在之间的布朗桥插值与动作轴上的噪声到数据流匹配相结合。同一检查点无需重新训练即可服务于三种推理范式：潜在规划、行为克隆动作采样和逆动力学，我们通过一种视频逆组合来查询这些范式，该组合首先在不依赖动作条件的情况下预测下一个潜在变量，然后提取动作。训练将质量集中在（动作时间，状态时间）噪声正方形的边缘，推理时查询预测器：用均匀内部采样替换它，在相同计算量下将Push-T规划从90.1 SR降至53.3 SR。在LeWM控制套件上，Qantara达到91.2 SR的三次训练种子平均值，并在OGBench-Cube上创下新的SOTA（比DINO-WM高+7.7 SR，比LeWM高+19.7 SR）。在相同权重下，行为克隆和视频逆路径在Push-T上达到82-83 SR，在Cube上达到71-73 SR。这些结果将JEPA世界模型从单范式规划器转变为多范式控制器。

## Abstract
Joint-Embedding Predictive Architectures (JEPAs) underpin a growing family of latent world models for control from raw pixels, but every existing JEPA world model commits at training time to a single inference paradigm: either trajectory optimisation in a learned dynamics model, or direct behaviour cloning. A single checkpoint that serves both would defer this choice to inference, when deployment constraints (rollout cost, observation accessibility) determine which path wins. We present Qantara, an end-to-end JEPA whose joint training objective pairs a Brownian-bridge interpolant between consecutive clean latents on the state axis with noise-to-data flow matching on the action axis. The same checkpoint serves three inference paradigms without retraining: latent planning, behaviour-cloning action sampling, and inverse dynamics, which we query through a video-inverse composition that first predicts the next latent without action conditioning, then extracts the action. Training concentrates mass on the edges of the (action-time, state-time) noise square, where inference queries the predictor: replacing it with uniform interior sampling drops Push-T planning from 90.1 to 53.3 SR at matched compute. On the LeWM control suite, Qantara reaches a 91.2 SR three-train-seed average and sets new SOTA on OGBench-Cube (+7.7 SR over DINO-WM, +19.7 over LeWM). From the same weights, the behaviour-cloning and video-inverse paths reach 82-83 SR on Push-T and 71-73 SR on Cube. These results move JEPA world models from single-paradigm planners to multi-paradigm controllers.

---

## 论文详细总结（自动生成）

## 1. 论文的核心问题与整体含义

- **研究动机**：现有的JEPA（联合嵌入预测架构）世界模型在训练时都承诺单一推理范式——要么是轨迹优化（规划），要么是直接行为克隆。这导致部署时无法根据实时约束（如延迟、观测可访问性）灵活切换范式。一个能同时支持多种推理范式的单一检查点，可以将范式选择推迟到推理时刻，从而提升部署灵活性。
- **整体含义**：Qantara首次证明了在子十亿参数规模的JEPA模型中，可以通过联合训练目标使同一组权重同时服务于潜在规划、行为克隆和逆动态三种推理范式，从而将JEPA从单范式规划器拓展为多范式控制器。

## 2. 方法论

- **核心思想**：在训练时对状态轴使用布朗桥插值（连接连续干净潜在变量），对动作轴使用噪声到数据流匹配。通过边缘对齐的噪声水平采样，将训练能力集中在推理时实际查询的（τ_a, τ_z）噪声正方形边缘上，避免浪费在无人问津的内部分布。
- **关键技术细节**：
  - **模型架构**：由编码器E_ϕ（将像素帧映射为潜在z_t）和预测器f_θ组成。预测器采用块因果Transformer：跨块因果注意力、块内双向注意力，使得同一块内的动作和状态令牌可以联合去噪。
  - **联合桥流匹配**：
    - 动作轴：使用噪声到数据流匹配，˜a_t = τ_a a_t + (1-τ_a)ε_a。
    - 状态轴：使用布朗桥插值，˜z_{t+1} = (1-τ_z)z_t + τ_z z_{t+1} + γ√[τ_z(1-τ_z)]ε_z，其中τ_z=0的源端正是前一个干净潜在变量，使得模型只需学习流形上的过渡。
  - **训练目标**：状态桥损失L_z_B = E[∥ˆz_{t+1} - z_{t+1}∥²]，动作流匹配损失L_a_FM = E[∥v_a_t - (a_t - ε_a)∥²]，以及SIGReg正则化器防止编码器坍缩。最终损失L = λ_z L_z_B + λ_a L_a_FM + λ_SIG SIGReg。
  - **边缘对齐(τ_a, τ_z)采样**：训练时只采样噪声正方形的四个边缘（对应三种推理范式和一个视频逆组合）加上对角线联合模式，共五种模式。每个批次行在这五种模式下复制，避免内部分布的浪费。
  - **三种推理范式**（无需重新训练）：
    - **潜在规划**（forward边缘）：τ_a=1, τ_z从0→1，预测器作为前向动力学模型，用CEM搜索动作序列最小化终端潜在与目标之间的距离。
    - **行为克隆采样**（policy边缘）：τ_z=0, τ_a从0→1，状态槽固定为z_t，动作槽从高斯噪声通过K步Euler方法去噪得到动作。
    - **视频-逆组合**：先通过video边缘（τ_a=0, τ_z从0→1）预测无动作条件下的下一个潜在ˆz_{t+1}，再通过inverse边缘（τ_z=1, τ_a从0→1）恢复出桥接这两个潜在的动作。

## 3. 实验设计

- **数据集与场景**：使用LeWM套件，包含四个环境：Two-Room（2D导航）、Push-T（2D接触式操作）、OGBench-Cube（3D抓取放置）、Reacher-Hard（精确电机控制）。所有环境使用连续动作空间，数据来自专家级示范（人类/脚本/RL策略）。
- **Benchmark**：端到端基于像素的世界模型，评价指标为成功率（SR）。对比方法包括PLDM、DINO-WM、LeWM（论文报告值和复现值）。
- **对比方法**：PLDM（VICReg正则化）、DINO-WM（冻结DINOv2）、LeWM（各向同性高斯正则化）。Qantara在相同管道中与这些方法进行公平比较。

## 4. 资源与算力

- 论文未明确说明使用的GPU型号、数量、训练时长等具体硬件信息。仅提及训练使用AdamW优化器，batch size 128（有效batch 640因五种模式复制），bf16混合精度，学习率3e-4，10个epoch约50k步优化。模型参数量约21M。

## 5. 实验数量与充分性

- **主要实验**：在四个环境上进行三次训练种子平均，每次评估50 episodes × 5评估种子，共750 episodes。结果报告均值±标准差。
- **消融实验**：包括边缘对齐采样 vs. 均匀采样、模式集消融（移除五种模式中的一种或多种）、状态头残差初始化、布朗桥γ值扫描、CEM去噪步数K扫描、dropout消融等。每组消融实验均进行多种子评估。
- **公平性**：与LeWM在相同管道中复现比较，确保数据、训练框架、评估预算一致。但直接引用的PLDM和DINO-WM结果来自原论文，可能存在协议差异。
- **充分性**：消融实验覆盖了主要设计选择（采样策略、模式集、残差连接、γ、K等），且揭示了反直觉的联合正则化效应（移除任意一个非forward模式会导致Push-T训练不稳定）。实验设计较为充分。

## 6. 主要结论与发现

- Qantara在LeWM套件上平均91.2 SR，在OGBench-Cube上达到93.7 SR（+7.7优于DINO-WM，+19.7优于LeWM）。
- 同一检查点可在不重新训练的情况下支持三种推理范式：潜在规划（CEM）90.1 SR（Push-T）、行为克隆82.1 SR、视频-逆组合83.2 SR，且后两者推理速度比规划快15-65倍。
- 边缘对齐采样显著优于均匀采样（Push-T CEM从90.1降至53.3）。移除任何单一模式（policy/inverse/joint）都会导致Push-T训练不稳定，即使该模式对应的噪声角并非规划路径所查询，这表明五种模式通过共享主干产生了联合正则化效应。
- 状态头残差形式+零初始化是必要的（除去后Push-T下降15.2 SR且训练不稳定）。
- 布朗桥γ=1或√2为佳，偏离会导致坍缩。

## 7. 优点

- **创新性**：首次在子十亿参数JEPA中实现多范式推理，将范式选择推迟到推理时。
- **实用性**：三种推理范式从同一检查点提供，无需重新训练，且BC/视频逆路径具有低延迟（17-24 ms）优势。
- **方法优雅**：利用布朗桥插值和流匹配统一处理状态和动作，边缘对齐采样直观且有效。
- **实验充分**：包含全面的消融和鲁棒性分析，揭示了联合正则化等非平凡现象。

## 8. 不足与局限

- **推理层面限制**：所有三种范式每次控制步骤仅去噪一个时间块，未扩展到分块多步推理（chunked multi-block inference）。
- **应用范围**：仅在仿真环境中的近专家离线数据上评估，未涉及真实机器人转移或自然视频预训练。
- **规模有限**：仅在约21M参数规模上验证，未测试更大JEPA主干（如V-JEPA-2-AC）的扩展性。
- **环境覆盖**：目标检测环境（如Reacher）需要精确关节匹配，行为克隆和视频逆路径在该环境下性能极低（6.1/4.8 SR），说明通用性仍有待提升。
- **数据需求**：行为克隆路径不能超过演示策略的质量，视频逆组合需要近乎专家的轨迹覆盖以确保想象的未来能接近目标，对数据质量要求较高。

（完）
