---
title: "The Mirage of Optimizing Training Policies: Monotonic Inference Policies as the Real Objective for LLM Reinforcement Learning"
title_zh: 优化训练策略的幻象：单调推理策略作为LLM强化学习的真正目标
authors: "Jing Liang, Hongyao Tang, Yi Ma, Yancheng He, Weixun Wang, Xiaoyang Li, Ju Huang, Wenbo Su, Jinyi Liu, Yan Zheng, Jianye Hao, Bo Zheng"
date: 2026-06-28
pdf: "https://arxiv.org/pdf/2606.29526v1"
tags: ["query:rl-vla-world"]
score: 9.0
evidence: 分析LLM强化学习中的训练-推理不匹配；提出单调推理策略
tldr: 大语言模型强化学习后训练中，训练与推理引擎分离导致概率不一致，引发离线策略问题，使训练不稳定。现有方法虽尝试解决，但忽略了训练策略更新未必提升推理策略。本文提出单调推理策略改进（MIPI）原则，并设计MIPU框架，通过构建采样器参考候选更新，利用推理侧差距代理选择性同步，实现推理策略单调提升。实验表明，在高不匹配场景下，MIPU显著提升推理性能与训练稳定性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.29526v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1431, \"height\": 204, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.29526v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1460, \"height\": 237, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.29526v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 744, \"height\": 388, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.29526v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 905, \"height\": 385, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.29526v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1492, \"height\": 314, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.29526v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 496, \"height\": 343, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.29526v1/fig-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 1491, \"height\": 326, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.29526v1/fig-008.webp\", \"caption\": \"\", \"page\": 0, \"index\": 8, \"width\": 1491, \"height\": 409, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.29526v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1492, \"height\": 459, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.29526v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1359, \"height\": 299, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.29526v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1059, \"height\": 1112, \"label\": \"Table\"}]"
motivation: 训练-推理引擎不匹配导致的离线策略问题，使得训练策略优化无法保证推理策略提升，需要针对性目标对齐。
method: 提出MIPI原则，并设计MIPU两步框架：先构建采样器参考候选策略更新，再通过推理侧概率差距代理选择接受同步候选。
result: 在两种模型规模的高不匹配实验中，MIPU相比基线显著提升平均推理准确率和训练稳定性，减少训练崩溃。
conclusion: 将优化目标从训练策略转向推理策略单调提升，能有效应对训练-推理不匹配，为LLM强化学习提供更稳健的优化范式。
---

## 摘要
强化学习（RL）在大语言模型（LLM）后训练中日益受到关注，然而RL训练仍然脆弱，可能出现不稳定或崩溃。一个重要原因是训练与推理的不匹配：LLM采用分离的推理引擎和训练引擎以兼顾生成效率和训练精度，但在实践中，即使模型参数同步，同一轨迹在训练端和推理端也会表现出不一致的概率。这自然导致了一种特殊的离策略性问题持续存在并毒害训练。以往的工作在解决离策略性以稳定不匹配下的训练策略方面做出了各种努力。在本文中，我们指出了现有工作忽视的目标错位：对训练引擎中策略的有效更新并不一定能保证推理策略（即部署中使用的策略）的改进。为此，我们提出了一种新的LLM RL策略优化目标，称为单调推理策略改进。遵循这一原则，我们引入了单调推理策略更新，这是一种两步LLM RL框架，它构建采样器参考候选更新，并使用推理端差距代理选择性地接受同步后的候选。在两个模型规模下高不匹配场景的实验表明，MIPU提高了平均推理性能和训练稳定性。

## Abstract
Reinforcement learning (RL) has gained growing attention in large language model (LLM) post-training, yet RL training remains fragile and can suffer from instability or collapse. One vital cause is training-inference mismatch: LLM adopts separate inference and training engines for generation efficiency and training precision, which in practice exhibits inconsistent probabilities for the same trajectories on training and inference sides, even with synchronized model parameters. This naturally induces a special type of off-policyness ever existing and poisoning the training. Prior works have made various efforts in addressing the off-policyness to stabilize the training policies under the mismatch. In this paper, we point out the objective misalignment neglected by existing works that an effective update to the policy in the training engine not necessarily ensures the improvement of the inference policy, i.e., the one used in deployment. To this end, we propose a new policy optimization objective for LLM RL, named Monotonic Inference Policy Improvement (MIPI). Following this principle, we introduce Monotonic Inference Policy Update (MIPU), a two-step LLM RL framework that constructs sampler-referenced candidate updates and selectively accepts synchronized candidates using an inference-side gap proxy. Experiments conducted on two model scales under high mismatch show that MIPU improves average reasoning performance and training stability.

---

## 论文详细总结（自动生成）

### 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：大语言模型（LLM）强化学习后训练中普遍存在训练-推理不匹配（training-inference mismatch）问题。现代LLM RL流水线将生成（rollout）与梯度计算分离，分别由推理引擎（如vLLM）和训练引擎（如Megatron）执行。即使模型参数同步，由于精度、解码方式等实现差异，同一轨迹在训练策略（π）和推理策略（μ）上的概率不一致，导致一种特殊的离策略性（off-policyness），进而引发训练不稳定甚至崩溃。
- **现有工作的不足**：先前方法（如TIS、MIS、LR-decay等）致力于从算法或系统层面减小不匹配，但忽略了目标层面的错位：训练策略的有效更新并不保证同步后的推理策略性能提升。传统的RL优化目标是提高训练策略J(π)，而实际部署和生成依赖推理策略J(μ)。因此需要将优化目标重新对准推理策略的单调提升。

### 论文提出的方法论

- **核心思想**：提出**单调推理策略改进（Monotonic Inference Policy Improvement, MIPI）**原则，即策略优化的目标应是保证推理策略μ的性能单调提升，而非仅关注训练策略π。基于此设计**单调推理策略更新（Monotonic Inference Policy Update, MIPU）**框架，分为两步：
    - **Step 1：采样器参考策略更新（Sampler-Referenced Policy Update）**  
        构造候选训练策略π_{k+1}，目标最小化J(π_{k+1}) - J(μ_k)（即式(5)中②+③项）。采用截断的重要性采样权重（truncated importance sampling, TIS），将预不匹配权重与当前更新比率解耦，并应用PPO风格裁剪。优化目标如式(7)所示，保证理论上半部分单调性J(π_{k+1}) - J(μ_k) ≥ 0。
    - **Step 2：推理间隙感知接受（Inference-Gap-Aware Acceptance）**  
        在同步候选策略μ_{k+1}后，估计后更新推理间隙Ť_post = J(μ_{k+1}) - J(π_{k+1})（即式(5)中①项）。基于反向性能差恒等式和长度归一化重要性权重构造近似代理。若Ť_post ≥ -c（c为容忍参数），则接受更新并推进；否则回滚到前一个检查点，避免有害同步。这一步保证下半部分单调性J(μ_{k+1}) - J(π_{k+1}) ≥ 0。
- **算法流程**：初始化策略和优化器，对每步k重复：保存检查点；使用推理策略μ_k收集训练批次并计算奖励和优势；优化式(7)得到候选π_{k+1}并同步为μ_{k+1}；在验证批次上计算Ť_post；根据接受准则决定接受或回滚。

### 实验设计

- **数据集与场景**：
    - **训练数据**：DAPO-Math-17和DeepMath-103K，经过筛选保留基础模型能解决但非饱和的题目（Qwen3-1.7B用5759例，Qwen3-4B用1491例）。
    - **评估数据集**：MATH-500、AIME24、AMC23、Minerva、OlympiadBench五个数学推理基准。报告pass@1准确率，对小型基准（AIME24、AMC23）使用avg@16。
    - **实验场景**：高不匹配设置——FP8量化推理（FP8-quantized rollout），量化放大训练-推理间隙。
- **基准（Baselines）**：
    - 标准GRPO（基线）
    - MIS（token级不匹配过滤）
    - LR-decay（学习率衰减）
    - 消融中还包括：仅Step1（TIS）、仅Step2、以及随机回滚控制（验证Step2有效性）。
- **对比方法**：与GRPO、MIS、LR-decay比较，并单独分析Step1和Step2的贡献。

### 资源与算力

- **明确说明**：实验使用8块H100 GPU，基于ROLL框架（Wang et al., 2025），训练引擎为Megatron，推理引擎为vLLM。论文未给出具体训练时长（如步骤数或小时数），但训练步骤数在图中显示约800步左右（Qwen3-4B）和类似规模。

### 实验数量与充分性

- **实验数量**较充分：
    - 主实验：两个模型规模（1.7B和4B）在五个基准上对比四种方法，共2×5×4=40个数据点（表1）。
    - 训练动态可视化（图2）：展示了Qwen3-4B的训练曲线。
    - 消融实验（表2、图3）：对比基线、+Step1、+Step2、完整方法，在四个基准上（除AMC23外）及训练指标（得分、不匹配KL、推理间隙、回滚率）。
    - 附加分析：Step2 vs 随机回滚（图4b）、容忍参数c的敏感性（图6）、Step1实现变体（PPO-IS、Vanilla-IS、TIS）对比（图5，附录B）。
- **公平性与客观性**：评估采用统一协议（pass@1、avg@16），超参数固定（表3），且报告了“稳定”列表示方法是否避免崩溃。消融设计合理，逐一剥离组件验证贡献。控制随机回滚的rollback率匹配Step2单独运行时的回滚率，说明Step2的信号是有用的而非单纯减少更新次数。

### 论文的主要结论与发现

1. **性能提升**：在高不匹配FP8量化rollout下，MIPU在两个模型规模上平均性能最佳（Qwen3-4B: 66.71%, Qwen3-1.7B: 53.97%），显著优于GRPO、MIS、LR-decay。
2. **训练稳定性**：MIPU能够避免其他方法在持续训练中出现的性能崩塌或急剧下降，保持稳定轨迹。
3. **组件互补**：Step1（采样器参考更新）改善候选更新方向；Step2（推理间隙感知接受）过滤不可靠的同步候选。两者结合比单独使用效果更好。
4. **Step2信号有效性**：后更新推理间隙Ť_post携带结构化不匹配信号（较小模型间隙更大、训练过程中变化），用于接受/回滚优于随机回滚，且需要校准容忍度而非简单严格拒绝。

### 优点

- **问题视角新颖**：首次从目标层面对齐角度处理训练-推理不匹配，而非仅视为系统差异或算法修正。
- **方法设计简洁有效**：两步骤各有明确目标，通过分解推理策略改进项（式5）自然导出，且实现灵活（Step1可用TIS等多种变体，Step2可替换不同代理）。
- **实验严谨**：在两个模型尺度、五个基准上验证，消融和分析充分，排除混淆因素（如随机回滚控制），并分析了容忍参数敏感性。
- **实际意义强**：解决LLM RL部署中的核心不稳定因素，为大规模训练提供稳健优化范式。

### 不足与局限

- **模型规模有限**：仅实验了1.7B和4B模型，未验证更大模型（如7B/13B/70B）或更多样化的训练系统，不匹配模式可能在不同规模下变化。
- **算力约束**：论文提及“significant computational constraints”，但未提供详细训练时长或资源消耗对比，可能影响可复现性评估。
- **Step2代理的近似性**：Ť_post是基于反向恒等式和长度归一化重要性权重的近似，并非精确估计，可能引入噪声；接受的阈值c需要动态调整，存在额外超参数。
- **单一量化场景**：仅测试了FP8量化rollout这一种高不匹配设置，未涉及其他来源（如不同解码算法、分布式框架差异）的不匹配。
- **方法局限性**：Step2的回滚机制可能丢弃一些有益更新（若代理误判），且要求额外验证批次和计算开销，效率方面未深入讨论。

（完）
