---
title: "Keypose Exploration: Efficient Automatic Trajectory Labelling and Cross-Embodiment Policy Transfer"
title_zh: 关键位姿探索：高效的自动轨迹标注与跨实体策略迁移
authors: "Yupu Lu, Hang Xu, Yizhou Chen, Jia Pan"
date: 2026-06-27
pdf: "https://arxiv.org/pdf/2606.29028v1"
tags: ["query:rl-vla-world"]
score: 9.0
evidence: 利用视觉语言模型自动标注轨迹，关键点引导策略用于机器人操作
tldr: 针对基于关键点的操作任务，现有方法依赖人工或启发式标注关键点。本文提出自动轨迹标注流水线，结合视觉语言模型进行语义事件检测与经典轨迹分析实现时间对齐，每个任务仅需对单个演示进行一次VLM推理。利用标注数据训练关键点引导的扩散策略，通过可达性图过滤候选关键点实现跨实体零样本迁移。在robomimic任务上实验表明，标注数据训练的策略性能与标准扩散策略基线相当，且可达性过滤在多模态插入任务中有利于零样本迁移。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.29028v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1831, \"height\": 1001, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.29028v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 880, \"height\": 674, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.29028v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1839, \"height\": 1362, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.29028v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 795, \"height\": 183, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.29028v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 846, \"height\": 533, \"label\": \"Table\"}]"
motivation: 减少关键点标注的人工成本，并探索关键点引导策略在跨实体迁移中的潜力。
method: 使用VLM检测语义事件并借助轨迹分析对齐时间，仅需单个演示的VLM推理；训练关键点引导的扩散策略，通过可达性图过滤关键点实现跨实体迁移。
result: 在robomimic任务上标注数据训练策略与标准DP基线性能相当，可达性过滤在多模态插入任务中零样本迁移有效。
conclusion: 自动标注流水线可行，关键点引导策略有潜力实现跨实体迁移，但需确保候选关键点可行。
---

## 摘要
基于关键位姿的操控将任务分解为关键路径点，以简化长时域任务的策略学习，但现有方法依赖任务特定的启发式规则或人工标注从演示中提取关键位姿。我们提出了一种针对抓取相关任务的自动轨迹标注流程。该流程结合了用于语义事件检测的视觉-语言模型（VLM）和用于精确时间对齐的经典轨迹分析，且每个任务仅需对重复演示中的单个样本进行一次VLM推理。利用标注数据，我们训练了一种关键位姿引导的扩散策略（DP），该策略利用关键位姿条件作用来干预演示分布。我们探索了将此性质应用于跨实体迁移的可能性：通过可达性图对候选关键位姿进行采样和过滤，将策略引导向目标机器人的运动学可行关键位姿。作为初步可行性研究，在两个robomimic任务上的实验表明，标注数据产生的策略与标准DP基线相匹配，并且在多模态插入任务中，当存在可行候选位姿时，经可达性过滤的关键位姿条件可能有利于零样本迁移。

## Abstract
Keypose-based manipulation decomposes tasks into critical waypoints to simplify policy learning for long-horizon tasks, but existing approaches rely on task-specific heuristics or manual annotation to extract keyposes from demonstrations. We present an automatic trajectory labelling pipeline for grasp-related tasks. This pipeline combines vision-language models (VLMs) for semantic event detection with classical trajectory analysis for precise temporal alignment, requiring VLM inference only on one single demo among repeating ones per task. Using the labelled data, we train a keypose-guided Diffusion Policy (DP) that exploits keypose conditioning to intervene demonstration distributions. We explore the possibility to apply this property for cross-embodiment transfer: candidate keyposes are sampled and filtered via a reachability map, steering the policy toward kinematically feasible keyposes for the target robot. As a preliminary feasibility study, experiments on two robomimic tasks show that the labelled data produces policies matching a standard DP baseline, and that reachability-filtered keypose conditioning may benefit zero-shot transfer on the multimodal insertion task when feasible candidates are available.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 核心问题与整体含义（研究动机和背景）

- **研究动机**：基于关键位姿（keypose）的操控方法将长时域任务分解为关键的末端执行器位姿序列，可简化策略学习并提高可解释性。然而，现有方法提取关键位姿严重依赖人工干预（如手工启发式规则或人工标注），缺乏灵活性和可扩展性。此外，如何利用关键位姿的特性（如多模态分布）进行跨实体（cross-embodiment）零样本迁移仍是一个开放问题。
- **整体含义**：本文旨在解决关键位姿自动标注的瓶颈，并探索关键位姿条件作用在跨实体策略迁移中的潜力，为未来更通用的机器人学习系统提供初步基础。

## 2. 方法论：核心思想、关键技术细节

- **核心思想**：组合视觉-语言模型（VLM）的语义理解能力与经典轨迹分析的精确对齐能力，实现自动标注；通过关键位姿条件作用解耦高层模式选择与低层轨迹生成，并利用可达性图过滤实现跨实体迁移。
- **关键技术细节**：
    1. **VLM引导的子任务分析**：使用 Qwen3-VL-235B 对单个演示视频（均匀采样帧）进行结构化分析，输出包含“定位（targeting）”、“交互（interaction）”、“结果（result）”三阶段的事件 JSON（含事件类型、时间戳）。每个任务仅需一次 VLM 推理。
    2. **基于轨迹的自动事件校正**：构建运动时间线（双层分割：快速运动 + 细微运动检测）和夹爪时间线，定义语义-运动对应规则（如 grasp 对齐夹爪闭合起始帧，detach 对齐夹爪闭合结束与运动开始的时间等），将 VLM 估计的帧精确对齐到物理运动边界。
    3. **关键位姿预测网络**：采用一致性模型（Consistency Model, CM）作为预测器，通过 EMA 蒸馏训练，单步生成多个候选关键位姿（N_cand=24），同时预测子任务阶段辅助稳定。
    4. **关键位姿引导的扩散策略（KP-DP）**：将预测的关键位姿与观测拼接到一起形成条件 c，条件化扩散去噪模型生成动作轨迹。训练损失为标准的噪声预测损失。
    5. **可达性过滤与迁移**：预先建立目标机器人的可达性图（RichMap，0.05 m 网格），对候选关键位姿进行聚类（最多3个方向模式），选取组成值（可达候选数 × 最佳置信度）最高的模式中的最佳置信度候选；若无可达候选则回退到最高置信度候选。此过程为“提出-过滤-生成”零样本迁移。

## 3. 实验设计

- **数据集与场景**：使用 robomimic 环境中的两个任务：
    - **Can**：拾放任务（固定放置位，单一抓取方向，几乎单峰分布）。
    - **Square**：将方形螺母插入方形栓的任务（三种不同插入方向，多模态分布）。
- **Benchmark**：标准 Diffusion Policy（DP）作为基线。
- **对比方法**：
    - **DP**：标准扩散策略（无关键位姿条件）。
    - **KP**：本文关键位姿引导策略（无需可达性过滤）。
    - **KP+Reach**：KP + 提出-过滤-生成流程（加入可达性约束）。
- **实验设置**：训练集为 200 条 Franka Panda 演示；评估零样本迁移到 Kinova3 和 UR5e 两个目标机器人。所有输入为低维状态向量（23维），不包含图像，以隔离视觉域偏移。
- **评估指标**：成功率（各条件在 9 个检查点上的均值±标准差，每个检查点评估 50 个回合）。

## 4. 资源与算力

- **文中未明确说明使用的 GPU 型号、数量或训练时长**。仅提及使用了 Qwen3-VL-235B（大语言模型）进行一次推理，但未给出其硬件需求。训练关键位姿预测器（0.37M 参数）和扩散策略（8.97M 参数）的算力成本也未量化。因此，无法提供具体资源消耗数据。

## 5. 实验数量与充分性

- **实验数量**：
    - 两个任务（Can / Square）× 三种方法（DP / KP / KP+Reach）× 三个机器人（Panda / Kinova3 / UR5e）= 18 组条件。每组条件基于 3 个训练种子 × 3 个检查点时期 = 9 个检查点，每个检查点 50 个回合，总计约 900 次评估（实际为 18×9×50=8100 个回合，但因同一组内标准差的统计方式，报告 9 个检查点的均值和标准差）。
    - 额外测量了候选可达性（UR5e 约 99% 可达，Kinova3 约 0.1% 可达）。
- **充分性评估**：
    - **充分性不足**：作者明确声明这是一项“初步可行性研究”，且实验仅覆盖了两个模拟任务（均短时域），未涉及真实机器人、长时域任务或多阶段任务。输入限定为低维状态，忽略了视觉域偏移。Kinova3 上的可达性几乎为零，导致过滤效果无法体现。
    - **公平性**：对比基线 DP 使用相同演示数据，条件设置一致；归一化等处理共享；这些方面是公平的。但 KP 与 KP+Reach 均依赖自动标注数据，其标注质量本身可能对结果有影响（但已在 Panda 上验证与 DP 基线匹配）。
    - 缺少关键消融实验（如：去掉 VLM 仅用轨迹分析？不同 VLM 选择？不同候选数量？）。

## 6. 主要结论与发现

1. **自动标注质量有效**：在 Panda 源机器人上，KP/KP+Reach 的成功率（Can 100%/98.9%，Square 84.2%/87.3%）与 DP 基线（98.7%/86.2%）基本持平，证明自动标注的可靠性和关键位姿信息的语义丰富性。
2. **关键位姿条件作用有助于多模态任务迁移**：在 Square 任务（多模态插入）上，KP+Reach 在 UR5e 上达到 82.9%，优于 DP 的 73.8% 且方差降低，这表明关键位姿条件作用可指导模式选择（避免多模态平均化）并诱发靠近关键位姿时的保守行为（提高精度）。KP 本身也改善了稳定性（75.8%）。
3. **可达性过滤的收益受限于候选可行性**：UR5e 上几乎所有候选可达，过滤有效；Kinova3 上几乎无候选可达，回退到普通 KP，效果无明显提升（24.2% vs DP 22.0%）。说明软条件作用在跨实体差距大时受限。
4. **Can 任务上 KP 表现略低于 DP**：因单模态分布下额外条件带来轻微干扰。

## 7. 优点

- **自动标注流程创新**：巧妙结合 VLM 高层语义理解与低层物理运动分析，仅需一次 VLM 推理即可为整个任务集生成精确关键位姿，显著降低人工成本。
- **跨实体迁移的新思路**：利用关键位姿条件作用解耦模式选择与轨迹生成，并提出“提出-过滤-生成”零样本迁移框架，将运动学约束引入高层决策。
- **实验设计合理**：选择单模态（Can）和多模态（Square）任务对比，清晰展示了关键位姿条件作用的优势来源。使用低维状态输入隔离视觉漂移，使结论聚焦于运动学与多模态问题。
- **开源与可复现性**：方法描述详细，参数公开（表 I），依赖框架明确。

## 8. 不足与局限

- **关键位姿抽象适用范围窄**：当前方法仅针对抓取类操控（grasp-related tasks），对连续接触（如推、擦）或非抓取行为（如倒水、搅拌）不适用。本文承认这一限制。
- **标注仍依赖少量手工参数**：每任务需设定平滑窗口 w 和每轴速度阈值 θ_k，并非完全无参数。
- **实验规模有限**：
    - 仅两个模拟任务，均短时域（1-2个交互事件）；长时域、多步骤任务未验证。
    - 机器人种类少（Panda→Kinova3/UR5e），且 Kinova3 的可行性条件极端，无法体现过滤效果。
    - 未包含真实机器人实验。
- **关键位姿作为软条件约束力弱**：当跨实体运动学差异大（如 Kinova3）时，无法保证生成的动作一定可达，导致转移失败。
- **未考虑视觉域偏移**：输入使用低维状态，回避了视觉差异问题，与真实跨实体迁移中的视觉域差距脱节。
- **VLM 依赖与传播假设**：假设所有演示遵循相同子任务序列且无错误，对含有重试、犹豫的遥操作数据可能失效。
- **资源与算力未报告**：缺乏训练和推理的计算成本分析。

（完）
