---
title: "One Lens, Many Worlds : A Capability-Typed Interface for World-Model Interpretability"
title_zh: 一个镜头，多个世界：面向世界模型可解释性的能力类型接口
authors: "Bhavith Chandra Challagundla, Sanskar Pandey, Param Thakkar, Rishikesh Mallagundla, Yugandhar Reddy Gogireddy, Wenhao Lu, Hindol Roy Choudhury, Shravani Challagundla, Mohamed Deraz Nasr, Spursh Deshpande"
date: 2026-06-07
pdf: "https://arxiv.org/pdf/2606.09936v1"
tags: ["query:rl-vla-world"]
score: 9.0
evidence: 世界模型的统一可解释性接口
tldr: 现有世界模型架构多样（如PlaNet、Dreamer、IRIS、I-JEPA），可解释性方法共享原语但需重复实现，因为现有工具假设语言模型。本文提出WorldModelLens，基于能力类型适配器定义接口：每个模型实现四个必需方法并声明可选头部，通过单层钩子和缓存暴露时间索引激活、想象回放等。关键结果：统一接口使所有分析只需编写一次。贡献：降低世界模型可解释性开发成本，促进跨架构分析。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.09936v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 977, \"height\": 660, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.09936v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1119, \"height\": 474, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.09936v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 859, \"height\": 387, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.09936v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 789, \"height\": 419, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.09936v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1466, \"height\": 493, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.09936v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 937, \"height\": 235, \"label\": \"Table\"}]"
motivation: 世界模型架构碎片化导致可解释性工具重复实现，现有钩子缓存工具无法支持动作、环境步等概念。
method: "提出WorldModelLens，定义能力类型适配器接口，包括四个必需方法（encode, transition, initial state, sample）和可选头部声明，通过钩子层统一暴露激活和回放。"
result: 单一接口支持多种世界模型（RL和自监督），所有可解释性方法只需实现一次，无需针对架构改写。
conclusion: WorldModelLens降低了世界模型可解释性开发成本，通过类型化接口抽象共享结构，提升工具复用性。
---

## 摘要
世界模型如今构建在截然不同的计算基座上。基于潜循环状态空间的模型（如PlaNet和Dreamer系列）将观测压缩为循环状态；基于令牌的模型（如IRIS）将观测量化到学习得到的码本中，并使用变压器进行自回归预测；联合嵌入预测架构（如I-JEPA）在学习的潜在空间中进行预测，无需像素解码器。应用于这些模型的可解释性方法——包括探测、激活补丁、稀疏自编码器和惊喜分析——共享一组共同的基元，但由于现有的钩子和缓存工具假设了一个没有动作、环境步骤或想象展开概念的变压器语言模型，这些方法需要为每种架构从头重新实现。我们认为这种碎片化反映了工具而非模型的问题，而世界模型的共享结构可以通过一个小的类型接口来捕捉。我们提出WorldModelLens，这是一个围绕能力类型适配器组织的开源可解释性基础框架：每个模型实现四个必需方法（编码、转移、初始状态、采样），并通过显式的能力描述符声明一组可选的头部（解码、奖励、继续、行动者、评论家），使得强化学习和自监督世界模型成为一等公民，而无需彼此模仿。一个统一的钩子和缓存层在此接口上公开时间索引的激活、想象展开和干预回放，使每种分析只需编写一次。

## Abstract
World models are now built on substantially different computational substrates. Latent recurrent state-space models such as PlaNet and the Dreamer family compress observations into recurrent states; token-based models such as IRIS quantize observations into a learned codebook and predict autoregressively with a transformer; and joint-embedding predictive architectures such as I-JEPA predict in a learned latent space with no pixel decoder. The interpretability methods applied to these models, including probing, activation patching, sparse autoencoders, and surprise analysis, share a common set of primitives, yet they are re-implemented from scratch for each architecture because existing hook-and-cache tooling assumes a transformer language model with no notion of actions, environment steps, or imagined rollouts. We argue that this fragmentation reflects the tooling rather than the models, and that the shared structure of world models is captured by a small typed interface. We present WorldModelLens, an open-source interpretability substrate organized around a capability-typed adapter: every model implements four required methods (encode, transition, initial state, sample) and declares a set of optional heads (decode, reward, continue, actor, critic) through an explicit capability descriptor, so that reinforcement-learning and self-supervised world models are first-class without either imitating the other. A single hook and cache layer exposes time-indexed activations, imagination rollouts, and intervention replay over this interface, allowing each analysis to be written once.

---

## 论文详细总结（自动生成）

# 论文深度总结

## 1. 核心问题与整体含义（研究动机和背景）
- **问题**：世界模型架构高度分化（如潜循环状态空间模型 PlaNet/Dreamer、令牌模型 IRIS、联合嵌入预测模型 I-JEPA），而现有可解释性工具（如 TransformerLens）假设变压器语言模型，缺少对动作、环境步、想象展开等概念的支持，导致可解释性方法（探测、激活补丁、稀疏自编码器、惊喜分析）需为每种架构重新实现。
- **动机**：作者认为碎片化源于工具而非模型本质。世界模型共享相同结构（编码器、转移、读取头），可通过一个小的类型接口统一抽象，从而让分析代码只写一次，跨架构复用。
- **整体含义**：通过能力类型适配器（capability-typed adapter）和统一的钩子缓存层，显著降低世界模型可解释性开发成本，促进跨架构比较与移植。

## 2. 方法论：核心思想、关键技术细节、公式或算法流程
- **核心思想**：定义一个世界模型的形式化元组 M = <O, S, Z, A, ι, E, τ, H>。必需核心：编码器 E、转移 τ、初始状态 ι、采样 sample_z。可选头部集合 H ⊆ {gdec, grew, gcont, π, V}。
- **WorldModelLens 三层设计**：
    - **适配器层**：BaseModelAdapter 实现 encode、transition、initial_state、sample_z 四个必需方法，并声明可选头部能力描述符 WorldModelCapabilities（has_decoder, has_reward_head, has_continue_head, has_actor, has_critic, uses_actions, is_rl_trained）。
    - **钩子缓存层**：HookedWorldModel 包裹适配器，挂载命名钩子点，通过 HookCacheManager 缓存时间索引的激活，支持 run_with_cache（记录激活）和 run_with_hooks（安装临时函数用于编辑）。
    - **分析层**：所有分析（探测、补丁、SAE、惊喜分析等）针对统一接口编写，不依赖具体架构。
- **关键技术细节**：
    - 钩子点标识为 (组件名, 时间步)，支持范围语法（如 z[0:10], t=5.z）。
    - 想象回放（imagine）和干预回放（intervention replay）作为世界模型特有的原语。
    - 缓存支持设备卸载和半精度量化。
- **核心公式**：
    - 稀疏自编码器损失：\( L_{SAE}(x) = \|x - W_d f\|_2^2 + \lambda \|f\|_1 \)
    - 集成梯度：\( IG_i(x) = (x_i - x'_i) \int_0^1 \frac{\partial F(x' + \alpha (x - x'))}{\partial x_i} d\alpha \)
    - CKA 相似度（中心核对齐）
    - 惊喜：每步 KL 散度 \( D_{KL}(q_\theta(z|o_t,s_{t-1}) \| p_\theta(z|s_t)) \)
- **算法 1** 展示了激活补丁（activation patching）流程：先干净运行缓存，然后在扰动输入上安装补丁钩子，计算恢复率。

## 3. 实验设计
- **数据集/场景**：未指定具体环境或数据集。I-JEPA 案例研究使用图像（来自 ImageNet 类）。
- **Benchmark**：论文未定义显式基准，主要评估：
    - 接口覆盖度：实现六大世界模型家族（DreamerV1/2/3, PlaNet, TD-MPC2, IRIS, Decision Transformer, I-JEPA）。
    - 分析有效性：在 I-JEPA 预测器上比较集成梯度（IG）与注意力权重的 Spearman 相关系数。
    - 开销测量：在 I-JEPA 适配器上比较裸适配器、钩子挂载但未激活、完整缓存三种条件下的每步延迟。
- **对比方法**：未与其他可解释性工具直接对比。在 I-JEPA 实验中，对比了两种归因方法（集成梯度 vs. 注意力权重）的一致性。

## 4. 资源与算力
- **文中明确说明**：开销测量依赖特定硬件，但未给出 GPU 型号、数量、训练时长等具体信息。仅提到“per-step latency on the I-JEPA adapter”，“Hardware and averaging details to be finalized”。
- **结论**：论文未提供详细的算力资源说明。

## 5. 实验数量与充分性
- **实验数量**：
    - 覆盖度：列出了 6 个家族（加上 Ha-Schmidhuber 共 7 个）。
    - I-JEPA 深度案例：一个模型、4 层预测器，报告 Spearman 相关性（图 4）。
    - 开销测量：三个条件对比（表 2）。
- **充分性**：论文承认“深度演示仅在 I-JEPA 上”，跨架构完整研究正在进行但未完成。实验规模较小，缺乏消融和统计严谨性（如无多次运行误差棒清晰标注）。结论偏向初步，但方法论逻辑成立。

## 6. 主要结论与发现
- 统一接口能够覆盖主流世界模型家族（通过能力类型适配）。
- 在 I-JEPA 上，注意力和集成梯度归因一致性较弱（仅一层中等正相关），表明注意力权重并非可靠因果解释。
- 钩子层处于非活跃状态时仅增加约 12% 每步开销，适合永久在线遥测。
- 工程贡献：分析库一次编写即可应用于所有后端。

## 7. 优点
- **设计优雅**：通过能力类型适配器统一了 RL 和自监督模型，避免强行伪造头部。
- **抽象正确**：将世界模型可解释性从模型特定代码中解脱出来，降低维护成本。
- **原语完整**：引入了想象回放和干预回放等世界模型特有操作，超越语言模型工具。
- **开源实现**：WorldModelLens 提供可复现的工程基础。
- **开销低**：12% 开销使得在训练循环中默认开启成为可能。

## 8. 不足与局限
- **实验覆盖不足**：仅在 I-JEPA 上做了深度分析，其他家族（Dreamer 等）仅列出适配器，无实际可解释性结果。
- **缺乏跨架构比较**：论文未展示同一分析在多个模型上的具体表现，未验证“写作一次”的实际益处。
- **基准与对比缺失**：未与现有工具（如 TransformerLens、Captum、NNsight）进行定量对比（性能、功能、易用性）。
- **算力细节缺失**：未给出 GPU 型号、步数、标准偏差等信息，难以复现开销测量。
- **应用限制**：仅对视觉世界模型验证，未讨论文本或控制领域；尚未加载原始官方检查点（适配器为再实现）。
- **警告**：论文开头 arXiv 时间戳为 2026 年 6 月，可能为预印本或已知错误年份，需注意时效性。

（完）
