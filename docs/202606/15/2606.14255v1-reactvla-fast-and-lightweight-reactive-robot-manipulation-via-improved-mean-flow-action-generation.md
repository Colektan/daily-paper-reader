---
title: "ReactVLA: Fast and Lightweight Reactive Robot Manipulation via Improved Mean Flow Action Generation"
title_zh: ReactVLA：基于改进平均流动作生成的快速轻量级反应式机器人操作
authors: "Yanzhao Guo, Wenkai Chen, Jianwei Zhang"
date: 2026-06-12
pdf: "https://arxiv.org/pdf/2606.14255v1"
tags: ["query:rl-vla-world"]
score: 9.0
evidence: 用于快速机器人操作的视觉语言动作模型
tldr: 基于扩散的策略虽能建模多模态动作分布，但迭代采样带来高延迟，限制机器人实时操控。本文提出ReactVLA，通过改进的均值流（iMF）动作生成器将多步扩散减少到一步或几步，并设计注意力残差（AttnRes）动态保留任务相关特征。在LIBERO等基准及真实任务上，ReactVLA性能提升最高1.65倍，推理速度提高4倍以上，延迟低于38.6毫秒，实现快速反应控制。该工作为轻量级实时机器人操控提供了有效方案。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.14255v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 625, \"height\": 484, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.14255v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1456, \"height\": 751, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.14255v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 914, \"height\": 341, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.14255v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 540, \"height\": 420, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.14255v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 903, \"height\": 365, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.14255v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1788, \"height\": 891, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.14255v1/fig-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 1802, \"height\": 414, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.14255v1/fig-008.webp\", \"caption\": \"\", \"page\": 0, \"index\": 8, \"width\": 895, \"height\": 248, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.14255v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1469, \"height\": 428, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.14255v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1253, \"height\": 246, \"label\": \"Table\"}]"
motivation: 扩散式VLA策略因迭代采样导致推理延迟高，难以满足机器人实时闭环操控需求。
method: 提出ReactVLA框架，包含改进均值流（iMF）动作生成器实现少步采样，以及注意力残差（AttnRes）特征路由机制保留多模态表征。
result: 在LIBERO等基准上性能提升1.65倍，推理速度提高4倍以上，真实环境延迟低于38.6毫秒。
conclusion: ReactVLA以轻量架构实现了快速反应式机械臂操控，兼顾性能与实时性。
---

## 摘要
基于扩散的视觉-语言-动作（VLA）策略在建模表达性和多模态动作分布方面表现出了强大的能力。然而，它们对迭代采样的依赖引入了显著的推理延迟，限制了其在反应式闭环机器人操作中的应用。为了解决这一限制，我们提出了\texttt{ReactVLA}，一个用于实时机器人操作的轻量级低延迟VLA框架。\texttt{ReactVLA}结合了两个互补的设计：（1）改进的平均流（iMF）动作生成器，将昂贵的多步扩散采样减少到一步或几步的动作生成；（2）注意力残差（AttnRes），一种动态深度特征路由机制，替代均匀残差累积以更好地保留任务相关的多模态表示。我们在大规模模拟基准（包括LIBERO和RoboIMI）以及真实世界机器人操作任务上评估了\texttt{ReactVLA}。实验结果表明，\texttt{ReactVLA}始终优于同等规模的VLA基线模型，包括SmolVLA和$π_0$。在具有挑战性的精密操作任务上，与领先的VLA模型相比，\texttt{ReactVLA}在任务性能上实现了高达1.65倍的提升，同时推理速度提高了4倍以上。最后，它将真实世界策略延迟降低到38.6毫秒以下，从而在物理机器人平台上实现了快速反应式控制。请访问我们的项目网站：https://game-loader.github.io/ReactVLA/。

## Abstract
Diffusion-based Vision-Language-Action (VLA) policies have demonstrated strong capability in modeling expressive and multimodal action distributions. However, their reliance on iterative sampling introduces substantial inference latency, which limits their applicability to reactive closed-loop robot manipulation. To address this limitation, we propose \texttt{ReactVLA}, a lightweight and low-latency VLA framework for real-time robotic manipulation. \texttt{ReactVLA} combines two complementary designs: (1) an improved Mean Flow (iMF) action generator that reduces expensive multi-step diffusion sampling to one-to-few-step action generation, and (2) Attention Residuals (AttnRes), a dynamic depth-wise feature routing mechanism that replaces uniform residual accumulation to better preserve task-relevant multimodal representations. We evaluate \texttt{ReactVLA} on large-scale simulation benchmarks, including LIBERO and RoboIMI, as well as real-world robotic manipulation tasks. Experimental results show that \texttt{ReactVLA} consistently outperforms similarly sized VLA baselines, including SmolVLA and $π_0$. On challenging precision manipulation tasks, \texttt{ReactVLA} achieves up to a 1.65$\times$ improvement in task performance while providing more than a 4$\times$ increase in inference speed compared with leading VLA models. Finally, it reduces real-world policy latency to below 38.6 ms, enabling fast reactive control on physical robot platforms. Please check out our project website at: https://game-loader.github.io/ReactVLA/.

---

## 论文详细总结（自动生成）

# 论文结构总结

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：基于扩散的视觉-语言-动作（VLA）策略虽然能够建模多模态动作分布，但依赖迭代采样（通常需要数十步），导致推理延迟高达数百毫秒，无法满足机器人实时闭环操控（如接触动力学、物体运动）的需求。
- **研究动机**：如何在保留扩散式策略表达能力的条件下，将推理步数降低到一步或几步，实现快速反应式机械臂控制。
- **整体意义**：提出一种轻量级、低延迟的 VLA 框架 ReactVLA，在保持高成功率的同时，大幅提升推理速度（最高 4×），使实时反应式操控成为可能。

## 2. 论文提出的方法论

### 核心思想
- 用改进的平均流（improved Mean Flow, iMF）动作生成器替代标准扩散采样，支持一步到少步（1~5 步）动作生成。
- 引入注意力残差（Attention Residuals, AttnRes）机制，在 Transformer 深层动态路由历史特征，缓解多模态表示的稀释。

### 关键技术细节

**① 改进平均流（iMF）动作生成**  
- 标准流匹配（Flow Matching）建模瞬时速度场，需细粒度积分；平均流（Mean Flow）建模有限区间平均传输，允许大步长生成。
- iMF 进一步增加 Jacobian-向量积（JVP）校正项，稳定低步训练：
  \[
  V_{\theta}(z_t, r, t) = u_{\theta}(z_t, r, t) + (t - r) \cdot \text{sg}\!\left( \text{JVP}[u_{\theta}; v_{\theta}] \right)
  \]
  其中 \(u_{\theta}\) 预测平均速度，\(v_{\theta}\) 为瞬时速度（来自同一网络），\(\text{sg}\) 为停止梯度。
- 推理时直接从噪声 \(z_1\) 按网格（如 \(1.0 \to 0.5 \to 0.0\)）用欧拉积分更新 \(z_0\)，得到动作轨迹。

**② 注意力残差（AttnRes）策略 Transformer**  
- 输入序列包含时间嵌入、条件序列（视觉、语言、本体）以及动作令牌，总长度 \(L=2+2N_{\text{vis}}+N_{\text{text}}+1+H\)。
- 标准 PreNorm 残差均匀累积所有层输出，随深度增加导致特征稀释。AttnRes 用可学习层间注意力权重动态路由历史表示：
  \[
  h_l = \sum_{i=0}^{l-1} \alpha_{i \to l} v_i, \quad \alpha_{i \to l} = \text{softmax}(w_l^{\top} \text{RMSNorm}(v_i))
  \]
  使得每层能选择性关注最相关的历史特征。

**③ Pseudo-Huber 损失**  
- 代替 MSE 损失，以抑制早期训练中 JVP 带来的梯度爆炸。其梯度有界于 \([-\delta, \delta]\)，而 MSE 梯度无上界。
- 损失定义：\(\rho_{\delta}(e_d) = \delta^2\left(\sqrt{1+(e_d/\delta)^2} - 1\right)\)，小误差时近似 L2，大误差时近似 L1。

**训练流程（Algorithm 1）**  
1. 用 AttnRes Transformer 提取上下文特征 \(h_{\text{ctx}}\)。  
2. 采样时间步 \(r \le t\) 及噪声 \(\epsilon\)，构造 \(z_t\)。  
3. 计算瞬时速度 \(v_{\theta}\)、JVP、校正速度 \(V_{\theta}\)。  
4. 以 Pseudo-Huber 损失优化 \(V_{\theta}\) 与目标速度的误差。

## 3. 实验设计

### 数据集/场景与 benchmark
- **LIBERO**（模拟）：包含四个子套件（Spatial、Object、Goal、Long）共 40 个任务。联合训练，评估成功率。
- **RoboIMI**（模拟）：自定义双臂仿真环境，两个高精度任务：Peg-in-Socket（插销入孔）和 Object Transfer（物体交接）。累计奖励作为指标。
- **真实世界**：Diana 7 单臂机器人，两个任务：Orange Pick-and-Place（橘子抓放）和 Block Stack（叠木块）。成功率与延迟。

### 对比方法
- **LIBERO**：Diffusion Policy (0.46B)、OpenVLA (6.74B)、π0 (Paligemma-3B, 5.97B)、π0 (4.03B)、SmolVLA (0.24B/0.45B)、ReactVLA (0.39B)。
- **RoboIMI**：ACT、SmolVLA（训练失败，结果为“–”）、Diffusion Policy、ReactVLA。
- **真实世界**：SmolVLA (0.45B)。

## 4. 资源与算力

- 文中**未明确说明**使用的 GPU 型号、数量或训练时长。
- 仅提及“不同基准在不同 GPU 平台上进行”，并强调“每个基准内所有方法在相同硬件上评估”以保证公平性。
- 附录描述了模型细节（16 层 Transformer，隐层 768，8 头注意力），但未给出训练耗用算力。

## 5. 实验数量与充分性

### 实验组数与规模
- **LIBERO**：多任务联合训练（40 个任务），每种方法计算平均成功率。ReactVLA 使用 2 步推理。
- **RoboIMI**：两个任务各 100 次随机 rollout，报告平均奖励和延迟。
- **真实世界**：两个任务各 20 次试验，报告成功率和延迟。
- **消融实验**：
  - AttnRes 与 vanilla 残差对比（图 4）：训练 100k 步，显示成功率曲线。
  - Pseudo-Huber 与 MSE 对比（图 5）：训练 60k 步，展示损失和成功率曲线。

### 充分性与公平性
- 覆盖多种场景（模拟 + 真实，单臂 + 双臂，高精度任务）。
- 对比了同类轻量级模型（SmolVLA）以及更大模型（OpenVLA、π0）。
- 消融实验验证了各关键组件（AttnRes、Pseudo-Huber）的必要性。
- 所有方法在同一基准内使用相同硬件和评估协议。
- **略有不足**：SmolVLA 在 RoboIMI 上完全失败，未提供其调整尝试；真实世界任务仅用 50 个示范，泛化范围有限。

## 6. 主要结论与发现

- **ReactVLA 在模拟和真实环境均优于或持平于同大小及更大基线**：
  - LIBERO 平均成功率 88.0%（最高），推理延迟仅 18.3 ms（相比 SmolVLA 的 74.1 ms 快 4×）。
  - RoboIMI 上 Peg-in-Socket 奖励比 Diffusion Policy 高 48.5%，延迟从 398 ms 降至 15.1 ms。
  - 真实世界 Block Stack 任务成功率 90%（SmolVLA 为 75%），延迟 38.6 ms（SmolVLA 82.2 ms）。
- **AttnRes 显著提升训练效率**：无 AttnRes 时成功率仅 28.7%，引入后升至 88.0%。
- **Pseudo-Huber 损失稳定训练**：MSE 损失引起梯度爆炸，Pseudo-Huber 使训练平滑收敛。
- **少步生成可以达到与多步扩散相当的质量**，同时大幅降低推理时间。

## 7. 优点

- **轻量高效**：仅 0.39B 参数，推理速度高（15~38 ms），适合实时部署。
- **创新的动作生成**：iMF 结合 JVP 校正，首次将改进平均流用于机器人动作，实现稳定少步采样。
- **新颖的特征路由**：AttnRes 动态保留多模态信息，缓解深度 Transformer 的表示稀释。
- **稳定的训练**：Pseudo-Huber 损失有效处理 JVP 带来的二阶梯度问题，降低对超参数的敏感度。
- **全面的实验验证**：覆盖三大基准（LIBERO、RoboIMI、真实），多种对比方法和消融分析，结果一致性高。

## 8. 不足与局限

- **真实世界任务范围有限**：仅测试了桌面操作（抓放、堆叠），未涉及更复杂场景（如动态环境、长时任务或密集杂乱场景）。
- **示范数量少**：每个真实任务仅 50 个示范，泛化能力可能受限于数据规模。
- **不同基准 GPU 硬件不同**：论文指出绝对延迟不能跨基准直接比较，只相对速度有意义；读者需注意实验室内部可复现性。
- **SmolVLA 在 RoboIMI 上失败**：未分析原因，可能表明某些方法对任务复杂度敏感。
- **推理步数并非始终为 1 步**：真实世界使用 5 步以保障平滑性（而非理论上的 1~2 步），实际延迟仍有改善空间。
- **未讨论长程规划或语言理解复杂性**：仅关注短时动作生成，未见与高级语义规划的集成。

（完）
