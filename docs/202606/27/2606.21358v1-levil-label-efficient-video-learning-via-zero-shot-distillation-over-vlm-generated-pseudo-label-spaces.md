---
title: "LEViL: Label-Efficient Video Learning via Zero-Shot Distillation over VLM-Generated Pseudo-Label Spaces"
title_zh: LEViL：基于VLM生成伪标签空间的零样本蒸馏的标签高效视频学习
authors: Aslı Çelik
date: 2026-06-19
pdf: "https://arxiv.org/pdf/2606.21358v1"
tags: ["query:rep-learning"]
score: 9.0
evidence: 利用VLM生成伪标签的免标注视频预训练和零样本蒸馏
tldr: 有监督视频预训练依赖大规模标注数据且域迁移受限。本文提出LEViL框架，利用视觉语言模型生成无标注视频的语义伪标签空间，通过零样本蒸馏学习表征，并结合目标标签集感知微调。在UCF101和HMDB51上，有限标签下性能超越半监督方法，且无标注预训练提供有效初始化，降低标注成本。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.21358v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 877, \"height\": 456, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.21358v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1748, \"height\": 803, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.21358v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1745, \"height\": 879, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.21358v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1354, \"height\": 525, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.21358v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1193, \"height\": 889, \"label\": \"Table\"}]"
motivation: 现有有监督视频预训练需要大量标注数据且跨域泛化受限，亟需一种标注高效的预训练方案。
method: 利用VLM生成未标注视频的伪标签空间，通过零样本蒸馏训练学生编码器，并在下游微调中结合目标标签集蒸馏。
result: 在UCF101和HMDB51的有限标签设定下，LEViL优于所有对比的半监督方法，且全数据微调有效。
conclusion: 所提框架实现了无标注预训练与目标感知微调的结合，显著降低标注依赖，具有可扩展性。
---

## 摘要
有监督视频预训练是提升下游动作识别性能的常见迁移学习实践。然而，它需要大规模标注源数据集，且所学初始化的效果受源域与目标域相似度影响。为不同目标域构建此类标注预训练数据集成本高昂且难以扩展。为解决这些局限，本研究提出一种标签高效视频学习框架，将无标注视频预训练与目标标签集感知微调相结合。预训练阶段，视觉语言模型（VLM）为未标注视频生成文本描述，经处理后构建可解释的语义伪标签空间。随后，冻结的视频语言模型在该空间上产生零样本软目标分布，使学生视频编码器无需人工源标注即可学习语义丰富的表示。下游适应阶段，目标标签集感知微调将来自标注目标视频的有监督学习与真实目标标签集上的零样本蒸馏相结合，在使预训练编码器适应目标任务的同时，保留VLM衍生的语义引导。在UCF101和HMDB51上的实验表明，所提框架在所有评估的有限标签设置下均优于对比的半监督视频动作识别方法。此外，无标注预训练阶段学习到的可迁移表示为全数据微调提供了有效的初始化，尽管仅依赖相对较小的未标注预训练池。

## Abstract
Supervised video pretraining is a common transfer learning practice for improving downstream action recognition performance. However, it requires large-scale labeled source datasets, and the effectiveness of the learned initialization is influenced by the similarity between the source and target domains. Constructing such labeled pretraining datasets for different target domains is costly and difficult to scale. To address these limitations, this study proposes a label-efficient video learning framework that combines annotation-free video pretraining with target-label-set-aware fine-tuning. During pretraining, a vision-language model (VLM) generates textual descriptions of unlabeled videos, which are processed to construct an interpretable semantic pseudo-label space. A frozen video-language model then produces zero-shot soft target distributions over this space, allowing a student video encoder to learn semantically rich representations without manual source annotations. During downstream adaptation, target-label-set-aware fine-tuning combines supervised learning from labeled target videos with zero-shot distillation over the actual target label set, helping preserve VLM-derived semantic guidance while adapting the pretrained encoder to the target task. Experiments on UCF101 and HMDB51 show that the proposed framework outperforms the compared semi-supervised video action recognition methods across all evaluated limited-label regimes. Moreover, the annotation-free pretraining stage learns transferable representations that provide an effective initialization for full-data fine-tuning, despite relying on a comparatively modest unlabeled pretraining pool.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **研究动机**：有监督视频预训练（如基于Kinetics数据集）需要大规模人工标注源视频数据，成本高昂且难以扩展到不同目标域。此外，源域与目标域的相似度直接影响预训练表征的迁移效果。同时，传统有监督预训练依赖固定类别标签（one-hot），提供的语义信息粗糙，无法捕捉视频中的物体、场景、交互等丰富上下文。
- **整体含义**：本文旨在提出一种**标签高效**的视频学习框架，通过**无标注视频预训练**与**目标标签集感知微调**相结合，降低对人工标注的依赖，同时提升下游动作识别性能，尤其是在有限标签场景下。

## 2. 论文提出的方法论：核心思想、关键技术细节

### 核心思想
- 利用**视觉语言模型（VLM）** 为无标注视频生成文本描述，从中自动构建可解释的**语义伪标签空间**，然后通过**零样本软蒸馏**训练学生视频编码器，最后在下游微调中结合**目标标签集上的零样本蒸馏**与有监督学习。

### 关键技术细节
1. **伪标签空间构建**：
   - 使用InternVL2-8B为每个无标注视频生成简短描述（提示词固定）。
   - 利用spaCy NLP管道提取动词（动作）、名词（动作相关物体）及动-宾短语作为候选伪标签。
   - 经过文档频率过滤（min_df=5, max_df=0.25）和停用词去除后，得到包含2391个文本伪标签的词汇表。

2. **零样本蒸馏预训练**：
   - 冻结的InternVideo2-CLIP-S模型将每个无标签视频编码为视觉特征，每个伪标签编码为文本特征，计算余弦相似度，经温度缩放softmax得到软目标分布 \( q^{\text{VLM}}_{i,k} \)。
   - 学生编码器（3D ResNet-18 + 投影头）输出分布 \( p^S_i \)，通过软交叉熵损失优化。

3. **目标标签集感知微调**：
   - 替换投影头为任务特定分类头，使用带标签的目标视频进行有监督交叉熵损失（\( L_{\text{CE}} \)）。
   - 同时，利用冻结教师模型对未标记目标视频计算目标标签集上的零样本软分布，使用蒸馏损失 \( L_{\text{TLSA-ZSD}} \)。
   - 最终损失：\( L_{\text{FT}} = \lambda_{\text{ft}} L_{\text{CE}} + \lambda_{\text{distill}} L_{\text{TLSA-ZSD}} \)，实验中 \( \lambda_{\text{ft}}=\lambda_{\text{distill}}=0.5 \)。
   - 在测试时仅使用学生模型，无额外推理成本。

## 3. 实验设计

### 数据集
- **预训练无标注池**：UCF101训练视频 + HMDB51训练视频 + Kinetics-400和Kinetics-600的5%子集（无标签使用）。
- **下游评估**：UCF101（101类）和HMDB51（51类），使用官方划分S1。

### Benchmark 与对比方法
- **半监督设定（有限标签）**：对比方法包括VideoSSL、DANet、CMPL、LTG、MvPL、L2A、ActorCutMix、FD-VLM、TimeBalance等，均使用CNN骨干（R3D-18或R(2+1)D-34）。标签比例分别为1%、10%、40%、50%（UCF101）和1%、10%、40%（HMDB51）。
- **全数据微调设定**：对比各种预训练策略（无预训练、ImageNet通胀、Supervised预训练、自监督方法如3DRotNet、ClipOrder、VideoMoCo等）和不同骨干。
- **消融**：文中未明确设计独立的消融实验，但通过对比不同方法间接分析各组件的贡献。

### 实验结果
- 半监督设定下：LEViL在所有标签比例下均优于所有对比方法，尤其在极端低标签（1%）时优势显著（UCF101: 54.3% vs 第二名29.1%）。
- 全数据微调：仅使用无标注预训练阶段（不含目标标签蒸馏）即可提供有效初始化，性能与有监督Kinetics预训练相近（UCF 72.5% vs Kinetics super. 81.7%；HMDB 56.2% vs 61.2%）。

## 4. 资源与算力

- **文中明确说明**：所有实验在**单张NVIDIA GeForce RTX 5090 GPU**上进行。
- **训练时长**：预训练100个epoch，微调20个epoch。具体总耗时未报告。
- **其他**：学生模型从零初始化，使用AdamW优化器（lr=1e-4），批量大小为80（预训练）或40/40（微调）。

## 5. 实验数量与充分性

- **实验数量**：主要两组实验（半监督Table 1、全数据Table 2），涵盖两个数据集、多个标签比例，对比方法较多（>10种）。
- **充分性评估**：
  - **正面**：对比方法涵盖主流半监督和自监督策略，且控制骨干一致（R3D-18），公平性较好。预训练池构成明确（含Kinetics 5%），与现有工作相比资源更节俭。
  - **不足**：缺少对伪标签空间构建策略的消融实验（如不同NLP过滤阈值、不同VLM生成模型的影响）。未报告多次跑实验的方差或统计显著性。仅在UCF101和HMDB51两个相对小规模数据集上评估，未在更大/更细粒度数据集（如Something-Something、ActivityNet）上验证。
- **整体评价**：实验设计较为规范，对比全面，但消融深度不足。

## 6. 论文的主要结论与发现

- **主要结论**：提出的LEViL框架通过无标注VLM驱动伪标签蒸馏和下游目标标签感知微调，显著优于现有半监督视频动作识别方法，尤其在标注极度稀缺时效果突出。
- **关键发现**：
  - 即使使用较小的无标注预训练池（仅5% Kinetics），也能学到可迁移的视频表征。
  - 零样本蒸馏保留了VLM的语义结构，避免了纯有监督微调的过拟合风险。
  - 框架模块化，未来可替换更强的VLM组件进一步提升性能。

## 7. 优点

- **创新性**：提出将VLM生成文本转化为可解释伪标签空间，而非直接使用特征蒸馏，使监督信号语义更丰富且可解释。
- **标签高效**：无需任何源域标注，预训练完全基于未标注视频；下游微调也结合了零样本蒸馏，降低对目标域标注的依赖。
- **计算效率**：仅训练阶段使用VLM，测试阶段无需额外模型；预训练池规模远小于全量Kinetics，但仍能获得有竞争力的结果。
- **公平对比**：在半监督实验中确保骨干网络一致（R3D-18），并综合对比了多种主流方法。

## 8. 不足与局限

- **实验覆盖有限**：仅在两个中等规模数据集（UCF101、HMDB51）上评估，缺乏在更复杂、长尾或细粒度动作数据集上的验证。
- **消融不充分**：未对伪标签构建中的关键参数（文档频率阈值、停用词列表、温度τ等）进行系统的消融实验；也未单独分析VLM生成文本质量的影响。
- **可能偏差风险**：预训练池中包含部分目标数据集（UCF、HMDB）的训练视频，虽然无标签，但若模型隐式学习到类别分布，可能带来一定“数据泄露”风险。论文说明未使用标签，但尚需更严格的控制实验。
- **应用限制**：依赖VLM进行描述生成（InternVL2-8B），若VLM对特定场景描述不准确，伪标签质量可能下降。另外，伪标签空间构建依赖NLP流水线，对非英语语料支持有限。
- **资源需求**：虽然只使用单块RTX 5090，但VLM生成caption仍需一定计算成本；整体训练时长未报告，可能较长。

（完）
