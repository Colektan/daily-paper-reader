---
title: Improving Vision-Language-Action Model Fine-Tuning with Structured Stage and Keyframe Supervision
title_zh: 通过结构化阶段和关键帧监督改进视觉-语言-动作模型微调
authors: "Yuan Xu, Yixiang Chen, Kai Wang, Jiabing Yang, Peiyan Li, Qisen Ma, Yan Huang, Liang Wang"
date: 2026-06-25
pdf: "https://arxiv.org/pdf/2606.26801v1"
tags: ["query:rl-vla-world"]
score: 9.0
evidence: 针对VLA微调的结构化阶段与关键帧监督
tldr: "VLA模型微调时对所有时间步均等监督，忽略操纵阶段结构，导致失败集中在夹爪事件转换。StaKe框架自动从示范夹爪状态提取阶段分类和关键帧预测辅助监督，不改变基础策略。在双臂仿真和单臂Franka真实任务中，成功率分别提升14%和56%，长程任务改进更大。结构化监督是增强VLA微调的有效通用策略。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.26801v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 830, \"height\": 347, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.26801v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 873, \"height\": 389, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.26801v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1293, \"height\": 616, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.26801v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1734, \"height\": 886, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.26801v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1752, \"height\": 792, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.26801v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1113, \"height\": 528, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.26801v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 895, \"height\": 345, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.26801v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 590, \"height\": 259, \"label\": \"Table\"}]"
motivation: 现有VLA微调对所有时间步均等监督，忽略操纵阶段结构，导致夹爪事件转换处失败率高。
method: 提出StaKe框架，添加阶段分类器和关键帧预测器两个轻量辅助头，利用夹爪状态自动生成监督信号。
result: "在双臂仿真和单臂Franka真实任务上，成功率分别相对提升14%和56%，长程任务提升更显著。"
conclusion: 结构化阶段和关键帧监督可有效提升VLA模型在长程操作中的泛化能力。
---

## 摘要
视觉-语言-动作（VLA）模型在泛化机器人操作中展现出强大潜力。然而，在微调过程中，动作监督在所有时间步上均等应用，没有对机器人处于哪个操作阶段或下一个夹爪事件目标应是什么进行结构化监督。这导致失败集中在具有挑战性的夹爪事件转换周围。为解决此问题，我们提出StaKe，一种插件式辅助监督框架，它从演示夹爪状态中自动推导两个互补信号，无需人工标注：一个阶段分类器，用于识别当前操作阶段；以及一个关键帧预测器，用于估计下一个夹爪转换时的目标关节动作。两者均建模为轻量级辅助头，在训练期间丰富学习到的表示，同时保持基础VLA策略架构和推理循环不变。在双臂仿真和单臂Franka真实机器人任务上的实验表明，StaKe持续提高了成功率（相对提升分别为14%和56%），在涉及更多夹爪事件转换的长时域任务上改进更大。消融研究验证了每个设计选择，定性分析证实学习到的表示忠实地跟踪了操作阶段。这些结果表明，结构化监督是在长时域操作中增强VLA微调的一种有效且通用的策略。项目网站：https://hi-yuanxu.github.io/StaKe-Web/

## Abstract
Vision-Language-Action (VLA) models have shown strong potential for generalizable robotic manipulation. During fine-tuning, however, action supervision applies equally across all timesteps, without structured supervision on which manipulation stage the robot is in or what the next gripper-event target should be. This causes failures to concentrate around challenging gripper-event transitions. To address this, we propose StaKe, a plug-in auxiliary supervision framework that automatically derives two complementary signals from demonstration gripper states without manual annotation: a stage classifier that identifies the current manipulation stage, and a keyframe predictor that estimates the target joint action at the next gripper transition. Both are modeled as lightweight auxiliary heads that enrich the learned representations during training, while leaving the base VLA policy architecture and inference loop unchanged. Experiments on bimanual simulation and single-arm Franka real-robot tasks show that StaKe consistently improves success rates (relative gains of 14% and 56%, respectively), with larger improvements on longer-horizon tasks that involve more gripper-event transitions. Ablation studies validate each design choice, and qualitative analysis confirms that the learned representations faithfully track manipulation stages. These results indicate that structured supervision is an effective and general strategy for enhancing VLA fine-tuning in long-horizon manipulation. Project website: https://hi-yuanxu.github.io/StaKe-Web/

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 论文的核心问题与整体含义（研究动机和背景）
- **核心问题**：现有的视觉-语言-动作（VLA）模型在微调时，对所有时间步的损失（如流匹配损失）进行均等监督，忽略了操作轨迹固有的结构——即轨迹自然分为自由空间运动（Motion）和接触约束技能执行（Skill）两个阶段，夹爪开合事件是阶段边界。这种均一监督导致策略难以捕捉阶段转换信息，失败集中发生在夹爪事件切换附近（如图1所示：运动失败或技能执行失败）。
- **研究动机**：通过引入结构化阶段和关键帧监督，在不改变基础VLA架构和推理流程的前提下，提升模型对操作阶段和关键转换事件的感知能力，从而改善长时域操作的泛化和成功率。
- **整体含义**：证明了结构化辅助监督是增强VLA微调的一种有效且通用的策略，尤其适用于涉及多次夹爪转换的复杂长时域任务。

## 2. 论文提出的方法论：核心思想、关键技术细节、公式或算法流程
- **核心思想**：提出**StaKe（Stage and Keyframe supervision）**框架，在训练时为VLA模型添加两个轻量级辅助头：阶段分类头（Stage Classification Head）和关键帧预测头（Keyframe Prediction Head），从示范数据夹爪状态自动提取标签，训练完成后推理时辅助头不参与，不改变原有动作生成流程。
- **关键技术细节**：
  - **阶段标签提取（Algorithm 1）**：根据夹爪开合状态，将夹爪关闭的帧（及其前后K帧扩展）标记为“技能”（Skill, st=1），其余为“运动”（Motion, st=0）。K为可调超参数，实验中设为25（约1秒）。
  - **关键帧目标提取（Algorithm 2）**：扫描轨迹中所有夹爪状态变化帧（开→关或关→开），记录该时刻的关节配置；对每个时间步t，赋值最近的下一个转换帧的关节状态qtnext作为关键帧目标；最后添加终帧作为最后一个关键帧。
  - **模型架构**：基于π0.5 VLA模型，在VLM骨干的输入序列中额外插入两个可学习的查询令牌（ess和ekf），骨干处理后得到对应表示z̃ss和z̃kf，分别输入两个轻量MLP头。阶段头输出标量概率（二元交叉熵损失），关键帧头输出关节向量（L1损失）。
  - **训练总损失**：L = Lpolicy + λs Lstage + λk Lkf，其中λs=λk=0.01（固定）。
  - **推理**：辅助头不调用，动作生成与原π0.5完全一致（迭代反除噪声）。
- **算法流程（文字说明）**：
  1. 从示范数据中提取夹爪状态序列；
  2. 根据夹爪开合区间（扩展K帧）生成每个时间步的阶段标签（0/1）；
  3. 检测所有夹爪状态变化帧，记录关节配置，为每个时间步分配下一个最近的转换关键帧作为目标；
  4. 在训练时，VLM骨干输入观察/指令和两个查询令牌，输出阶段预测和关键帧预测，与真实标签计算损失并反向传播；
  5. 推理时仅使用动作专家。

## 3. 实验设计：使用了哪些数据集/场景、benchmark、对比方法
- **仿真实验**：
  - **场景**：RoboTwin 2.0双臂操作基准（基于SAPIEN模拟器），共10个任务，按关键帧数分组：6个2KF任务（如Adjust Bottle、Click Alarmclock等）、1个3KF任务（Put Object Cabinet）、2个4KF任务（Handover Block、Place Burger Fries）、1个5KF任务（Stack Blocks Two）。
  - **数据**：每个任务提供50条示范，使用demo clean设置。
  - **评估**：每任务100个episode，报告成功率（由内置检查器判定）。
  - **对比方法**：Diffusion Policy (DP)、ACT、RDT-1B（结果取自官方基准）、π0.5（基础VLA模型，相同训练配方）。
- **真实机器人实验**：
  - **场景**：Franka Research 3单臂并行夹爪，前视ZED 2i摄像头。4个桌面任务（按关键帧复杂度递增）：Mug on Plate (2KF)、Stack Bowls (2KF)、Bowl in Drawer (3KF)、Two Mugs in Basket (4KF)。
  - **数据**：每任务50条遥操作示范，多任务联合训练。
  - **评估**：每任务10次尝试，报告逐关键帧累积成功率。
  - **对比**：π0.5（相同训练设置）。

## 4. 资源与算力（文中明确说明）
- **训练硬件**：4块NVIDIA H20 GPU。
- **训练设置**：全模型后训练（更新VLM骨干和动作专家），20000步，batch size 64，学习率5e-5，动作块长度H=50。
- **未说明**：未提及训练总时间（如小时数）、推理速度对比等。

## 5. 实验数量与充分性
- **实验数量**：
  - 仿真：10个任务，每组100 episodes，共1000次评估。
  - 真实：4个任务，每组10 trials，共40次评估。
  - 消融实验：4个代表性任务（Stack Blocks、Burger Fries、Lift Pot、Click Alarmclock）上测试不同变体（去除阶段监督、去除关键帧监督、替代关键帧目标、替代查询令牌方式），以及超参数K（0,15,20,25,30）敏感性。
- **充分性与公平性**：
  - 对比基线包括经典方法和当前SOTA（π0.5），且StaKe与π0.5共享完全相同的骨干和训练配方，仅增加辅助损失，对比公平。
  - 仿真任务覆盖不同关键帧数（2~5），真实任务从简单到复杂，体现泛化性。
  - 消融实验验证了每个组件的必要性及设计选择的合理性，K值敏感性实验表明在合理范围内稳定。
  - 不足：仿真中100 episodes可能因随机性存在一定方差，但未报告置信区间；真实任务10 trials数量较少，统计显著性有限；未在所有任务上做完整消融（仅选4个）。

## 6. 论文的主要结论与发现
- **核心结论**：StaKe成功提升了VLA微调的成功率，在双臂仿真任务上平均成功率从π0.5的52.4%提升至59.8%（相对提升14%），在单臂真实任务上从40.0%提升至62.5%（相对提升56%）。
- **具体发现**：
  - 改进幅度与任务关键帧数量正相关：2KF任务提升较小，4~5KF任务提升显著（如Place Burger Fries +12pp, Handover Block +8pp, Stack Blocks Two +4pp）。
  - 两辅助损失互补：单独去除SS或KS都会导致性能下降，二者协同效果最佳。
  - 事件驱动的关键帧目标（下一夹爪转换）优于固定未来时间步目标（t+H）。
  - 专用可学习查询令牌优于使用VLM末尾令牌。
  - 阶段标签扩展K=25表现最佳且对范围不敏感（20~30）。
  - 学习到的阶段预测能准确跟踪实际运动/技能切换；关键帧预测误差在归一化空间保持在10^-5 ~10^-4级别。
  - 真实任务中，改进主要集中在后续关键帧（如放置、关闭抽屉），初始抓取阶段差异不大。

## 7. 优点：方法或实验设计上的亮点
- **方法优点**：
  - **轻量无侵入**：仅添加两个可学习令牌和MLP头，不改变基础模型架构和推理流程，计算开销极小。
  - **全自动标签提取**：仅依赖夹爪状态（二进制开/关），无需人工标注，可推广到任何含夹爪的操作示范。
  - **通用性**：在双臂仿真和单臂真实任务上均有效，适用于多种操作类型（抓取、放置、转移、堆叠等）。
  - **清晰的分析**：通过逐关键帧累积成功率和定性可视化（图4）揭示了改进来源，证明辅助监督让策略更关注阶段转换。
- **实验设计亮点**：
  - 在仿真基准上使用标准对比（DP, ACT, RDT, π0.5），且StaKe基于π0.5，直接体现辅助损失增益。
  - 消融实验覆盖多个设计维度（损失组件、目标选择、令牌类型、超参数），结论可靠。
  - 真实实验设计合理，任务难度递增，逐关键帧统计提供了细粒度的性能洞察。

## 8. 不足与局限：包括实验覆盖、偏差风险、应用限制
- **应用限制**：
  - 方法基于二进制夹爪开合事件定义阶段和关键帧，因此**仅适用于支持抓取的操作（prehensile manipulation）**，无法直接扩展到非抓取操作（如推动、转动旋钮）或灵巧操作（如多指手内操作）。这限制了方法的适用范围。
  - 阶段标签扩展K为固定值（25帧），虽经实验验证稳定，但可能在某些快速或慢速操作场景下不最优。
- **实验覆盖局限**：
  - 真实任务数量有限（4个，各10次），统计显著性不足，且未与其他方法（如DP、ACT）在真实场景直接对比。
  - 仿真任务虽多样但均为面向夹紧的pick-and-place类，未包含需要连续滑动或持续接触的任务（如研磨、擦拭）。
  - 未评估辅助头对训练速度或内存的影响（虽声称“negligible”但无数据）。
  - 未探索在推理时利用辅助表示的方法（论文明确提出作为未来工作）。
- **偏差风险**：
  - 仅使用50条示范，对于复杂任务数据量偏少，可能限制更复杂场景的泛化。
  - 所有实验在同一实验室环境（SAPIEN模拟器和Franka桌面），未在多臂或不同硬件上验证。

（完）
