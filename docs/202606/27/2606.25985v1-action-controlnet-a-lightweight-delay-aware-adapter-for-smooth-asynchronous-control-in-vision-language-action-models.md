---
title: "Action ControlNet: A Lightweight Delay-Aware Adapter for Smooth Asynchronous Control in Vision-Language-Action Models"
title_zh: 动作控制网络：面向视觉-语言-动作模型中平滑异步控制的轻量级延迟感知适配器
authors: "Tiecheng Guo, Meng Guo"
date: 2026-06-24
pdf: "https://arxiv.org/pdf/2606.25985v1"
tags: ["query:rl-vla-world"]
score: 9.0
evidence: 视觉-语言-动作模型中面向时延的无缝控制适配器
tldr: VLA模型在机器人操控中面临推理延迟导致控制不稳的问题。异步执行可缓解延迟，但旧观测量预测动作会产生抖动和失败。本文提出轻量级适配器ACNet，利用已执行的轨迹后缀作为残差条件，固定预训练骨干，仅微调动作头。在Kinetix、Meta-World MT50和真实SO-ARM101平台上，ACNet提升了延迟下的鲁棒性，生成了更平滑的异步轨迹，且比全模型重训练更轻量。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.25985v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 886, \"height\": 873, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.25985v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 899, \"height\": 469, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.25985v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 904, \"height\": 469, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.25985v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1815, \"height\": 491, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.25985v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 887, \"height\": 296, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.25985v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 836, \"height\": 490, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.25985v1/fig-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 892, \"height\": 338, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.25985v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 901, \"height\": 286, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.25985v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 895, \"height\": 271, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.25985v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 905, \"height\": 180, \"label\": \"Table\"}]"
motivation: VLA模型异步执行中，延时旧观测导致动作衔接不连续，引起抖动和接触任务失败，现有方案需全策略重训或特定运行时逻辑。
method: ACNet：以已执行动作后缀为残差条件，冻结预训练骨干，仅微调动作头（兼容扩散/流匹配），实现延迟感知的轻量适配。
result: 在Kinetix、Meta-World MT50和真实SO-ARM101平台上，ACNet比直接拼接更鲁棒、轨迹更平滑，且参数量远少于全延迟条件重训练。
conclusion: ACNet提供一种轻量、即插即用的异步控制增强方式，无需重训练即提升VLA模型在高频控制中的稳定性。
---

## 摘要
视觉-语言-动作（VLA）模型在通用机器人操作方面展现出强大潜力，但其推理延迟仍是实现稳定高频控制的主要障碍。异步执行通过将策略推理与动作执行重叠来缓解这一瓶颈，但在机器人持续运动期间，下一个动作片段仍需利用过时的观测进行预测。因此，直接拼接片段会导致交接不连续、动作抖动以及接触密集型操作失败。现有解决方案通常需要完全重新训练策略或采用特定于架构的运行时逻辑。本文提出动作控制网络（ACNet），一种轻量级延迟感知适配器，它利用已执行的动作后缀作为大部分冻结的动作头的残差条件。ACNet保持预训练骨干网络不变，引入少量可训练参数，并与扩散模型和流匹配等生成式动作头兼容。在Kinetix、Meta-World MT50以及真实世界的SO-ARM101平台上，ACNet在推理延迟下增强了鲁棒性，比直接拼接片段产生更平滑的异步轨迹，同时比完整延迟条件重训练更轻量。

## Abstract
Vision-language-action (VLA) models have shown strong potential for general-purpose robot manipulation, but their inference latency remains a major obstacle to stable high-frequency control. Asynchronous execution mitigates this bottleneck by overlapping policy inference with action execution, yet the next action chunk is still predicted from stale observations while the robot continues to move. Direct chunk stitching therefore introduces handoff discontinuities, action jitter, and failures in contact-rich manipulation. Existing remedies typically require either full-policy retraining or architecture-specific runtime logic. This work proposes Action ControlNet (ACNet), a lightweight delay-aware adapter that uses the executed motion suffix as a residual condition for a mostly frozen action head. ACNet leaves the pretrained backbone unchanged, introduces few trainable parameters, and remains compatible with generative action heads such as diffusion and flow matching. On Kinetix, Meta-World MT50, and a real-world SO-ARM101 platform, ACNet improves robustness under inference delay and yields smoother asynchronous trajectories than direct chunk stitching, while remaining more lightweight than full delay-conditioned retraining.

---

## 论文详细总结（自动生成）

### 论文核心问题与整体含义（研究动机和背景）

- **核心问题**：视觉-语言-动作（VLA）模型在通用机器人操作中表现出色，但其**推理延迟**（尤其是大模型骨干与迭代式动作头）导致高频控制不稳定。标准的同步执行模式（等待推理完成再执行）引入了明显的停顿间隙，降低了任务完成速度。异步执行（推理与执行重叠）虽能提升有效控制频率，但**下一个动作片段仍基于过时观测生成**，直接拼接连续片段会在手部交接处产生**不连续性、动作抖动**，在接触密集或精度敏感的操作中易导致任务失败。
- **现有方案局限**：完全重训练策略（如延迟条件训练）虽能直接解决但成本高昂；运行时修复方法（如修补、插值、辅助校正头）通常依赖于特定架构或启发式规则，缺乏通用性。
- **研究动机**：作者将异步延迟带来的主要错误视为**局部手部交接问题**而非全局任务理解失败——过时观测通常仍能指定任务语义和粗略规划，**不可靠的部分是从已执行动作延续到新动作的边界**。因此，本文旨在设计一种**轻量级、即插即用的适配器**，在不改变预训练骨干的前提下，利用已执行的动作后缀作为边界条件，改善异步执行下的交接平滑度。

### 论文提出的方法论：核心思想、关键技术细节

- **核心思想**：将异步延迟下的VLA控制问题形式化为**片段交接处的边界条件问题**，定义“延迟动作”（delay action）——即推理延迟期间机器人实际执行的那段动作后缀——作为关键条件信号。ACNet将其编码为**残差调节信号**，注入到大部分冻结的动作头中，实现对原始预测的局部修正，而不改变预训练骨干网络。
- **关键技术细节**：
    1. **问题形式化**：定义片段分解：\( H = d + e + r \)（\( d \)为推理延迟步数，\( e \)为重启动间隔，\( r \)为可选未来后缀）。延迟动作 \( a_{t}^{\text{delay}} \) 是前一片段中从第\( e \)步到第\( e+d-1 \)步的子序列。目标：通过参数高效适配 \(\eta\) 最小化延迟控制风险，包含预测损失和边界一致性损失 \( \mathcal{L}_{\text{bd}} = \| \hat{a}_t^{(d)} - a_{t-e}^{(e+d-1)} \|^2 \)。
    2. **ACNet架构**：
        - **冻结骨干**：感知-语言骨干（如Evo-1）保持冻结，输出视觉-语言潜在向量 \( B_\omega(o_t, l) \)。
        - **延迟动作编码**：将延迟动作填充至长度\( H \)（未观测的未来位置使用可学习的填充令牌），通过轻量级Transformer编码器 \( E_\phi \) 处理，再利用与动作专家相同的时序池化操作，得到紧凑的上下文向量 \( c_t \)。
        - **残差注入**：在动作专家的某一层（论文选择最后一层，通过消融实验验证最佳）添加残差连接：\( h'_l = h_l + Z_{\phi,l}(c_t) \)，其中 \( Z_{\phi,l} \) 是将上下文投影到该层隐藏维度的线性层。这种残差设计使原始预测成为默认模式，且对小扰动近似线性（式12），从而支持局部边界校正而非全局重映射。
        - **延迟覆盖训练**：由于骨干共享，一次前向传播可缓存潜在向量，然后对不同采样延迟并行计算对应的条件向量 \( c_{t,d} \)，高效覆盖多种延迟。
    3. **训练目标**：对于流匹配动作专家，采用标准流匹配损失（式16），但速度预测器变为延迟-条件形式 \( v_{\psi,\eta}^{\text{ACNet}}(x_\tau, \tau, c_{t,d}) \)。训练时延迟从分布 \( p(d) \) 中采样，流时间 \( \tau \sim \text{Beta}(2,2) \)。

### 实验设计：数据集/场景、基准、对比方法

- **数据集与场景**：
    - **Kinetix**（基于Kinetix模拟器）：遵循RTC[4]的评估协议，包含多种物物理交互任务（如装配、滑动等），评价延迟 \( d \in \{0,1,2,3,4\} \)。
    - **Meta-World MT50**：全部50个任务，预测步数 \( H=50 \)，执行间隔 \( e=25 \)，延迟 \( d \in \{0,5,10,15\} \)。
    - **真实世界平台SO-ARM101**：桌面操作任务（"将蓝色立方体放入盒子"和"清洁桌子"），各10次试验，共20次。
- **基准方法**：
    - **Naïve Async**：直接拼接连续片段（最简单异步方案）。
    - **RTC**[4]：运行时修补（冻结已提交动作，内插剩余片段）。
    - **Training-RTC**[17]：延迟条件训练（完整重训练策略）。
    - **ACNet**：本文方法。
- **对比公平性**：Kinetix中，RTC和Training-RTC基于π0模型，ACNet基于Evo-1骨干。Metaworld中相同。作者指出骨干不同可能引入不公平，但主要对比指标是成功率、可训练参数量、延迟和频率，且ACNet参数量远少于Training-RTC，效果接近，显示出轻量优势。

### 资源与算力

- **文中说明**：Meta-World实验使用**单张RTX 4080 SUPER**。未明确说明训练时长、总GPU数或训练epoch数。Kinetix训练中，ACNet阶段1训练1个epoch，阶段2训练24个epoch，然后冻结骨干训练ACNet和池化层8个epoch。具体训练时间未给出。
- **结论**：算力消耗相对较低，但具体数值不详。

### 实验数量与充分性

- **实验组数**：
    1. **Kinetix**：四种方法 × 5种延迟 → 20组成功率对比；另报告可训练参数量比例。
    2. **Meta-World MT50**：四种方法 × 4种延迟 → 16组成功率对比；另报告延迟和频率；对代表性任务（nut-assembly-v3、plate-slide-back-v3）绘制了50步内的动作抖动曲线（图4）。
    3. **真实世界实验**：两个任务各10次试验 → 共20次试验，报告成功率与定性快照（图5）。
    4. **消融实验**（Meta-World MT50上）：
        - 残差注入位置消融（层0、层1、层3、层7、层7+混合）→ 5组，验证最后一层最优。
        - 未来填充方式消融（零填充、噪声填充、可学习填充）→ 3组，验证可学习填充最佳。
        - 各层更新幅度统计（图6）：流匹配1000步上的平均令牌更新量。
- **充分性与客观性**：实验覆盖了模拟和真实环境、多种延迟、多个基准方法，并进行了导致性能差异的消融分析。数据集多样，但骨干不统一可能引入一定偏差。作者在文中也指出这一点，但通过参数量比较突出了ACNet的轻量优势。整体实验设计较为充分，结论可信。

### 论文的主要结论与发现

- **ACNet在异步延迟下显著提高成功率**：在Kinetix上，ACNet平均成功率0.79，与完整重训练（Training-RTC，0.80）相当，远高于Naïve Async（0.61）和RTC（0.72）；仅训练约20%参数，Training-RTC更新100%。
- **Meta-World MT50**：ACNet平均成功率0.74，同样匹配Training-RTC，高于其他对比；端到端延迟91ms（11.0 Hz），优于RTC（159ms）和Training-RTC（134ms）。
- **真实世界任务**：ACNet实现20/20成功（100%），Naïve Async为17/20（85%），且动作更平滑。
- **轨迹平滑性**：动作抖动（jerk）分析显示ACNet在交接边界处抖动更小，曲线更平坦。
- **消融验证**：最后一层注入残差效果最佳；可学习填充令牌优于零/噪声填充。

### 优点

1. **轻量高效**：冻结预训练骨干，仅引入少量可训练参数（约20%），训练成本低。
2. **即插即用**：兼容多种生成式动作头（扩散、流匹配），无需修改架构或运行时逻辑。
3. **延迟覆盖训练**：通过缓存骨干潜在向量、并行采样延迟，高效覆盖多种延迟分布。
4. **局部修正思想**：将延迟问题视为边界条件，避免全局重训，保留原模型语义能力。
5. **实时性能优**：端到端延迟低（91ms），控制频率达11.0 Hz，适合高频闭环控制。
6. **真实验证**：在真实机器人平台验证了有效性，展示了明显改善的平滑性和成功率。

### 不足与局限

1. **骨干不统一**：对比方法（RTC、Training-RTC）使用π0骨干，ACNet使用Evo-1骨干。虽然作者说明主要对比参数量与延迟，但骨干差异可能影响绝对性能比较的公平性。未在相同骨干上对比ACNet与完整重训练的效果（例如在Evo-1上训练延迟条件的动作头），削弱了消融的严密性。
2. **延迟范围有限**：实验仅覆盖特定延迟集合（Kinetix: 0-4; Meta-World: 0,5,10,15），未探索更大或时变延迟下的鲁棒性。
3. **仅考虑固定执行间隔**：文中假设执行间隔 \( e \) 固定，未考虑动态变化。
4. **未开源或详述训练成本**：缺少训练时间、内存消耗等具体指标，不利于复现。
5. **真实实验规模小**：仅两个任务各10次试验，统计显著性受限；未涉及更多复杂接触任务或长期任务。
6. **理论分析有限**：虽然给出了局部泰勒展开（式12）的直观解释，但未提供严格的收敛性或泛化界分析。
7. **未讨论延迟分布偏移**：训练时采样延迟分布可能与部署时存在差异，能否通过少量数据快速适应未知。

（完）
