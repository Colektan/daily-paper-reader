---
title: Neuro-Symbolic Injection of LTLf Constraints in Autoregressive Reinforcement Learning Policies
title_zh: 在自回归强化学习策略中注入LTLf约束的神经符号方法
authors: "Ashkan Ansarifard, Matteo Mancanelli, Elena Umili, Fabio Patrizi"
date: 2026-06-06
pdf: "https://arxiv.org/pdf/2606.08312v1"
tags: ["query:rl-vla-world"]
score: 9.0
evidence: 神经符号框架将LTLf约束注入基于Transformer的离线强化学习策略。
tldr: 离线强化学习常采用Transformer建模序列，但忽略LTLf表达的时序任务约束。本文提出神经符号框架，将LTLf公式编译为确定有限自动机，通过可微逻辑损失注入自回归策略。实验表明该方法在导航环境下提升约束满足率，同时保持与原基线相当的回报。该架构无关的方法为引入时序知识提供灵活方案。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.08312v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 829, \"height\": 1000, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.08312v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1632, \"height\": 332, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.08312v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1650, \"height\": 483, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.08312v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1497, \"height\": 926, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.08312v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1496, \"height\": 924, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.08312v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1491, \"height\": 922, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.08312v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1323, \"height\": 306, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.08312v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1368, \"height\": 307, \"label\": \"Table\"}]"
motivation: 现有Transformer策略单纯优化奖励，无法满足高层时序约束如安全性和可达性。
method: 将LTLf编译为DFA，导出可微满足信号作为正则项，与奖励损失联合训练。
result: 在多个导航环境中，约束满足率显著提升，且累积奖励与基线持平。
conclusion: 神经符号注入有效融合时序知识，是架构无关的通用增强方法。
---

## 摘要
在这项工作中，我们研究了在有限迹线性时态逻辑（LTLf）表达的时间扩展任务约束下的离线强化学习（RL）。最近，基于Transformer的方法（如Trajectory Transformers和Decision Transformers）被采用以将RL视为序列建模问题。然而，这些方法纯粹优化奖励，并未考虑高层时序需求。在此，我们引入了一个神经符号框架，将LTLf背景知识注入到这类基于Transformer的RL策略中。我们的方法将LTLf公式编译为确定性有限自动机（DFA），并通过可微表示和基于逻辑的损失函数将其整合到学习过程中。具体而言，我们从DFA进展中推导出可微的满足信号，并将其作为训练过程中的正则化项。所得方法在不同模型中与架构无关。我们在具有涵盖安全性和可达性时序属性组合的规范套件的导航环境中评估了所提出的框架。实验结果表明，融入背景知识不仅改善了约束满足，而且与普通基线相比保持了有竞争力的回报。

## Abstract
In this work we study offline reinforcement learning (RL) under temporally extended task constraints expressed in Linear Temporal Logic over finite traces (LTLf). Recently, transformer-based approaches such as Trajectory Transformers and Decision Transformers have been adopted to address RL as a sequence modeling problem. However, these methods optimize purely for reward and do not account for high-level temporal requirements. Here, we introduce a neurosymbolic framework that injects LTLf background knowledge into such transformer-based RL policies. Our approach compiles LTLf formulas into deterministic finite automata (DFAs) and integrates them into the learning process through a differentiable representation and a logic-based loss function. In particular, we derive differentiable satisfaction signals from DFA progression and use them as a regularization term during training. The resulting method is architecture-agnostic across different models. We evaluate the proposed framework on navigation environments with specification suites covering combinations of safety and reachability temporal properties. Experimental results show that incorporating background knowledge not only improves constraint satisfaction, but also maintains competitive return compared to vanilla baselines.

---

## 论文详细总结（自动生成）

# 论文总结：Neuro-Symbolic Injection of LTLf Constraints in Autoregressive Reinforcement Learning Policies

## 1. 核心问题与整体含义（研究动机和背景）

- **研究背景**：离线强化学习（Offline RL）中，基于Transformer的序列建模方法（如Trajectory Transformer、Decision Transformer）通过最大化似然学习轨迹分布，但只优化累积奖励，忽略了高层时序约束（如安全性、可达性）。在安全关键场景（机器人、自动驾驶）中，违反约束可能导致严重后果，而离线环境无法通过在线交互纠正错误。
- **核心问题**：如何将形式化时序逻辑知识（LTLf）注入自回归Transformer策略，使其在保持奖励的同时满足约束。
- **整体含义**：提出一个神经符号框架，通过可微逻辑损失将LTLf约束作为正则项，引导策略生成符合逻辑的轨迹，提升可靠性且不依赖奖励工程或特定约束子集。

## 2. 方法论：核心思想、关键技术细节

- **核心思想**：将用户指定的LTLf约束编译为确定性有限自动机（DFA），利用可微的DFA进展（DeepDFA）计算轨迹满足概率，并构造逻辑损失与监督损失加权联合训练。
- **关键技术细节**：
  - **LTLf→DFA**：使用自动工具（如ltlf2DFA）将公式转为等价DFA，并扩展处理终止符号（EOT），确保仅在完整迹上评估。
  - **可微采样**：训练时采用Gumbel-Softmax采样从概率分布生成“软”离散符号，保持梯度流动。
  - **可微满足度计算**：使用DeepDFA（神经网络松弛的DFA）将软符号序列映射为概率满足值 \( P_{\text{DDFA}}(\tilde{\tau} \models \phi) \)。
  - **逻辑损失构造**：采样N条迹，计算平均满足概率 \( \hat{P}_{\theta\models\phi} = \frac{1}{N}\sum P_{\text{DDFA}} \)，逻辑损失为 \( L_\phi = -\log(\hat{P}_{\theta\models\phi}) \)。
  - **联合训练目标**：\( L = \alpha L_\phi + (1-\alpha)L_{\mathcal{D}} \)，其中 \(\alpha \in [0,1]\) 平衡逻辑约束与数据拟合。
  - **架构无关性**：该方法适用于任何自回归模型（TT、DT等），只需适配逻辑适配器（token schema、符号映射）。

## 3. 实验设计

- **环境**：ColourBomb导航网格，包含起点(S)、目标(P/Y/U)、炸弹(B)、墙壁(W)。每个步骤有小惩罚，到达目标正奖赏，踏入炸弹负奖赏并终止。
- **LTLf规格**：
  - **安全**：\( G(\neg bomb) \)（始终避免炸弹）
  - **可达-安全**：\( G(\neg bomb) \land F(goal) \)（安全地到达目标）
- **对比方法**：
  - 基线：vanilla TT和DT（\(\alpha=0\)，无逻辑正则化）
  - 本文方法：TT和DT分别设置不同 \(\alpha\)（0.01, 0.05, 0.1, 0.2, 0.4, 0.6, 0.8）
- **评估指标**：回报（Return）、满足率（Satisfaction Rate）、目标到达率（Goal Rate）、炸弹命中率（Bomb Hit Rate）
- **推理策略**：所有模型使用贪婪解码，隔离正则化效果。

## 4. 资源与算力

- 论文**未明确说明**使用的GPU型号、数量、训练时长等具体算力资源。仅提及代码已公开（GitHub），实验在标准计算设备上进行，未进一步详述。

## 5. 实验数量与充分性

- **实验数量**：在TT和DT两种架构上，针对两种规格各进行多组α值实验（每组α运行3次取均值标准差），共约2种架构 × 2种规格 × 7~8个α × 3次 = 84+个实验点。另有关键结果表格（表1、表2）总结对比。
- **充分性评估**：
  - **优点**：覆盖不同正则化强度，重复3次降低随机性，指标全面（回报、满足率、目标率、炸弹率）。
  - **不足**：
    - 仅一个导航环境，缺乏多样性（如复杂连续控制、多任务）。
    - 未与其他Safe RL方法（如CQL、CPO、Shielding、Constrained DT等）进行基准对比。
    - 仅测试贪婪解码，未比较束搜索等随机采样策略。
    - TT实验结果提升幅度较小（最高满足率0.499 vs 0.353），原因未深入分析。
  - **整体评价**：作为初步验证，实验设计合理但覆盖范围有限，充分性中等。

## 6. 主要结论与发现

- **Logic-regularized DT**：高α（0.8）能完全消除炸弹命中、达到100%满足率和目标率，回报从-1.46提升至-0.78。低α无改善，存在“阈值”效应。
- **Logic-regularized TT**：安全规格下α=0.8达到最佳满足率0.499 vs 基线0.353；可达-安全规格下α=0.1最佳满足率0.047 vs 基线0.018。TT提升幅度小于DT，回报改善较温和。
- **权衡**：引入逻辑正则化会降低回报（过于保守），需靠α调参平衡。DT上的逻辑学习能力更强，TT对复杂规格提升有限。
- **结论**：神经符号注入可有效提升约束满足，同时保持与基线相当的回报，是架构无关的通用增强方法。

## 7. 优点

- **架构无关性**：适用于任何自回归模型（TT、DT等），只需替换逻辑适配器，泛化性好。
- **无需修改数据或奖励**：不改变离线数据集，也不依赖奖励塑形，直接正则化训练过程，保留数据分布。
- **支持任意LTLf约束**：不受限制于“安全”片段，可表达复杂的时序目标（如顺序访问、避免模式等）。
- **可微逻辑损失**：利用Gumbel-Softmax和DeepDFA实现端到端梯度传播，简单有效。
- **规范评估堆栈**：统一token schema、符号映射、DFA评估和EOT处理，避免语义不一致。

## 8. 不足与局限

- **实验覆盖不足**：仅在单一网格导航环境验证，缺乏复杂连续控制、多智能体等场景。
- **缺乏与主流Safe RL方法对标**：未与CQL、CPO、Shielding、Constrained Decision Transformer等对比，无法评估相对优势。
- **TT提升有限**：尤其对“可达-安全”复杂规格，满足率仅从0.018提升至0.047，实际意义可能有限，原因未解释。
- **推理策略单一**：仅测试贪婪解码，未考虑束搜索或受自动机约束的剪枝解码（作者提到未来工作）。
- **调参敏感**：α需手动调整，且不同架构/规格最佳值差异大，缺乏自适应机制。
- **计算开销**：训练时需对每条迹进行DFA进展（DeepDFA）和Gumbel采样，可能增加训练时间，但未提供开销分析。
- **可扩展性**：当命题符号多或公式复杂时，DFA状态数量可能爆炸，影响可微计算效率。

（完）
