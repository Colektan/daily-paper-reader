---
title: "Gradient Perturbation: Learning to Perturb Gradients for Adaptive Training"
title_zh: 梯度扰动：学习扰动梯度以实现自适应训练
authors: Hua Li
date: 2026-05-28
pdf: "https://arxiv.org/pdf/2605.29494v1"
tags: ["query:rep-learning"]
score: 9.0
evidence: 梯度扰动框架用于自适应训练优化
tldr: 深度网络训练中，前向链路的扰动已被广泛研究，但反向链路的梯度扰动缺乏系统探索。本文建立梯度扰动统一框架，将SAM、梯度裁剪等方法纳入其中，并提出LPG（Learning to Perturb Gradients），自适应地对类别级logit梯度进行扰动以实现类别感知训练。实验表明LPG在平衡分类、长尾分类和噪声标签学习任务中一致优于现有方法，且可作为即插即用模块。理论方面通过PAC-Bayesian分析建立了梯度扰动边界与泛化保证的联系。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.29494v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1263, \"height\": 434, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.29494v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1266, \"height\": 472, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.29494v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1266, \"height\": 436, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.29494v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1267, \"height\": 437, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.29494v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1230, \"height\": 643, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.29494v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1263, \"height\": 395, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2605.29494v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1261, \"height\": 647, \"label\": \"Table\"}]"
motivation: 现有方法集中于前向扰动，忽略了梯度扰动的潜力；本文旨在统一梯度扰动框架并设计自适应的类别感知梯度扰动方法。
method: 提出梯度扰动统一框架，并针对logit级梯度设计LPG方法，根据类别自适应地放大或缩小梯度范数，实现类别感知训练。
result: 在平衡分类、长尾分类和噪声标签学习任务中，LPG均一致优于SAM等现有方法，并能作为插件模块提升性能。
conclusion: 通过PAC-Bayesian分析建立了梯度扰动边界与泛化保证的理论联系，为自适应训练提供了新范式。
---

## 摘要
深度神经网络训练既包括前向传播（从特征经由logits到损失），也包括反向传播（从损失经由梯度到参数更新）。尽管前向链中的扰动（包括特征扰动、logit扰动和标签扰动）已被广泛研究，但反向链中的梯度扰动却鲜有系统性研究。本文建立了一个统一的梯度扰动框架，揭示现有方法如锐度感知最小化（SAM）、梯度裁剪和梯度噪声注入均可解释为施加特定形式的梯度扰动。类比于近期提出的Logit扰动学习（LPL），我们推测放大某个类别的梯度范数起到正增强（增强学习）作用，而抑制则起到负增强（抑制过拟合）作用。基于这些观察，我们提出学习扰动梯度（LPG），该方法在类别级别自适应地扰动logit梯度，以实现类别感知训练。我们还通过PAC-Bayesian分析建立了梯度扰动边界与泛化保证之间的理论联系。在平衡分类、长尾分类和噪声标签学习上的实验表明，LPG始终优于现有方法，并且可以作为即插即用模块与它们结合。

## Abstract
Deep neural network training involves both forward propagation (from features through logits to loss) and backward propagation (from loss through gradients to parameter updates). While perturbations along the forward chain, including feature perturbation, logit perturbation, and label perturbation, have been extensively studied, the backward chain's gradient perturbation has received little systematic investigation. In this paper, we establish a unified framework for gradient perturbation, revealing that existing methods such as Sharpness-Aware Minimization (SAM), gradient clipping, and gradient noise injection can all be interpreted as imposing specific forms of gradient perturbation. Analogous to the recently proposed Logit Perturbation Learning (LPL), we conjecture that amplifying the gradient norm for a class acts as positive augmentation (enhancing learning), while dampening it acts as negative augmentation (suppressing overfitting). Based on these observations, we propose Learning to Perturb Gradients (LPG), which adaptively perturbs logit-level gradients at the class level to achieve category-aware training. We also establish theoretical connections between gradient perturbation bounds and generalization guarantees via PAC-Bayesian analysis. Experiments on balanced classification, long-tail classification, and noisy label learning demonstrate that LPG consistently outperforms existing methods and can be combined with them as a plug-in module.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：深度神经网络训练包含前向传播和反向传播两个计算链。前向链中的扰动（如数据增广、logit扰动、标签扰动）已被广泛研究，但反向链中的梯度扰动缺乏系统性探索。现有方法（如SAM、梯度裁剪、梯度噪声注入）虽然隐式地扰动梯度，但彼此孤立，没有统一框架，且不能根据类别自适应调整。
- **整体含义**：本文旨在建立梯度扰动的统一理论框架，并借鉴Logit Perturbation Learning (LPL) 的思想，提出一种自适应、类别感知的梯度扰动方法LPG，以提升模型在平衡分类、长尾分类和噪声标签学习等场景下的性能。该工作填补了梯度扰动系统性研究的空白，并提供了理论泛化保证。

## 2. 论文提出的方法论：核心思想、关键技术细节、公式或算法流程

- **核心思想**：
  - 将现有隐式梯度扰动方法（SAM、梯度裁剪、梯度噪声注入）统一表示为 `̃g = g + δg`，并揭示它们对不同类别的扰动模式是非自适应的。
  - 提出两个猜想：放大某类梯度范数 → 正增强（增强学习）；抑制某类梯度范数 → 负增强（抑制过拟合）。
  - 基于此，设计LPG：在logit梯度空间（低维，维度=类别数C）上学习类别级扰动，再通过链式法则传播到参数梯度。

- **关键技术细节**：
  - **Logit梯度扰动**：对样本x_i，logit梯度`h_i = ∂l/∂u_i`（C维）。LPG加入类别级扰动`δhc`：`̃h_i = h_i + δhc`（x_i属于类别c）。参数梯度通过Jacobian `J_i` 得到：`δc = J_i δhc`。
  - **闭式解**：沿`h_c`方向缩放，正增强：`̃h_c = h_c (1 + α_c/∥h_c∥)`；负增强：`̃h_c = h_c (1 - α_c/∥h_c∥)`。此形式避免显式计算Jacobian。
  - **PGD精化解**：在完整RC空间使用投影梯度下降（3步即可）优化`δhc`，最大化或最小化`∥J_i^T (h_i+δ)∥`。
  - **类别划分**：
    - 平衡分类：按类别平均准确率，低于阈值τ的类正增强，高于的负增强。
    - 长尾分类：按类别频率，少样本类正增强，多样本类负增强。
    - 噪声标签：按类别内梯度方差，方差高（噪声多）负增强，其余正增强。
  - **扰动边界**：`ϵc = ϵ + Δϵ·|τ - s̄_c|`，离阈值越远的类扰动越大。
  - **算法流程**（Algorithm 1）：每个epoch计算划分统计量→划分`Pa, Na`→计算`ϵc`→每个batch：前向得到logits和损失→反向求logit梯度→对每个类别施加扰动（闭式或PGD）→用扰动后的logit梯度计算参数梯度并更新。

- **理论贡献**：
  - 证明LPL是LPG的特例（通过二阶导数约束），LPG更灵活。
  - 用PAC-Bayesian分析给出泛化界：`L(W) ≤ L̂_LPG(W) + √[(KL||+log(2N/β))/(2N)] + λ Σ (N_c/N) ϵ_c`，说明扰动边界与泛化之间存在trade-off。

## 3. 实验设计：使用的数据集 / 场景，benchmark，对比方法

- **数据集**：CIFAR-10、CIFAR-100（平衡分类）；CIFAR-10-LT、CIFAR-100-LT（长尾，不平衡比100:1和10:1）；噪声标签实验：向CIFAR-10/100注入20%、50%、80%对称噪声。
- **场景**：平衡分类、长尾分类、噪声标签学习。
- **对比方法**：
  - 平衡分类：CE、Label Smoothing、Mixup、ISDA、LA、LPL（mean+fixed/varied）、SAM、Gradient Noise、LPG（mean+fixed/varied/PGD）。
  - 长尾分类：CE、LA、ISDA、LPL、SAM、LPG。
  - 噪声标签：CE、GCE、SCE、SAM、DivideMix、LPG。
- **网络架构**：Wide-ResNet-28-10、ResNet-110、ResNet-32、SE-ResNet-110、WRN-16-8（依实验不同）。
- **训练细节**：SGD动量0.9，权重衰减5e-4，batch 128；平衡分类训练200 epoch，学习率0.1，第100/150 epoch衰减0.1；长尾和噪声遵循各自标准协议。超参数ϵ, Δϵ, τ通过验证集选择。

## 4. 资源与算力

- **文中未明确说明**使用的GPU型号、数量和训练时长。仅指出LPG增加的时间开销：闭式解约3%，PGD约8%（对比SAM约15%）。因此，算力细节无法基于此文得出。

## 5. 实验数量与充分性

- **实验数量**：涵盖3大场景（平衡、长尾、噪声），每个场景包含多个数据集（CIFAR-10、CIFAR-100及其长尾/噪声变种）；对比了10+种基线方法；有多组消融（LPG闭式 vs PGD，固定 vs 变化扰动边界）；还做了组合实验（LPG+LA, LPG+LPL, LPG+SAM）；跨架构实验（ResNet-32, SE-ResNet-110, WRN-16-8）；超参数敏感性分析（τ, ϵ, PGD步数）；特征空间可视化（t-SNE）。
- **充分性与公平性**：实验设计比较全面，覆盖主要场景和主流方法，遵循标准协议（LPL论文设定），对比公平。但缺少大规模数据集（如ImageNet-LT）实验，泛化性有待验证。

## 6. 论文的主要结论与发现

- 建立了梯度扰动的统一框架，将SAM、梯度裁剪、梯度噪声纳入同一视角。
- LPG在平衡分类、长尾分类、噪声标签学习中一致优于LPL、SAM等方法，尤其长尾场景尾类准确率提升显著（CIFAR-100-LT整体32.0%，超LPL 3.2%）。
- LPG可作为即插即用模块，与LA、LPL、SAM组合后进一步提升性能，证实前向扰动与反向扰动的互补性。
- 在80%噪声率下，LPG在CIFAR-10上达75.5%准确率，超过DivideMix 2.7%。
- 理论分析揭示了梯度扰动边界与泛化的权衡关系。

## 7. 优点

- **理论贡献**：首次系统性建立梯度扰动统一框架，并给出PAC-Bayesian泛化界。
- **方法创新**：利用logit梯度空间的低维性，实现高效的类别自适应扰动，比直接在高维参数空间操作更可行。
- **实验全面**：在三个困难场景（平衡、长尾、噪声）验证，并与多种前沿方法对比，包含组合实验和消融。
- **实用性**：LPG增加开销极小（3%~8%），可作为插件融入现有训练流程。
- **理论联系**：揭示了梯度放大/抑制对应正/负增强的直观机制，与LPL形成对称性。

## 8. 不足与局限

- **实验覆盖不足**：未在更大规模数据集（如ImageNet、iNaturalist）或非视觉任务（NLP、生成模型）上验证，泛化性存疑。
- **类别划分规则依赖场景**：针对不同场景需要手动设计划分标准（准确率/频率/方差）与阈值τ，可能引入偏差。
- **理论假设强**：PAC-Bayesian分析依赖若干Lipschitz常数等条件，实际验证不充分。
- **扰动方向限制**：闭式解仅沿梯度方向缩放，PGD虽更优但增加计算，且未与更多样化的扰动形式（如旋转、缩放）对比。
- **缺少与更多噪声处理方法的对比**：在噪声标签实验中仅与DivideMix等对比，未包括最新方法如MentorNet、JoCoR等。
- **长期效应未分析**：梯度扰动在训练后期可能影响收敛性质，文中仅关注最终准确率，未分析训练动态。

（完）
