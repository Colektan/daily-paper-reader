---
title: Momentum-Guided Semantic Forecasting (MoFore) for Self-Supervised Video Representation Learning
title_zh: 动量引导的语义预测（MoFore）用于自监督视频表示学习
authors: Qinwu Xu
date: 2026-06-08
pdf: "https://arxiv.org/pdf/2606.14765v1"
tags: ["query:rep-learning"]
score: 8.0
evidence: 通过动量引导语义预测进行自监督视频表征学习
tldr: 针对自监督视频表示学习中重建与对比方法的局限性，提出MoFore框架。该方法通过从时间间隔上下文剪辑预测未来潜在嵌入，结合对比正则化与随机间隔训练，学习时间预测性表示。在UCF101上，表示具有时间稳定性与类别结构，检索展现运动感知组织。提供无需重建目标的计算高效新范式。
source: arxiv
selection_source: carryover_cache
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.14765v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1408, \"height\": 900, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.14765v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1394, \"height\": 163, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.14765v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 877, \"height\": 496, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.14765v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1440, \"height\": 1065, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.14765v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1440, \"height\": 1061, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.14765v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1439, \"height\": 1068, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.14765v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1447, \"height\": 156, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.14765v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1494, \"height\": 587, \"label\": \"Table\"}]"
motivation: 现有自监督视频方法依赖像素重建或语义对齐，效率或泛化性有限，需探索预测式表示学习。
method: 提出MoFore，从时间间隔上下文剪辑预测未来潜在嵌入，采用对比正则化与随机间隔训练，促进时间一致性与特征多样性。
result: 在UCF101上，学到的表示时间稳定且具类别结构，定性检索显示运动感知关联，定量分析验证有效性。
conclusion: 长期潜在预测为自监督视频学习提供有效、计算高效的范式，无需重建目标，具扩展潜力。
---

## 摘要
自监督视频表示学习近期通过对比学习、掩码重建和预测表示学习取得了进展。基于重建的方法如MAE和VideoMAE通过恢复掩码视觉内容来学习表示，而对比方法如CLIP通过表示对齐学习语义有意义的嵌入空间。本文提出了一种动量引导的语义预测框架（MoFore），用于自监督视频表示学习。该方法不优化像素级重建或任务特定的语义对齐，而是通过从时间上遥远的上下文片段预测未来潜在嵌入，来学习时间预测性的视频表示。为提升跨时间尺度的鲁棒性，我们在训练中进一步引入了随机时间间隔预测。该框架结合了预测潜在编码和对比正则化，以在防止表示坍塌的同时促进时间一致性。在UCF101数据集上的实验表明，该框架在训练中未使用动作标签的情况下，学习了时间一致且语义有意义的视频表示。定量分析显示，学习到的嵌入空间具有较强的时间稳定性和涌现的类别级结构，而定性检索实验揭示了相关活动之间具有运动感知的组织结构。总体而言，结果表明长程潜在预测为自监督视频表示学习提供了一种有效且计算高效的方法，而不依赖于基于重建的目标。

## Abstract
Self-supervised video representation learning has recently advanced through contrastive learning, masked reconstruction, and predictive representation learning. Reconstruction-based approaches such as MAE and VideoMAE learn representations by recovering masked visual content \cite{he2022mae,tong2022videomae}, while contrastive methods such as CLIP learn semantically meaningful embedding spaces through representation alignment \cite{radford2021clip}.   In this work, we introduce a Momentum-Guided Semantic Forecasting framework (MoFore) for self-supervised video representation learning. Instead of optimizing for pixel-level reconstruction or task-specific semantic alignment, the proposed method learns temporally predictive video representations by forecasting future latent embeddings from temporally distant context clips. To improve robustness across temporal scales, we further introduce randomized temporal-gap forecasting during training. The framework combines predictive latent forecasting with contrastive regularization to encourage temporal consistency while preventing representation collapse.   Experiments on the UCF101 dataset demonstrate that the proposed framework learns temporally consistent and semantically meaningful video representations without using action labels during training. Quantitative analysis shows strong temporal stability and emergent category-level structure in the learned embedding space, while qualitative retrieval experiments reveal motion-aware organization across related activities. Overall, the results suggest that long-range latent forecasting provides an effective and computationally efficient approach for self-supervised video representation learning without relying on reconstruction-based objectives.

---

## 论文详细总结（自动生成）

# 论文结构化总结

## 1. 核心问题与整体含义（研究动机与背景）

- **研究动机**：当前自监督视频表示学习的主流范式包括**对比学习**（如SimCLR、MoCo）和**掩码重建**（如VideoMAE）。对比学习依赖大量负样本或动量机制，重建方法则强制模型恢复像素级细节，消耗大量计算资源且可能与高层语义无关。此外，现有预测式方法（如CPC、JEPA）多局限于短程或空间预测，未能充分利用视频中的长程时间结构。
- **整体含义**：本文提出一种**动量引导的语义预测框架（MoFore）**，旨在通过**从时间上远离的上下文片段预测未来潜在嵌入**来学习视频表示。该方法不依赖像素重建或语义标签，而是通过**长程潜在空间预测**直接捕获语义演化，为自监督视频学习提供了一种计算高效且语义突出的新范式。

## 2. 方法论：核心思想、关键技术细节与算法流程

- **核心思想**：将视频表示学习建模为**长程时序语义预测**任务。给定一个上下文片段和一个未来目标片段（二者间存在随机时间间隔），学生网络学习从上下文表示预测未来表示，而教师网络（动量更新）提供稳定的预测目标。
- **关键技术细节**：
  - **随机时间间隔采样（Randomized Temporal-Gap Forecasting）**：时间间隔 \( g \) 在训练时随机采样，使模型适应从短到长的多种预测跨度，避免过拟合到特定时间尺度。
  - **动量教师网络**：教师编码器参数通过指数移动平均（EMA）更新：\(\xi \leftarrow m\xi + (1-m)\theta\)，提供稳定的未来语义目标。
  - **轻量预测网络**：一个两层MLP（\(P_\phi(z) = W_2\sigma(W_1z)\)）将上下文表示映射到未来潜在空间。
  - **损失函数**：结合**语义预测损失（MSE）** 和**对比正则化（InfoNCE）**。
    - 预测损失：\(\|\hat{z}_t - z_t\|_2^2\)，推动预测嵌入与教师目标对齐。
    - 对比损失：\(-\log \frac{\exp(\text{sim}(z_i, z_i^+)/\tau)}{\sum_j \exp(\text{sim}(z_i, z_j)/\tau)}\)，防止表示坍塌，促进特征多样性。
    - 总损失：\(\mathcal{L}_{\text{forecast}} + \lambda \mathcal{L}_{\text{con}}\)。
- **算法流程**（文字说明）：
  1. 从视频中随机采样上下文片段 \(C\) 和未来目标片段 \(F\)，二者间隔 \(g\) 随机。
  2. 学生编码器提取 \(C\) 的帧级特征并聚合为上下文表示 \(z_c\)，预测网络输出 \(\hat{z}_t\)。
  3. 教师编码器提取 \(F\) 的表示 \(z_t\)（不参与梯度反向传播）。
  4. 计算 MSE 预测损失和 InfoNCE 对比损失，反向传播更新学生网络参数。
  5. 用 EMA 更新教师网络参数。

## 3. 实验设计：数据集、基准与对比方法

- **数据集**：**UCF101**（13,320 个视频，101 个人类动作类别，包含多样化运动、相机运动、场景等）。训练/验证按 80/20 随机划分。**训练阶段不使用任何标签**。
- **基准与对比**：
  - **未直接与现有方法（如 VideoMAE、CPC、JEPA）进行数值对比**（如在下游任务中的动作识别准确率）。
  - 仅在**结构上**与 MoCo（对比学习）和 FUTURIST（密集像素级预测）进行了概念性比较（Table 1），说明 MoFore 聚焦于抽象、全局的潜在时域预测。
  - 实验主要评估**自身框架的嵌入质量**，包括预测误差、时间一致性、类内/类间相似度，以及定性检索结果。

## 4. 资源与算力

- **文中未明确说明**使用的 GPU 型号、数量、训练时长等算力信息。
- 仅提及优化器为 Adam，学习率 1×10⁻⁴，batch size 为 8，教师动量系数为 0.7。训练细节不够充分。

## 5. 实验数量与充分性

- **实验数量**：仅有一组定量实验（Table 2）和若干定性检索示例（Figure 3-5）。
- **充分性评价**：
  - **不足**：缺乏消融实验（如随机间隔 vs 固定间隔、对比损失权重 \(\lambda\) 的影响、预测层设计等）；未在更大规模数据集（如 Kinetics、Something-Something）上验证；未与主流方法在同一下游任务（如动作识别准确率）上对比。
  - **客观性**：定量指标（预测误差 0.0039、同视频相似度 0.9995、同类别相似度 0.7901 等）在统计上合理，但样本量较小（仅列举了 YoYo 和 Skydiving 两类）。检索结果有一定说服力，但存在失败案例（Hula Hoop 检索到 Cello），说明表示仍受姿态和运动结构影响。
  - **公平性**：未提供与 SOTA 的公平对比，难以判断其实际性能定位。

## 6. 主要结论与发现

- **长程语义预测**可以成功学习到**时间稳定**（同视频不同视图相似度 0.9995）、**语义有意义**（同类相似度 0.7901 vs 异类 0.5521）的视频表示，且**无需动作标签**。
- 检索结果揭示表示空间按**运动模式、姿态几何和身体动力学**组织，超越了单纯的外观相似性（如 Skiing 检索到 HammerThrow 等共享旋转动作的视频）。
- 证明了**预测式潜在目标**可以替代像素重建，作为一种计算高效的自监督视频学习范式。

## 7. 优点

- **创新性**：将视频自监督学习重新定义为**长程潜在语义预测**，区别于短期预测或像素重建，具有清晰的物理直觉。
- **设计简洁高效**：轻量预测网络（仅两层 MLP），不依赖自回归生成或密集 token 预测，计算开销低。
- **随机间隔预测**：增强了模型对不同时间尺度的鲁棒性，避免固定间隔的过拟合。
- **动量教师 + 对比正则化**：有效防止表示坍塌，同时保持时间一致性和特征多样性。
- **可视化与分析**：定性检索实验直观展示了表示的语义组织，为理解模型行为提供有力证据。

## 8. 不足与局限

- **实验覆盖过窄**：仅使用 UCF101 单个数据集，且未与现有最先进方法（如 VideoMAE、JEPA 等）在标准动作识别基准上比较准确率，无法判断实际性能水平。
- **缺乏消融研究**：未单独验证随机间隔、对比损失、动量更新等各组件的作用和敏感性。
- **算力信息缺失**：无法评估方法的实际训练成本和可扩展性。
- **表示仍有偏差**：检索失败案例表明表示对姿态和运动结构敏感，但可能混淆语义不同的动作（如 Hula Hoop 与 Playing Cello），说明缺乏精细时序建模。
- **潜在风险**：当前采用帧级平均聚合，未建模时序顺序变换，可能限制对复杂动作（如多阶段活动）的表示能力。
- **泛化性存疑**：未在更大、更多样化的视频数据集（如 Kinetics-400、Ego4D）上验证，且未在时序动作检测等下游任务测试。

（完）
