Title: Unification and Optimization of Robust Supervised Learning

URL Source: https://arxiv.org/pdf/2605.28165v1

Published Time: Thu, 28 May 2026 00:56:37 GMT

Number of Pages: 25

Markdown Content:
# Unification and Optimization of Robust Supervised Learning 

Jonas Hanselle ∗

LMU Munich, MCML 

jonas.hanselle@ifi.lmu.de 

Valentin Margraf ∗

LMU Munich, MCML 

valentin.margraf@ifi.lmu.de 

Clemens Damke 

LMU Munich, MCML 

clemens.damke@ifi.lmu.de 

Eyke Hüllermeier 

LMU Munich, MCML, DFKI 

eyke@lmu.de 

Abstract 

The literature has proposed various robust alternatives to empirical risk minimiza-tion to address failure modes such as distribution shift, label noise and finite-sample degeneracies. Examples include distributionally robust optimization, label smooth-ing, vicinal risk minimization, and Mixup. However, such approaches are typically developed in isolation, forcing practitioners to commit a priori to a single failure mode even when the dominant mode for the task is unclear. To address this, we organize a broad class of existing methods along three common design axes and de-rive a tractable training procedure that decomposes robust learning into sequential stages (reference distribution enrichment, input-space perturbation, label-space per-turbation, and sample-level aggregation), each with a choice of stance (pessimistic, neutral, or optimistic). This results in a unified design space in which joint hyper-parameter optimization can compose and configure robustness strategies suited to the task at hand. Across tabular, image, and reward modeling benchmarks, joint hyperparameter optimization is competitive with the best single-method baseline in each setting, offering a reliable default for practitioners who do not know a priori which failure mode dominates their task. 

1 Introduction 

A typical supervised learning process involves a sequence of decisions under uncertainty. Training data is collected, labels are assigned, a model is trained, and the resulting predictor is deployed into an environment the practitioner can rarely fully anticipate. At any of these stages, a failure can occur. For instance, consider reward modeling from human preferences, a central component in aligning modern language models [ 23 , 50 ]. A practitioner building such a reward model faces multiple, potentially interacting sources of imperfection simultaneously. First, training data is finite and often unrepresentative: annotators cover only a small portion of the prompt space, leaving large regions unobserved [ 42 ]. Second, labels are inherently noisy and subjective: different annotators apply different criteria, and even the same annotator may be inconsistent over time [ 21 ]. Third, deployment conditions differ from the annotation setting: models are evaluated on prompts that vary in topic, style, language, and implicit standards. Each of these failure modes is well-studied in isolation, and a wide range of methods have been proposed under the umbrella term of robust supervised learning. Finite-sample degeneracy is mitigated by enriching the training distribution in input space via data augmentation and vicinal                

> ∗equal contribution Preprint.
> arXiv:2605.28165v1 [cs.LG] 27 May 2026 Feature x1
> Feature  x2
> ERM (Acc: 0.64)
> Feature x1
> VRM (Acc: 0.80)
> Feature x1
> W-DRO (Acc: 0.79)
> Feature x1
> W-DRO + VRM (Acc: 0.88)
> Class 0 Class 1 Train Test Gaussian Vicinity Perturbed Data Point 2Radius

Figure 1: Two-moons classification with a gap in class 0 and a shifted test distribution (triangles). 

VRM enriches Pref with virtual samples (dots); W-DRO perturbs points toward the decision boundary (crosses). Their combination produces a decision boundary that generalizes best. methods [ 8, 17 , 25 , 47 , 48 ]. Label uncertainty is addressed through softening or relaxing labels, e.g. label smoothing [ 43 ] and label relaxation [ 29 ]. Distribution shift is handled through adversarial training [ 14 , 31 ] and distributionally robust optimization (DRO) [ 4, 40 ], as well as its optimistic coun-terpart, distributionally favorable optimization (DFO) [ 22 ]. Despite their diversity, these approaches share a common goal: learning predictors that generalize reliably under uncertainty. However, they differ along several implicit modeling choices—how the data distribution is represented, what deviations are considered plausible, and how uncertainty is resolved. Prior work entangles these choices within specific objectives, making it difficult to compare methods systematically and forcing practitioners to commit a priori to a particular failure mode, even when the dominant mode for the task at hand is unclear. Yet, mechanisms targeting different failure modes can compose to outperform any single one when multiple modes coexist, as illustrated in Figure 1 on the two-moons example. Our work aims to relieve practitioners of the burden to commit to a single robustness mechanism a priori. To this end, we introduce a unified framework that decomposes robust supervised learning into three orthogonal design axes—a reference distribution , an ambiguity set , and a disambiguation principle —and a tractable training procedure that exposes them as continuous hyperparameters of a single objective. This enables joint hyperparameter optimization to discover the right configuration of robustness mechanisms for a given task. Our contributions can be summarized as follows: • Unified framework. We introduce a general formulation of robust supervised learning spanning three explicit modeling axes: (i) the reference distribution, (ii) the ambiguity set, and (iii) the disambiguation principle, which recovers many existing methods as special cases. • Theoretical connections. We give closed-form reductions of vicinal risk minimization (VRM), Mixup, and W-DRO to modified ERM objectives for linear models with squared loss, clarifying the qualitative relations between these mechanisms. • Tractable training procedure. We derive a single training pipeline in which all design choices appear as continuous hyperparameters of a single objective, enabling joint hyperpa-rameter optimization across the entire space and end-to-end training via backpropagation. • Practical applicability. Across tabular, vision, and reward-modeling tasks, joint optimiza-tion delivers consistent gains on suitable metrics and the largest improvements on reward modeling, hence offering a reliable default when the dominant failure mode is unknown and committing to a single mechanism is risky. 

2 Robust Supervised Learning 

In supervised learning, we consider an input space X , an output space Y, and assume that data is sampled from P ⋆ ∈ P (X × Y ), where P(X × Y ) denotes the set of all probability measures on 

X × Y . Given a hypothesis class H and a loss function ℓ : Y × Y → R, the goal is to find a hypothesis 

h ∈ H minimizing the population risk, 

h⋆ = arg min 

> h∈H

R(h; P ⋆) = arg min 

> h∈H

E(x,y )∼P ⋆ [ℓ(h(x), y )] .

2Empirical    

> Pn
> x
> p
> augment/
> smooth
> Reference
> Pref
> x
> p
> perturb
> Ambiguity Set
> U(Pref )
> x
> p
> select
> Disambiguation
> Φ
> Q∈U (Pref )
> x
> p

Figure 2: Our three-axis framework (i) enriches the empirical distribution Pn into a reference distribution Pref , (ii) defines an ambiguity set U(Pref ) of plausible perturbations of Pref , and (iii) resolves the resulting uncertainty via a disambiguation principle Φ.Since P ⋆ is usually unknown, one relies on a dataset D = {(xi, y i)}ni=1 drawn i.i.d. from P ⋆ and constructs the empirical distribution Pn := 1

> n

Pni=1 δ(xi,y i), where δ(x,y ) denotes the Dirac measure at (x, y ). Empirical risk minimization (ERM) then selects a hypothesis 

ˆhERM = arg min 

> h∈H

R(h; Pn) = arg min 

> h∈H

1

n

> n

X

> i=1

ℓ(h(xi), y i).

ERM can be suboptimal in practice for several reasons [ 4]. First, Pn is a poor proxy for P ⋆ when data are scarce: it is a degenerate discrete measure that assigns zero mass to unobserved regions of the input space. Second, observed samples may deviate from P ⋆ due to noise, annotation errors, or contamination. Third, model performance is ultimately evaluated under a deployment distribution 

Pdeploy that may differ from P ⋆ due to, e.g. distributional shift or adversarial attacks. Together, these issues motivate learning paradigms that go beyond ERM. In the following, we recall existing methods relevant to this work, organized by the failure mode they address. 

Finite-sample degeneracy. To mitigate the degeneracy of Pn, methods such as vicinal risk mini-mization (VRM) [ 8 ] and Mixup [ 48 ] enrich the training distribution by introducing synthetic samples. VRM places a local vicinity distribution around each training point, while Mixup interpolates globally between pairs of examples. More broadly, a large body of work develops modality-specific data augmentation strategies, e.g. CutMix [47] and AugMix [17] for image data. 

Label noise and miscalibration. When labels are unreliable, robustness can be achieved by modifying the label distribution. Label smoothing [ 43 ] redistributes probability mass away from the annotated class, improving calibration [ 32 ]. Label relaxation [ 29 ] replaces hard labels with sets of admissible distributions, allowing multiple predictions to incur zero loss. Crucially, these methods act on the label space while leaving the input distribution unchanged. 

Distribution shift. When the deployment distribution differs from the training distribution, robustness requires accounting for plausible shifts. At the instance level, adversarial train-ing [ 14 , 31 , 36 , 46 , 49 ] enforces robustness to local perturbations. At the distribution level, distri-butionally robust optimization (DRO) [ 4 , 40 ] guards against worst-case shifts that may arise after 

deployment (e.g., covariate shift or adversarial perturbations). In contrast, distributionally favorable optimization (DFO) [ 22 ] adopts an optimistic perspective, targeting shifts that occurred before the decision (e.g., label noise or sample contamination) and may be partially correctable [ 4 ]. Intermediate approaches, such as probabilistically robust learning [35], interpolate between these regimes. Despite targeting different failure modes of ERM, the methods above ultimately aim at learning predictors that generalize reliably to deployment data. However, they achieve robustness through different formulations, making it difficult to compare approaches, combine their strengths, or tune them in a coherent manner. A unified framework that makes these assumptions explicit is therefore needed. 

3 A Unified Framework 

Each robustness method reviewed above can be understood as making a set of modeling choices, often implicitly. To enable systematic comparison and combination, we make these choices explicit and organize them into a unified framework. Concretely, we decompose robust learning into three independent design dimensions: (i) the choice of a reference distribution , (ii) the specification of 3plausible deviations from that reference, and (iii) the principle used to resolve the resulting uncertainty into a single training objective. This decomposition isolates the core assumptions underlying existing approaches and enables systematic comparison and combination. For instance, Wasserstein-DRO uses the empirical distribution Pn as a reference, defines plausible deviations as a Wasserstein ball of radius ϱ around it, and resolves uncertainty by taking the worst case over that ball [ 26 ]. This however is one specific choice along each of three axes. We treat those as independent design dimensions (cf. Figure 2) and formalize below. 1. Reference distribution Pref : The distribution around which perturbations are defined. This can be the empirical distribution Pn, or a smoothed or augmented variant thereof. 2. Ambiguity set U(Pref ): A set of plausible distributions capturing uncertainty in inputs, labels, or both, defined via a distribution distance ∆ and a perturbation radius ϱ. The source of uncertainty can be separately structured across input and label spaces: 

U(Pref ) = Q ∈ P (X × Y ) : ∆( Q, P ref ) ≤ ϱ .

Common choices for ∆ include the p-Wasserstein distance Wp, which measures the cost for geometric displacement of mass between Q and Pref [ 26 ], and the KL divergence, which measures re-weighting of Pref ’s support and is therefore restricted to Q ≪ Pref [20]. 3. Disambiguation principle Φ: A rule encoding the practitioner’s stance toward the uncer-tainty captured by U(Pref ). If the practitioner believes deviations represent contamination, like label noise, corrupted samples, etc., an optimistic stance is appropriate, seeking the best case risk within the ambiguity set. If deviations are expected to occur after deployment, like adversarial perturbations, covariate shift, domain change, etc., a pessimistic stance is warranted, hedging against the worst case. Together, these three axes specify an explicit robust learning problem: the reference Pref anchors the ambiguity set U(Pref ), which defines the distributions over which the disambiguation principle Φ is taken. We define the general robust objective as follows: 

ˆhrobust = arg min 

> h∈H

Φ 

> Q∈U (Pref )

E(x,y )∼Q[ℓ(h(x), y )] (1) Three observations help orient how this objective subsumes existing methods. First, ERM is the special case Pref = Pn, ϱ = 0 : the ambiguity set collapses, the aggregation is trivial, and (1) reduces to empirical risk. Second, methods that act through the reference (label smoothing, VRM, Mixup) correspond to ϱ = 0 with a non-trivial Pref , so the singleton case is not a special encoding but the natural limit ϱ → 0. Third, methods that act through the ambiguity set (DRO, DFO) keep Pref = Pn

and vary Φ ∈ { sup , inf } over the ambiguity set, differing only in their stance toward uncertainty. Table 1 presents these recoveries; formal statements and proofs are in Appendix D. 

Remark 1 (Structured and factorized ambiguity sets) . The framework places no restriction on the form of U(Pref ). Of particular practical interest is the case where the uncertainty decomposes over the input and label space and the sampling distribution. In this case, a structured ambiguity set across the spaces with distinct distances and radii for each is applicable. The disambiguation principle then decomposes as ΦX ◦ ΦY ◦ ΦS , permitting different stances toward covariate shift, label noise, and sample corruption simultaneously. This is the basis for the training procedure in Section 4. 

3.1 Regularization for Linear Models 

For linear models hw(x) = w⊤x trained with the squared loss ℓ(hw(x), y ) = ( y − w⊤x)2, covering both linear regression and binary classification with y ∈ {− 1, +1 } (least-squares classification), several methods recovered by the framework reduce to ERM with an explicit regularizer whose forms are qualitatively distinct. We make this precise below; full proofs are given in Appendix D. We assume linear models include an intercept, incorporated implicitly by augmenting features with a constant. Vicinal risk minimization with Gaussian noise ( ˜x = x + ϵ, ϵ ∼ N (0 , σ 2I)) induces the following regularizer [8, Section 3a]: 

RVRM (w) = RERM (w) + σ2∥w∥2. (Props. 7, 10) 4Table 1: Existing methods can be recovered as special cases of the unified robust framework (1) , each defined by a specific choice of reference distribution Pref , ambiguity set U(Pref ), and disambiguation principle Φ. Detailed propositions and proofs are provided in Appendix D.                                               

> Method Reference Pref Ambiguity set U(Pref )Disambig. principle Φ
> Label smoothing [43] Smoothed ˜Pls Singleton {˜Pls }-Prop. 1
> Label relaxation [29] Empirical PnTV ball over labels, radius αinf Prop. 2
> DRO [4] Empirical PnStatistical ball (e.g. Wasserstein), radius ϱsup Prop. 3
> DFO [22] Empirical PnStatistical ball, radius ϱinf Prop. 4
> Mixup [48] Mixup ˜Pmix Singleton {˜Pmix }-Prop. 5
> VRM [8] Vicinal ˜Pvic Singleton {˜Pvic }-Prop. 6

Mixup has a different effect. Let ¯x = 1

> n

Pni=1 xi and ¯y = 1

> n

Pni=1 yi denote the empirical feature and label means. A direct calculation gives the deterministic identity 

RMixup (w) = (1 − 2cα) RERM (w) + 2 cα (¯ y − w⊤ ¯x)2, (Props. 8, 11) with cα = E[λ(1 − λ)] for λ ∼ Beta( α, α ). The added term (¯ y − w⊤ ¯x)2 vanishes at the ERM minimizer (whose normal equations imply w⊤ ¯x = ¯ y), so Mixup leaves the linear least-squares solution unchanged, which recovers the observation of Carratino et al. [7, Corollary 6]. W-DRO induces a regularizer that is linear in ∥w∥ rather than quadratic. The identity is asymptotic, obtained from the small-radius first-order expansion of the Wasserstein dual [ 11 , Theorem 1], which replaces the distributional supremum by an instance-wise gradient-norm surrogate: 

RDRO (w) = RERM (w) + 2 ϱ ∥w∥q∗ · 1

n

> n

X

> i=1

|yi − w⊤xi| + o(ϱ), (Props. 9, 12) where ∥w∥q∗ is the dual norm to the Wasserstein cost and controls the scale of the weight. Label smoothing additionally admits a clean closed-form characterization in the classification setting. With smoothing parameter αLS ∈ [0 , 1] and softened targets ˜yi = (1 − αLS ) yi,

RLS (w) = (1 − αLS )2 RERM 

 w/ (1 − αLS ), i.e. w⋆ 

> LS

= (1 − αLS ) w⋆

> ERM

. (Prop. 13) Label smoothing thus does not change the direction of the ERM solution, it only rescales its magnitude by the factor 1 − αLS .Thus, the four methods behave very differently on linear least squares. VRM penalizes the size of the weights, with strength fixed by the assumed noise level. Mixup has no effect: it penalizes the residual variance, which is already zero at the ERM optimum, so the minimizer does not move. W-DRO also penalizes the size of the weights, but scales the penalty by the current residuals. Label smoothing rescales the ERM solution toward zero without changing its direction. 

4 Tractable Learning and Optimization 

The unified framework (1) encompasses objectives with fundamentally different structures, ranging from inner max/min problems over distribution spaces to sampling-based vicinal objectives. A priori, it is not obvious that these admit a common training procedure, and exact solutions are generally intractable. However, exact solutions are not a prerequisite for effective learning and good generalization: Stochastic gradient descent does not solve the ERM problem exactly either, yet generalizes well in practice. Thus, rather than solving (1) to optimality, we construct a practical surrogate that preserves the qualitative behavior of each instantiation while admitting end-to-end training via backpropagation. Figure 3 illustrates this unified training procedure. Given a mini-batch sampled from the empirical distribution Pn, providing a Monte Carlo estimate of all subsequent expectations, the algorithm proceeds through four stages before a gradient update. Stage 1 either leaves the mini-batch untouched, or enriches it with e.g. VRM or Mixup. Stage 2 realizes ΦX by applying an (instance-wise) input-space perturbation within a Wasserstein ball of radius ϱ via projected gradient descent (PGD) [ 31 , 40 ], 5Batch                                                                

> (xi, y i)∼Pn
> 1. Reference dist. Pref
> Gaussian VRM
> ˜x=x+ε, ε∼ N (0 , σ 2I)
> Mixup
> ˜x=λx i+ (1 −λ)xj
> ˜y=λy i+ (1 −λ)yj
> Identity Pref =Pn
> 2. Input-space ΦX
> Ambiguity set, radius ϱ,via PGD
> pessimistic (W-DRO)
> arg max ∥δ∥≤ ϱℓ(h(x+δ), y )
> neutral δ=0
> optimistic (W-DFO)
> arg min ∥δ∥≤ ϱℓ(h(x+δ), y )
> 3. Label-space ΦY
> Credal set, mass α,independent of ΦX
> pessimistic
> shift αto arg min k̸=yipk
> neutral (label smoothing)
> ˜y=(1 −α)y+αK1
> optimistic (label relaxation)
> shift αtoward model pred.
> 4. Sample-space ΦS
> KL-ball aggregation, temp. τ
> pessimistic (KL-DRO)
> τlog 1
> n
> P
> iexp( ℓi/τ )
> neutral (ERM) 1
> n
> P
> iℓi
> optimistic (KL-DFO)
> −τlog 1
> n
> P
> iexp( −ℓi/τ )
> Update
> θ←θ−ηlr ∇θℓ

Figure 3: Unified robust training procedure. A mini-batch passes through four stages: distributional enrichment ( Pref ), input-space perturbation ( ΦX ), label-space perturbation ( ΦY ), and batch-level aggregation ( ΦS ), before a single gradient update. adopting a pessimistic (worst-case), neutral, or optimistic (best-case) stance. Stage 3 realizes ΦY

as an independent label-space perturbation within a credal set of mass α, again with a choice of stance, ranging from label smoothing (neutral) to label relaxation (optimistic) to adversarial label shifting (pessimistic). This is based on an extension of the loss defined in [ 29 ] and discussed in the Appendix B.1. Stage 4 realizes ΦS by aggregating individual losses across the mini-batch. 

Lpess = τ log  1

> n

P 

> i

exp( ℓi/τ ) and Lopt = −τ log  1

> n

P 

> i

exp( −ℓi/τ ) are the closed-form duals of the KL-constrained worst/best-case expectation problem [ 3, 27 ]. The neutral case recovers standard ERM. Interestingly, Lpess and Lopt are equivalent to tilted ERM [ 28 ] with tilt parameters t = 1 /τ 

and t = −1/τ , respectively. All design choices such as the VRM bandwidth σ, the Wasserstein radius ϱ, the label relaxation strength α, and the KL dual parameter τ are expressed as explicit hyperparameters within a single unified loss. A single training pipeline therefore implements a wide range of established methods, e.g. σ > 0 alone recovers VRM and ϱ > 0 alone recovers W-DRO. 

Joint hyperparameter optimization. Because every design choice is exposed as a continuous hyperparameter of a single objective, configuring an entire robustness pipeline reduces to a single hyperparameter optimization (HPO) problem that can be solved with standard black-box methods such as Bayesian optimization [ 13 ] or random search. This reframes the practitioner’s role from selecting a robustness method a priori to specifying a search budget. Rather than committing in advance to a particular approach (e.g., W-DRO with a hand-tuned radius), one performs joint HPO over (σ, ϱ, α, τ ). This unified formulation naturally includes existing methods as special cases while also enabling novel combinations. Thus, rather than expecting the practitioner to anticipate which combination of robustness mechanisms works best, this decision is now made in a data-driven way. 

5 Experiments 

In practice, the dominant source of uncertainty for a given task is rarely known a priori, and the right robustness method to deploy is correspondingly unclear. Selecting one method up front amounts to committing to a particular failure mode, regardless of which failure modes actually arise in the data. We therefore evaluate all methods under identical conditions across three modalities: tabular data via TableShift [ 12 ] (Hypert., Diabetes, College, Hospital; remaining datasets in Appendix C), image data via Waterbirds [ 38 ] and CelebA [ 30 ], and reward modelling via HH-RLHF [ 2]. Many robustness methods exist; we therefore compare against representative methods that each address one failure mode. Eight baselines span the three robustness axes: distribution shift (W-DRO, KL-DRO, W-DFO, KL-DFO), output-space (Label Smoothing, Label Relaxation), input-space (VRM); together with ERM. We additionally include Joint , the configuration enabled by our framework that tunes the full search space jointly. We report accuracy, Brier score, and CVaR ( 10% ) over 10 seeds. Following Gulrajani and Lopez-Paz [15] , we evaluate under both ID and OOD validation regimes. Within each run, the same metric drives both HP search and checkpoint selection. To give every method a fair chance at its best performance, all methods receive the same HPO budget of 50 trials with Optuna’s TPE sampler [ 1 ]. Reported numbers therefore reflect what each method can achieve under matched tuning effort. Full details are deferred to Appendix C. 

Tabular. Table 2 reports results on four TableShift datasets. Several patterns are visible across the columns. On accuracy, all methods achieve comparable performance: ERM, KL-DRO, LS, LR, and the Joint configuration all sit within one standard deviation of each other on every dataset, with 6Table 2: Tabular benchmarks. Mean ± std over 10 seeds, OOD val / OOD test. Acc. ↑ CVaR ↓ Brier ↓

Hypert. Diabetes College Hospital Hypert. Diabetes College Hospital Hypert. Diabetes College Hospital ERM .643 ±.006 .829 ±.003 .833 ±.003 .617 ±.001 1.325 ±.084 2.076 ±.107 2.298 ±.031 1.354 ±.035 .443 ±.003 .244 ±.002 .249 ±.005 .461 ±.001 

VRM .640 ±.003 .826 ±.004 .831 ±.003 .618 ±.001 1.338 ±.153 1.919 ±.361 2.173 ±.135 1.353 ±.022 .447 ±.004 .243 ±.006 .250 ±.003 .461 ±.001 

W-DRO .553 ±.013 .827 ±.000 .767 ±.026 .569 ±.008 1.150 ±.040 2.108 ±.032 1.777 ±.140 .994 ±.029 .497 ±.008 .260 ±.002 .330 ±.020 .483 ±.004 

KL-DRO .643 ±.001 .830 ±.001 .827 ±.003 .617 ±.001 .880 ±.070 1.625 ±.153 .814 ±.007 .802 ±.016 .442 ±.002 .242 ±.006 .251 ±.006 .460 ±.000 

W-DFO .585 ±.016 .828 ±.001 .772 ±.021 .581 ±.008 1.319 ±.060 2.176 ±.028 1.796 ±.209 1.173 ±.043 .486 ±.013 .255 ±.002 .331 ±.017 .480 ±.004 

KL-DFO .639 ±.005 .826 ±.004 .810 ±.003 .601 ±.014 1.641 ±.231 3.662 ±1.211 1.900 ±.179 1.544 ±.027 .461 ±.014 .245 ±.006 .284 ±.006 .466 ±.001 

LS .638 ±.004 .831 ±.001 .832 ±.003 .618 ±.001 .913 ±.034 .988 ±.048 .992 ±.009 .899 ±.008 .443 ±.002 .243 ±.005 .253 ±.005 .460 ±.001 

LR .639 ±.003 .831 ±.001 .832 ±.005 .618 ±.001 .907 ±.036 .970 ±.060 1.016 ±.013 .901 ±.008 .444 ±.002 .245 ±.005 .252 ±.006 .460 ±.001 

,→ JOINT .635 ±.010 .830 ±.002 .830 ±.004 .618 ±.001 .761 ±.029 .844 ±.006 .805 ±.015 .767 ±.013 .442 ±.002 .245 ±.005 .248 ±.005 .460 ±.000 

Table 3: Image benchmarks. Mean ± std over seeds, ID val / ID test and OOD val / OOD test. ID OOD Acc. ↑ CVaR ↓ Brier ↓ Acc. ↑ CVaR ↓ Brier ↓

CelebA Waterbirds CelebA Waterbirds CelebA Waterbirds CelebA Waterbirds CelebA Waterbirds CelebA Waterbirds ERM .942 ±.002 .778 ±.008 1.117 ±.043 3.362 ±.094 .086 ±.004 .327 ±.015 .945 ±.003 .769 ±.010 1.127 ±.067 3.415 ±.098 .082 ±.004 .343 ±.015 

VRM .942 ±.001 .723 ±.007 .888 ±.011 3.266 ±.063 .084 ±.002 .321 ±.020 .945 ±.003 .712 ±.010 .887 ±.044 3.261 ±.046 .080 ±.005 .338 ±.021 

W-DRO .943 ±.002 .780 ±.007 1.093 ±.045 3.216 ±.156 .084 ±.003 .316 ±.005 .946 ±.002 .771 ±.007 1.102 ±.095 3.178 ±.139 .080 ±.004 .327 ±.004 

KL-DRO .943 ±.000 .817 ±.008 .710 ±.003 .806 ±.016 .086 ±.003 .281 ±.014 .945 ±.003 .811 ±.009 .712 ±.006 .804 ±.020 .083 ±.004 .292 ±.014 

W-DFO .941 ±.002 .722 ±.032 1.135 ±.038 3.506 ±.075 .086 ±.004 .322 ±.013 .945 ±.002 .714 ±.032 1.128 ±.082 3.570 ±.091 .082 ±.004 .336 ±.013 

KL-DFO .942 ±.001 .795 ±.000 1.196 ±.070 3.256 ±.406 .087 ±.003 .341 ±.013 .945 ±.002 .735 ±.043 1.189 ±.118 3.333 ±.434 .082 ±.004 .358 ±.012 

LS .944 ±.002 .806 ±.005 .758 ±.007 1.025 ±.007 .088 ±.003 .279 ±.008 .947 ±.001 .793 ±.007 .751 ±.010 1.028 ±.007 .085 ±.003 .289 ±.008 

LR .943 ±.001 .791 ±.008 .778 ±.012 1.173 ±.017 .087 ±.003 .303 ±.006 .946 ±.002 .787 ±.007 .774 ±.014 1.186 ±.019 .083 ±.003 .316 ±.006 

,→ JOINT .943 ±.003 .812 ±.008 .697 ±.001 .723 ±.001 .084 ±.003 .296 ±.008 .945 ±.002 .802 ±.008 .697 ±.000 .724 ±.002 .081 ±.003 .310 ±.009 

the Wasserstein-based variants (W-DRO, W-DFO) trailing by several points. This is consistent with Gulrajani and Lopez-Paz [15] : under matched protocols, no robustness method consistently beats ERM on accuracy in domain-generalization benchmarks. The ranking on tail risk is markedly different. CVaR separates the methods into two groups: ERM, VRM, and the Wasserstein-based variants stay above 1.0 on most datasets, while KL-DRO, LS, LR, and the Joint configuration cluster around or below 1.0. Joint optimization is within the lower group on all four datasets, with CVaR roughly half that of ERM on Hypert. and Diabetes. Brier scores show smaller spreads but follow the same general pattern: KL-DRO, LS, and Joint are tightly grouped at the low end. 

Image. Table 3 reports results on CelebA and Waterbirds. Accuracy sits in a narrow band across methods, so the interesting variation is on tail risk. On Waterbirds in particular, CVaR spreads over more than a factor of four across methods, from above 3.0 for ERM, VRM, and the Wasserstein-based variants down to below 0.8 at the low end. The label-side interventions (LS, LR) recover a large portion of this gap on their own, and methods that target the ambiguity-set structure (KL-DRO, Joint) close the rest, reaching CVaR values an order of magnitude below ERM on Waterbirds OOD without measurable accuracy cost. CelebA shows the same ordering on a compressed scale, since group imbalance there is milder. Brier scores move in a narrower range and broadly track the CVaR ranking. 

Reward learning. Table 4 reports results on HH-RLHF reward modelling. The Joint configuration reaches 0.713 accuracy on the OOD test, against 0.687 for the strongest baseline (W-DFO), and reduces OOD CVaR from 0.697 (KL-DRO) to 0.695 . The gap to ERM is substantially larger than on the tabular and image benchmarks. Figure 4 summarizes Tables 2–4 into a per-modality view. For each (dataset , metric ) cell, we normalize OOD test performance (flipping loss metrics so higher is better) and average across cells, yielding one distribution per method and modality. The joint configuration ranks within the top group across all modalities, though its margin varies: it is competitive with KL-DRO and LR on image and tabular, and clearly leads on reward learning. On any single modality, a well-tuned baseline can match the joint configuration. However, selecting the right method requires knowing the dominant failure mode a priori . By combining axes, the joint configuration avoids this selection problem and remains consistently competitive across modalities, providing robustness under unknown failure modes. 

5.1 Understanding the Joint configuration 

We now examine the results for the reward learning task more closely through a Shapley analysis and a budget analysis that rules out the richer search space as a confound. 7Table 4: Reward learning , Mean ± std over seeds, ID val / ID test and OOD val / OOD test. ID OOD Acc. ↑ CVaR ↓ Brier ↓ Acc. ↑ CVaR ↓ Brier ↓                                                       

> ERM .766 ±.000 1.117 ±.014 .156 ±.000 .612 ±.004 .906 ±.015 .230 ±.002
> VRM .760 ±.000 .855 ±.005 .162 ±.000 .614 ±.005 .781 ±.008 .231 ±.001
> W-DRO .760 ±.000 .790 ±.009 .175 ±.000 .560 ±.014 .770 ±.010 .246 ±.002
> KL-DRO .766 ±.000 .698 ±.000 .158 ±.000 .673 ±.017 .697 ±.000 .230 ±.002
> W-DFO .758 ±.000 1.137 ±.016 .163 ±.000 .687 ±.056 .938 ±.015 .199 ±.016
> KL-DFO .765 ±.000 1.038 ±.011 .157 ±.000 .639 ±.007 .914 ±.016 .232 ±.002
> LS .766 ±.001 1.033 ±.007 .157 ±.000 .631 ±.005 .862 ±.012 .229 ±.002
> LR .766 ±.001 1.033 ±.007 .157 ±.000 .631 ±.006 .727 ±.003 .229 ±.002
> ,→JOINT .765 ±.001 .694 ±.000 .157 ±.000 .713 ±.005 .695 ±.000 .202 ±.003 ERM
> VRM
> W-DRO
> KL-DRO
> W-DFO
> KL-DFO
> LS
> LR
> Joint
> 0
> 0.3
> 0.6
> 1
> Avg. norm. score ↑

Tabular 

> ERM
> VRM
> W-DRO
> KL-DRO
> W-DFO
> KL-DFO
> LS
> LR
> Joint

Image 

> ERM
> VRM
> W-DRO
> KL-DRO
> W-DFO
> KL-DFO
> LS
> LR
> Joint

Reward Learning 

Figure 4: Average normalized score per modality. Per-method composite across all (dataset, metric) in the OOD setting; higher is better. Joint (red) leads on Tabular and Reward Learning and is in the top group on Image. Baselines in grey. 

Shapley value analysis. Using the Faithful Shapley Interaction Index [ 33 , 44 ], we analyze three representative components, each acting on a different part of the training procedure: VRM (input perturbation), LS (label perturbation), and KL-DRO (sample-level loss aggregation). For each of the 

23 = 8 coalitions, active components are set to their HPO-tuned values and inactive ones disabled. Figure 6 reports the marginal contributions and interactions. Two patterns emerge. First, single-component contributions do not predict behavior in combination. 

On accuracy and Brier, LS is the weakest single component yet the interaction of LS with DRO dominates. On CVaR, DRO is strongest in isolation and VRM adds an independent gain, but their combination is negative. Second, which interactions matter is metric-dependent. Accuracy and calibration are driven by LS combined with DRO; tail risk by DRO acting alone or with VRM. No single configuration wins across all metrics, so exposing the full space to joint HPO — as the Joint configuration does — is necessary to recover the best combination for a given objective. 

Robustness to HPO budget. Joint exposes a richer search space than the baselines, raising a natural concern about the matched 50-trial budget: simple baselines like VRM expose a single hyperparameter and may saturate after a handful of trials, leaving the remaining budget unused, while Joint has many more degrees of freedom and could keep improving until the very end. Figure 5 plots running-best OOD-validation performance against trial count on HH-RLHF and shows this concern is unfounded. The baselines plateau early, often within the first 5–20 trials, but so does Joint: despite its larger search space, it stops improving on the same timescale. The 50-trial budget is therefore not the binding constraint for either side, and the gap between Joint and the baselines is not budget-driven. 

6 Related Work 

Most prior work on robust supervised learning operates along a single axis of our framework, leaving the others fixed implicitly. Along the ambiguity-set axis, Hu et al. [19] expose the structure of the ambiguity set as a configurable choice, showing that f -divergence DRO with an unstructured set collapses to ERM under classification losses. Slowik and Bottou [41] establish that calibrated 81 20 30 40 50          

> HPO trial
> 0.5
> 0.6
> 0.7 W-DFO
> Joint
> KL-DRO
> KL-DFO
> LS
> LR
> VRM
> W-DRO
> Accuracy ↑
> 120 30 40 50
> HPO trial
> 0.7
> 0.8
> 0.9
> KL-DFO
> LS
> VRM
> LR
> W-DFO
> W-DRO
> KL-DRO
> Joint
> CVaR-10 ↓
> 120 30 40 50
> HPO trial
> 0.20
> 0.22
> 0.24
> W-DRO
> VRM
> KL-DFO
> KL-DRO
> LS
> LR
> Joint
> W-DFO
> Brier ↓

Figure 5: HPO budget on HH-RLHF: running-best OOD-validation performance vs. trial count. Joint reaches best CVaR and Brier within the first 20 trials; the advantage is not budget-driven. 0.04 

> 0.02
> 0.00
> 0.02
> 0.04
> 0.06
> Interaction value
> DRO
> LS
> VRM
> Accuracy 0.02
> 0.00
> 0.02
> 0.04
> 0.06
> Interaction value
> DRO
> LS
> VRM
> CVaR 0.02
> 0.01
> 0.00
> 0.01
> 0.02
> 0.03
> 0.04
> Interaction value
> DRO
> LS
> VRM
> Brier

Figure 6: Shapley interaction analysis on OOD test performance for reward learning. Each bar is a coalition’s marginal contribution relative to ERM. DRO is equivalent to ERM on a re-weighted mixture. Along the disambiguation-principle axis, probabilistically robust learning [ 35 ] replaces the worst-case supremum with a ρ-quantile that interpolates between ERM and adversarial training at the input level. Tilted ERM [ 28 ] reweights samples through an exponential tilt equivalent to the KL-DRO/DFO dual that appears in stage 4 of our pipeline. Singh et al. [39] train an augmented hypothesis h(x, λ ) that exposes a continuum of CVaR-aggregated solutions. Work that varies more than one axis remains scarce. DRO-Augment [ 18 ] composes W-DRO with data augmentation, jointly perturbing the reference distribution and the ambiguity set. Freiesleben and Grote [10] offer a conceptual analysis of robustness as a multi-place relation, but without a corresponding algorithmic instantiation. What prior work lacks is a unifying decomposition: a way of viewing different robustness mechanisms not as competing alternatives but as configurable choices along design axes. We provide such a framework. The reference distribution, the ambiguity set, and the disambiguation principle are three axes that span a common design space, with existing methods arising from specific axis choices. Under this view, combining and tuning mechanisms becomes a matter of configuration within it. 

7 Conclusion and Future Work 

We introduced a unified framework for robust supervised learning that organizes a broad class of existing methods along three design axes: a reference distribution Pref , an ambiguity set U(Pref ), and a disambiguation principle Φ. Existing methods arise as specific instantiations of these components, with ERM corresponding to a degenerate case. We further developed a tractable and modular learning procedure that exposes all design choices as hyperparameters within a single search space. This enables the relative contribution of different robustness mechanisms to be determined via hyperparameter optimization, rather than fixed a priori . Across tabular, image, and reward-modeling benchmarks, the resulting joint configurations are competitive with strong single-axis baselines, with the largest gains on reward modeling. When the dominant failure mode is unknown, joint hyperparameter optimization (which can recover any single mechanism as a special case or combine several) is a more reliable default than committing in advance. Several directions remain for future work. First, extending the framework to more general aggregation operators, the most general being the Choquet integral [ 9 ], may further increase expressivity. Second, the framework naturally lends itself to downstream applications, such as learning credal ensembles via different instantiations of its components [45]. 

Limitations and Broader Impact Our empirical evaluation is limited to eight baselines and three metrics and would benefit from broader coverage. Further, methods such as adversarial training 9variants and group DRO were not included and may interact with our framework in ways we did not test. HPO is performed with Optuna’s TPE sampler at a fixed 50-trial budget; alternative black-box optimizers such as Gaussian-process Bayesian optimization or genetic optimizers may yield different configurations, and a systematic comparison is left to future work. The present work advances the field of robust machine learning. We do not foresee any direct negative broader impacts arising from this beyond those generic to improved supervised learning. 10 References 

[1] Akiba, T., Sano, S., Yanase, T., Ohta, T., and Koyama, M. (2019). Optuna: A next-generation hyperparameter optimization framework. In Proceedings of the 25th ACM SIGKDD International Conference on Knowledge Discovery and Data Mining .[2] Bai, Y., Jones, A., Ndousse, K., Askell, A., Chen, A., DasSarma, N., Drain, D., Fort, S., Ganguli, D., Henighan, T., Joseph, N., Kadavath, S., Kernion, J., Conerly, T., Showk, S. E., Elhage, N., Hatfield-Dodds, Z., Hernandez, D., Hume, T., Johnston, S., Kravec, S., Lovitt, L., Nanda, N., Olsson, C., Amodei, D., Brown, T. B., Clark, J., McCandlish, S., Olah, C., Mann, B., and Kaplan, J. (2022). Training a helpful and harmless assistant with reinforcement learning from human feedback. CoRR , abs/2204.05862. [3] Ben-Tal, A., Den Hertog, D., De Waegenaere, A., Melenberg, B., and Rennen, G. (2013). Robust Solutions of Optimization Problems Affected by Uncertain Probabilities. Management Science ,59(2):341–357. [4] Blanchet, J., Li, J., Lin, S., and Zhang, X. (2025). Distributionally robust optimization and robust statistics. Statistical Science , 40(3):351–377. [5] Bradley, R. A. and Terry, M. E. (1952). Rank Analysis of Incomplete Block Designs: I. The Method of Paired Comparisons. Biometrika , 39(3/4):324. [6] Brier, G. W. (1950). Verification of forecasts expressed in terms of probability. Monthly Weather Review , 78(1):1–3. [7] Carratino, L., Cissé, M., Jenatton, R., and Vert, J. (2022). On mixup regularization. J. Mach. Learn. Res. , 23:325:1–325:31. [8] Chapelle, O., Weston, J., Bottou, L., and Vapnik, V. (2000). Vicinal Risk Minimization. In 

Advances in Neural Information Processing Systems , volume 13. MIT Press. [9] Choquet, G. (1954). Theory of capacities. Annales de l’Institut Fourier , 5:131–295. [10] Freiesleben, T. and Grote, T. (2023). Beyond generalization: a theory of robustness in machine learning. Synthese , 202(4):109. [11] Gao, R., Chen, X., and Kleywegt, A. J. (2024). Wasserstein distributionally robust optimization and variation regularization. Oper. Res. 

[12] Gardner, J., Popovic, Z., and Schmidt, L. (2023). Benchmarking distribution shift in tabular data with tableshift. Advances in Neural Information Processing Systems .[13] Garnett, R. (2023). Bayesian Optimization . Cambridge University Press. Available for free at 

https://bayesoptbook.com/ .[14] Goodfellow, I. J., Shlens, J., and Szegedy, C. (2015). Explaining and harnessing adversarial examples. In 3rd International Conference on Learning Representations, ICLR 2015, San Diego, CA, USA, May 7-9, 2015, Conference Track Proceedings .[15] Gulrajani, I. and Lopez-Paz, D. (2021). In search of lost domain generalization. In 9th International Conference on Learning Representations, ICLR 2021, Virtual Event, Austria, May 3-7, 2021 .[16] He, P., Gao, J., and Chen, W. (2023). Debertav3: Improving deberta using electra-style pre-training with gradient-disentangled embedding sharing. In The Eleventh International Conference on Learning Representations, ICLR 2023, Kigali, Rwanda, May 1-5, 2023 . OpenReview.net. [17] Hendrycks, D., Mu, N., Cubuk, E. D., Zoph, B., Gilmer, J., and Lakshminarayanan, B. (2020). Augmix: A simple method to improve robustness and uncertainty under data shift. In International Conference on Learning Representations .[18] Hu, J., Mukherjee, D., and Paschalidis, I. C. (2025). Dro-augment framework: Robustness by synergizing wasserstein distributionally robust optimization and data augmentation. CoRR ,abs/2506.17874. 11 [19] Hu, W., Niu, G., Sato, I., and Sugiyama, M. (2018). Does distributionally robust supervised learning give robust classifiers? In Proceedings of the 35th International Conference on Machine Learning , pages 2029–2037. [20] Hu, Z. and Hong, L. J. (2013). Kullback-Leibler divergence constrained distributionally robust optimization. Available at Optimization Online , 1(2):9. [21] Jeong, D., Aggarwal, S., Robinson, J., Kumar, N., Spearot, A., and Park, D. S. (2023). Ex-haustive or exhausting? evidence on respondent fatigue in long surveys. Journal of Development Economics , 161:102992. [22] Jiang, N. and Xie, W. (2024). Distributionally Favorable Optimization: A Framework for Data-Driven Decision-Making with Endogenous Outliers. SIAM Journal on Optimization , 34(1):419– 458. [23] Kaufmann, T., Weng, P., Bengs, V., and Hüllermeier, E. (2025). A survey of reinforcement learning from human feedback. Trans. Mach. Learn. Res. 

[24] Kingma, D. P. and Ba, J. (2015). Adam: A method for stochastic optimization. In Bengio, Y. and LeCun, Y., editors, 3rd International Conference on Learning Representations, ICLR 2015, San Diego, CA, USA, May 7-9, 2015, Conference Track Proceedings .[25] Krizhevsky, A., Sutskever, I., and Hinton, G. E. (2012). Imagenet classification with deep convolutional neural networks. In Advances in Neural Information Processing Systems 25: 26th Annual Conference on Neural Information Processing Systems 2012. Proceedings of a meeting held December 3-6, 2012, Lake Tahoe, Nevada, United States , pages 1106–1114. [26] Kuhn, D., Esfahani, P. M., Nguyen, V. A., and Shafieezadeh-Abadeh, S. (2019). Wasserstein Distributionally Robust Optimization: Theory and Applications in Machine Learning. In Netessine, S., Shier, D., and Greenberg, H. J., editors, Operations Research & Management Science in the Age of Analytics , pages 130–166. INFORMS. [27] Levy, D., Carmon, Y., Duchi, J. C., and Sidford, A. (2020). Large-scale methods for distribu-tionally robust optimization. Advances in neural information processing systems , 33:8847–8860. [28] Li, T., Beirami, A., Sanjabi, M., and Smith, V. (2021). Tilted empirical risk minimization. In 

9th International Conference on Learning Representations, ICLR 2021, Virtual Event, Austria, May 3-7, 2021 . OpenReview.net. [29] Lienen, J. and Hüllermeier, E. (2021). From label smoothing to label relaxation. In Thirty-Fifth AAAI Conference on Artificial Intelligence, AAAI 2021, Thirty-Third Conference on Innovative Applications of Artificial Intelligence, IAAI 2021, The Eleventh Symposium on Educational Advances in Artificial Intelligence, EAAI 2021, Virtual Event, February 2-9, 2021 , pages 8583– 8591. AAAI Press. [30] Liu, Z., Luo, P., Wang, X., and Tang, X. (2015). Deep learning face attributes in the wild. In 

2015 IEEE International Conference on Computer Vision, ICCV 2015, Santiago, Chile, December 7-13, 2015 , pages 3730–3738. IEEE Computer Society. [31] Madry, A., Makelov, A., Schmidt, L., Tsipras, D., and Vladu, A. (2018). Towards deep learning models resistant to adversarial attacks. In International Conference on Learning Representations .[32] Müller, R., Kornblith, S., and Hinton, G. E. (2019). When does label smoothing help? In 

Advances in Neural Information Processing Systems , volume 32. [33] Muschalik, M., Baniecki, H., Fumagalli, F., Kolpaczki, P., Hammer, B., and Hüllermeier, E. (2024). shapiq: Shapley interactions for machine learning. In Advances in Neural Information Processing Systems 38: Annual Conference on Neural Information Processing Systems 2024, NeurIPS 2024, Vancouver, BC, Canada, December 10 - 15, 2024 .[34] Radford, A., Kim, J. W., Hallacy, C., Ramesh, A., Goh, G., Agarwal, S., Sastry, G., Askell, A., Mishkin, P., Clark, J., Krueger, G., and Sutskever, I. (2021). Learning transferable visual models from natural language supervision. In Proceedings of the 38th International Conference on Machine Learning, ICML 2021, 18-24 July 2021, Virtual Event , Proceedings of Machine Learning Research, pages 8748–8763. PMLR. 12 [35] Robey, A., Chamon, L., Pappas, G. J., and Hassani, H. (2022). Probabilistically robust learning: Balancing average and worst-case performance. In Chaudhuri, K., Jegelka, S., Song, L., Szepesvari, C., Niu, G., and Sabato, S., editors, Proceedings of the 39th International Conference on Machine Learning , volume 162 of Proceedings of Machine Learning Research , pages 18667–18686. PMLR. [36] Robey, A., Chamon, L. F. O., Pappas, G. J., Hassani, H., and Ribeiro, A. (2021). Adversarial robustness with semi-infinite constrained learning. In Advances in Neural Information Processing Systems 34: Annual Conference on Neural Information Processing Systems 2021, NeurIPS 2021, December 6-14, 2021, virtual , pages 6198–6215. [37] Rockafellar, R. T. and Uryasev, S. (2000). Optimization of conditional value-at-risk. The Journal of Risk , 2(3):21–41. [38] Sagawa, S., Koh, P. W., Hashimoto, T. B., and Liang, P. (2020). Distributionally robust neural networks. In International Conference on Learning Representations .[39] Singh, A., Chau, S. L., Bouabid, S., and Muandet, K. (2024). Domain generalisation via imprecise learning. In Proceedings of the 41st International Conference on Machine Learning ,volume 235 of ICML’24 , pages 45544–45570, Vienna, Austria. JMLR.org. [40] Sinha, A., Namkoong, H., and Duchi, J. (2018). Certifying some distributional robustness with principled adversarial training. In International Conference on Learning Representations .[41] Slowik, A. and Bottou, L. (2022). On distributionally robust optimization and data rebalancing. In Camps-Valls, G., Ruiz, F. J. R., and Valera, I., editors, International Conference on Artificial Intelligence and Statistics, AISTATS 2022, 28-30 March 2022, Virtual Event , Proceedings of Machine Learning Research, pages 1283–1297. PMLR. [42] Stiennon, N., Ouyang, L., Wu, J., Ziegler, D., Lowe, R., Voss, C., Radford, A., Amodei, D., and Christiano, P. F. (2020). Learning to summarize with human feedback. In Advances in Neural Information Processing Systems , volume 33, pages 3008–3021. Curran Associates, Inc. [43] Szegedy, C., Vanhoucke, V., Ioffe, S., Shlens, J., and Wojna, Z. (2016). Rethinking the inception architecture for computer vision. In 2016 IEEE Conference on Computer Vision and Pattern Recognition, CVPR 2016, Las Vegas, NV, USA, June 27-30, 2016 , pages 2818–2826. IEEE Computer Society. [44] Tsai, C., Yeh, C., and Ravikumar, P. (2023). Faith-shap: The faithful shapley interaction index. 

J. Mach. Learn. Res. , 24:94:1–94:42. [45] Wang, K., Faza, G. A., Cuzzolin, F., Chau, S. L., Moens, D., and Hallez, H. (2026). Learning Credal Ensembles via Distributionally Robust Optimization. arXiv:2602.08470 [cs.LG] .[46] Wang, Y., Zou, D., Yi, J., Bailey, J., Ma, X., and Gu, Q. (2020). Improving adversarial robustness requires revisiting misclassified examples. In 8th International Conference on Learning Representations, ICLR 2020, Addis Ababa, Ethiopia, April 26-30, 2020 .[47] Yun, S., Han, D., Chun, S., Oh, S. J., Yoo, Y., and Choe, J. (2019). Cutmix: Regularization strategy to train strong classifiers with localizable features. In 2019 IEEE/CVF International Conference on Computer Vision, ICCV 2019, Seoul, Korea (South), October 27 - November 2, 2019 , pages 6022–6031. IEEE. [48] Zhang, H., Cissé, M., Dauphin, Y. N., and Lopez-Paz, D. (2018). Mixup: Beyond empirical risk minimization. In 6th International Conference on Learning Representations, ICLR 2018, Vancouver, BC, Canada, April 30 - May 3, 2018, Conference Track Proceedings . OpenReview.net. [49] Zhang, H., Yu, Y., Jiao, J., Xing, E. P., Ghaoui, L. E., and Jordan, M. I. (2019). Theoretically principled trade-off between robustness and accuracy. In Proceedings of the 36th International Conference on Machine Learning, ICML 2019, 9-15 June 2019, Long Beach, California, USA ,Proceedings of Machine Learning Research, pages 7472–7482. PMLR. [50] Ziegler, D. M., Stiennon, N., Wu, J., Brown, T. B., Radford, A., Amodei, D., Christiano, P. F., and Irving, G. (2019). Fine-tuning language models from human preferences. CoRR ,abs/1909.08593. 13 A Extended Background 

In supervised learning, we consider an input space X , an output space Y and we assume there exists an unknown distribution P ⋆ ∈ P (X × Y ), where P(X × Y ) denotes the set of all probability measures on X × Y . Given a hypothesis class H and a loss function ℓ : Y × Y → R, the goal is to find a hypothesis h ∈ H minimizing the population risk, 

h⋆ = arg min 

> h∈H

R(h; P ⋆) = arg min 

> h∈H

E(x,y )∼P ⋆ [ℓ(h(x), y )] .

Since P ⋆ is unknown, it cannot be optimized against directly. Instead, given a finite dataset D =

{(xi, y i)}ni=1 drawn i.i.d. from P ⋆, one constructs the empirical distribution Pn := 1

> n

Pni=1 δ(xi,y i),

where δ(x,y ) denotes the Dirac measure at (x, y ), and uses it as a proxy for P ⋆. Empirical risk minimization (ERM) then selects a hypothesis by minimizing the empirical risk, 

ˆhERM = arg min 

> h∈H

R(h; Pn) = arg min 

> h∈H

1

n

> n

X

> i=1

ℓ(h(xi), y i).

ERM can be suboptimal in practice for several reasons [ 4]. First, Pn is a poor proxy for P ⋆ when data are scarce: being a degenerate discrete measure, it is sensitive to random fluctuations and assigns zero probability to unobserved regions of the input space. Second, the observed samples may not exactly follow P ⋆ due to data contamination such as measurement noise or label errors. Third, model performance is ultimately evaluated under a deployment distribution Pdeploy that may differ from P ⋆

due to e.g. distributional shift. Together, these issues motivate learning paradigms that go beyond ERM, and we now recall existing methods relevant to this work, organized by the failure mode they address. 

A.1 Finite-sample degeneracy 

Methods in this subsection address the degeneracy of the empirical distribution Pn arising from finite data. Rather than optimizing directly over Pn, they enrich or smooth the training distribution to improve generalization to unseen regions of the input space. 

Vicinal Risk Minimization (VRM). VRM [ 8] defines a vicinal distribution that spreads probability mass over neighborhoods of each training example: Pvic = 1

> n

Pni=1 ν(· | xi, y i), where ν(· | xi, y i)

is a vicinity distribution centered at (xi, y i). The VRM objective is then 

ˆhVRM = arg min 

> h∈H

E(x,y )∼Pvic [ℓ(h(x), y )] .

Mixup. Mixup [ 48 ] constructs synthetic training samples via convex interpolation between pairs of training examples. Given two examples (xi, y i) and (xj , y j ) with one-hot encoded labels, it constructs ˜x = λx i + (1 − λ)xj and ˜y = λy i + (1 − λ)yj , where λ ∼ Beta( α, α ). Unlike VRM, which spreads mass locally around individual points, Mixup explicitly interpolates between distinct examples, encouraging the model to behave approximately linearly between training points. An exemplary illustration of the effect of Mixup is depicted in Figure 7 

A.2 Label noise and miscalibration 

Methods in this subsection address imperfections in the observed labels. Rather than treating labels as ground truth, they either soften target distributions or replace individual labels with sets of admissible distributions, leading to better-calibrated and more robust predictions. 

Label Smoothing. Label smoothing [ 43 ] uniformly redistributes a small amount of probability mass from the true class to all other classes. For a K-class problem with one-hot label y =(0 , . . . , 0, 1, 0, . . . , 0) ∈ RK , it constructs ˜y = ( α/K, . . . , α/K, 1 − α + α/K, α/K, . . . , α/K ),

where α ∈ [0 , 1] controls the smoothing strength. 

Label Relaxation. Label relaxation [ 29 ] takes a set-valued perspective: rather than committing to a single softened target, it replaces each observed label yi with an ambiguity set of admissible label distributions, Qαi = {p ∈ P (Y) | P  

> y̸=yi

p(y) ≤ α}, where α ∈ [0 , 1] controls the degree of relaxation. Any prediction within Qαi incurs zero loss; predictions outside are penalized by their minimal distance to the set. 14 ERM          

> mixup
> Accuracy =0.87
> KL-DFO
> Accuracy =0.85
> W-DRO
> Accuracy =0.57
> Robust Accuracy =0.93 Accuracy =0.92 Accuracy =0.80
> Class 0 Class 1 Train Test Mixup Sample Perturbed Data Point
> `2Radius

Figure 7: Exemplary demonstration of Mixup, KL-DFO and W-DRO in comparison to standard ERM. 

A.3 Distribution shift 

The methods in this subsection address the discrepancies between the training distribution Pn and the deployment distribution Pdeploy . They do so by optimizing not against Pn directly, but against a set of distributions in its vicinity, an ambiguity set U(Pn), differing in how they aggregate over this set: pessimistically (DRO) or optimistically (DFO). 

Distributionally Robust Optimization (DRO). DRO [ 4] optimizes against the worst-case distribu-tion within the ambiguity set. The ambiguity set is typically defined via a statistical distance measure; common choices include the KL-divergence, or the Wasserstein distance. In the latter instantiation, 

U(Pn) = {Q ∈ P (X × Y ) : Wp(Q, P n) ≤ ϱ}, where Wp denotes the p-Wasserstein distance and 

ϱ ≥ 0 controls the size of the ambiguity set. The DRO objective is then 

ˆhDRO = arg min 

> h∈H

sup 

> Q∈U (Pn)

E(x,y )∼Q[ℓ(h(x), y )] .

Notably, adversarial training [ 31 ] can be viewed as a special case of Wasserstein-DRO in which the ambiguity set is restricted to pointwise input perturbations x 7 → x + δ within a geometric set 

∆ ⊂ Rd [40]. An example of this is depicted in Figure 7. 

Distributionally Favorable Optimization (DFO). DFO [ 22 ] takes the opposite stance, optimizing for the best-case distribution within the ambiguity set: 

ˆhDFO = arg min 

> h∈H

inf  

> Q∈U (Pn)

E(x,y )∼Q[ℓ(h(x), y )] .

This is motivated by settings where observed data may be corrupted or pessimistically biased, and optimizing for the best-case risk aims to recover a cleaner signal. An example of KL-DFO, that reweights the support of the original distribution in a favorable way is depicted in Figure 7. 

Probabilistically Robust Learning (PRL). PRL [ 35 ] interpolates between DRO and ERM by requiring robustness to most perturbations rather than all, addressing the tendency of worst-case methods to overcompensate for rare events. Given a distribution r over the perturbation set ∆ and a tolerance level ρ ∈ [0 , 1) , the PRL objective is 

ˆhPRL = arg min 

> h∈H

E(x,y )∼Pn

 ρ- ess sup δ∼r ℓ(h(x + δ), y ),

where the ρ-essential supremum discards the worst fraction ρ of perturbations. As ρ → 0, PRL recovers adversarial training; as ρ → 1, it approaches ERM. 15 B Conceptual Extension 

B.1 Robust Label Loss 

In order to incorporate a pessimistic variant of label relaxation, we adapt the loss function proposed by Lienen and Hüllermeier [29]. The credal target set for the optimistic case is given by 

Qαi =

n

p ∈ P (Y) P  

> y̸=yi

p(y) ≤ α

o

,

and their optimistic loss is 

Lopt (Qαi , ˆp) = min 

> p∈Qαi

dKL (p∥ˆp).

The natural pessimistic counterpart replaces the infimum with a supremum. Instantiating the supre-mum with KL yields 

sup 

> p∈Qαi

dKL (p∥ˆp) = (1 − α) log 1−α 

> ˆp(yi)

+ α log α   

> min y̸=yiˆp(y)

,

attained at the vertex placing mass 1 − α on yi and α on arg min y̸ =yi ˆp(y). This grows without bound as min y̸ =yi ˆp(y) → 0. We instead define 

Lpess (Qαi , ˆp) = sup 

> p∈Qαi

Ey∼p

 − log ˆ p(y).

The objective is linear in p, so the supremum is attained at a vertex of the polytope Qαi . The lower bound p(yi) ≥ 1 − α is binding, and the residual mass α concentrates on the class with largest 

− log ˆ p(y), yielding 

Lpess (Qαi , ˆp) = −(1 − α) log ˆ p(yi) − α log min  

> y̸=yi

ˆp(y).

Consequently, Lpess acts in an adversarial manner. While standard label smoothing distributes α mass uniformly across all incorrect classes, the pessimistic variant identifies the class the model is most confident is wrong and forces the model to hedge against that specific alternative. 

C Experimental Details 

C.1 Datasets 

We evaluate across three modalities—tabular, vision, and NLP—each exhibiting distinct forms of distribution shift. 

Tabular data. We use the TableShift benchmark [ 12 ], which contains real-world datasets with naturally occurring distribution shift. We evaluate on four datasets: Hypert. 

(brfss_blood_pressure ), Diabetes (brfss_diabetes ), College (college_scorecard ), and 

Hospital (diabetes_readmission ), each with predefined in-distribution (ID) and out-of-distribution (OOD) splits. 

Image data. We evaluate on Waterbirds [ 38 ] and CelebA [ 30 ], which exhibit spurious correlation shift. In Waterbirds, bird species are spuriously correlated with background during training. In CelebA, target attributes are correlated with demographic features in the training distribution. 

NLP data. We use the Anthropic HH-RLHF dataset [ 2], consisting of human preference anno-tations over assistant responses. We train on helpful-base and evaluate OOD generalization on 

harmless-base , which differs in annotation criteria and prompt distribution. 

C.2 Model Architectures Tabular models. We use a single-hidden-layer encoder-decoder network: the encoder maps inputs to a 32-dimensional latent space via a linear layer followed by batch normalization and ReLU; a linear decoder reconstructs the input from the latent representation; and a linear classifier with dropout (p = 0 .25 ) predicts the class from the latent code. All methods share the same hypothesis class for fair comparison. 16 Table 5: Tabular benchmarks (additional datasets). Acc. ↑ CVaR ↓ Brier ↓

ASSIST HELOC Lead ASSIST HELOC Lead ASSIST HELOC Lead ERM .469 ±.043 .484 ±.071 .920 ±.000 1.323 ±.190 1.010 ±.093 2.024 ±.292 .562 ±.028 .522 ±.033 .144 ±.005 

VRM .452 ±.014 .484 ±.071 .920 ±.000 1.260 ±.144 1.010 ±.094 2.051 ±.271 .566 ±.032 .523 ±.033 .144 ±.003 

W-DRO .437 ±.000 .488 ±.079 .920 ±.000 1.200 ±.060 1.027 ±.086 1.764 ±.298 .587 ±.022 .522 ±.033 .156 ±.004 

KL-DRO .470 ±.034 .508 ±.048 .920 ±.000 .789 ±.042 .913 ±.060 1.795 ±.202 .528 ±.010 .505 ±.010 .140 ±.003 

W-DFO .437 ±.000 .488 ±.078 .920 ±.000 1.240 ±.113 1.027 ±.086 1.882 ±.414 .591 ±.027 .522 ±.033 .156 ±.004 

KL-DFO .457 ±.023 .489 ±.077 .920 ±.000 1.437 ±.263 1.011 ±.096 2.001 ±.768 .592 ±.047 .523 ±.033 .144 ±.007 

LS .457 ±.020 .505 ±.048 .920 ±.000 .907 ±.016 .951 ±.049 .977 ±.041 .554 ±.021 .502 ±.011 .155 ±.008 

LR .462 ±.023 .508 ±.046 .920 ±.000 .969 ±.060 .988 ±.089 1.026 ±.040 .521 ±.011 .502 ±.011 .146 ±.003 

,→ JOINT .479 ±.039 .505 ±.056 .920 ±.000 .753 ±.006 .918 ±.055 .992 ±.135 .517 ±.005 .504 ±.009 .142 ±.002 

C.2.1 Vision models. 

We extract frozen image embeddings using CLIP ViT-B/32 [ 34 ]. Images are resized to 224 × 224 ,encoded into 512-dimensional ℓ2-normalized embeddings, and passed to a linear classification head. 

C.2.2 NLP models. 

We embed full conversations using reward-model-deberta-v3-large-v2 [ 16 ]. The CLS token representation of the final layer is extracted, yielding 1024-dimensional ℓ2-normalized embeddings. 

Bradley–Terry model. Given a dataset of pairwise human preferences, we fit a Bradley–Terry model [ 5]. Each item i is assigned a latent scalar utility si ∈ R, and the probability that item a is preferred over item b is 

P (a ≻ b) = σ sa − sb

 = esa

esa + esb

, (2) where σ denotes the sigmoid function. Given a binary preference label y = 1[a ≻ b], the model is trained by minimizing the pairwise binary cross-entropy 

LBT = BCE  σ(s(ea) − s(eb)) , y , (3) where ea, eb ∈ Rd are fixed embeddings of the two items. Pairs are randomly flipped during data loading to prevent the model from exploiting position bias. 

Siamese score network. The utility function s : Rd → R is shared between the two branches (Siamese structure). Each training sample is stored as the concatenation [ea; eb] ∈ R2d; at forward time the two halves are scored independently and subtracted. The score network is a two-layer MLP Mixup is excluded because interpolating pairwise binary preference labels is semantically ill-defined. 

C.3 Training Procedure 

All models are trained using Adam optimization [ 24 ] and PyTorch Lightning. All modalities use a batch size of 512. Training runs for up to 50 epochs (tabular), 100 epochs (vision), and 150 epochs (NLP). Hyperparameter optimization (HPO) uses shorter budgets of 50 epochs per trial for tabular and vision, and 100 epochs per trial for NLP. Hyperparameters are tuned per method and dataset using Optuna [ 1] with the TPE sampler. We fix the number of trials to 50 regardless of the number of tunable hyperparameters. HPO is run separately for both ID and OOD validation splits. 

C.4 Evaluation Metrics 

We report the following metrics across all tasks. 17 Accuracy. Standard fraction of correctly classified examples: 

Acc = 1

n

> n

X

> i=1

1[ˆ yi = yi]. (4) 

Brier score. Mean squared error between the predicted class probabilities pi ∈ ∆K and the one-hot target yi [6]: 

BS = 1

n

> n

X

> i=1

∥pi − yi∥22. (5) Lower is better; the Brier score jointly penalizes miscalibration and misclassification. 

CVaR (10%). The Conditional Value-at-Risk at level α = 0 .9 [ 37 ] is the expected per-sample loss in the worst 10% tail: 

CVaR 0.9(ℓ) = 1

n(1 − 0.9) 

X

> i∈T 0.9

ℓi, (6) where T0.9 indexes the ⌈0.1 n⌉ samples with the highest individual losses ℓi. CVaR provides a distributional robustness signal that is more sensitive to hard or tail examples than average loss. 

OOD accuracy. For tasks with held-out distribution-shifted test splits, we report accuracy on those splits directly. This measures generalization under covariate shift without access to the shifted distribution at training time. 

C.5 Hardware Specification 

Experiments were conducted using 2 CPU cores and 8 GiB of RAM. The HPC nodes utilized for the computations are equipped with two AMD Milan563 7763 processors and a total of 256 GiB of main memory. CIFAR-10 experiments were conducted on an RTX4070. 

D Proofs 

D.1 Recovery of Existing Methods 

We now prove that the unified framework (1) recovers each of the methods listed in Table 1 as a special case. 

Proposition 1 (Recovery of Label Smoothing) . Consider the robust framework (1) . Let (i) the reference distribution be defined with smoothed labels in the y-space: 

Pref = ˜Pls = 1

n

> n

X

> i=1

δxi ⊗ ˜yi, ˜yi = (1 − α) yi + α 1

K ,

where yi are one-hot labels, α ∈ [0 , 1] is the smoothing parameter, K is the number of classes, and 

1/K denotes the uniform distribution over labels; and (ii) the ambiguity set be a singleton: 

U(Pref ) = {Pref }.

Then the resulting optimization problem reduces to 

ˆh = arg min 

> h∈H

E(x,y )∼ ˜Pls 

ℓ(h(x), y ),

which is exactly empirical risk minimization with label smoothing [43]. Proof. Since U(Pref ) = {Pref } is a singleton, the aggregation over the ambiguity set is trivial regardless of the choice of Φ:

Φ 

> Q∈{ Pref }

E(x,y )∼Q[ℓ(h(x), y )] = E(x,y )∼Pref [ℓ(h(x), y )] .

18 Substituting Pref = ˜Pls = 1

> n

Pni=1 δxi ⊗ ˜yi, the expectation expands as 

E(x,y )∼ ˜Pls [ℓ(h(x), y )] = 1

n

> n

X

> i=1

Ey∼˜yi [ℓ(h(xi), y )] = 1

n

> n

X

> i=1

ℓ(h(xi), ˜yi),

where the last equality uses linearity of the loss in its second argument (as is standard for cross-entropy with soft targets). The minimizer of this objective is exactly ˆhERM trained with smoothed labels ˜yi,which is by definition label smoothing. 

Proposition 2 (Recovery of Label Relaxation) . Consider the robust framework (1) instantiated with (i) the empirical distribution Pref = Pn, (ii) an ambiguity set over labels defined via total variation with radius α:

U(Pref ) = 

n

Q ∈ P (X × Y ) : TV( QY| xi , δ yi ) ≤ α ∀ i

o

,

and (iii) the optimistic (infimum) aggregation. Then the resulting optimization exactly recovers label relaxation [29]: ˆhrelax = arg min 

> h∈H

inf   

> Q∈U (Pref )

E(x,y )∼Q[ℓ(h(x), y )] .

Proof. Substituting into the framework and expanding the expectation over Pn = 1

> n

Pni=1 δ(xi,y i):

inf   

> Q∈U (Pref )

E(x,y )∼Q[ℓ(h(x), y )] = inf  

> Q∈U (Pref )

1

n

> n

X

> i=1

Ey∼QY| xi [ℓ(h(xi), y )] .

Since the constraint on Q decouples across data points—each QY| xi is constrained independently via 

TV( QY| xi , δ yi ) ≤ α—the infimum factors as 

= 1

n

> n

X

> i=1

inf  

> qi∈P (Y)TV( qi, δ yi)≤α

Ey∼qi [ℓ(h(xi), y )] .

The total variation constraint TV( qi, δ yi ) ≤ α is equivalent to P  

> y̸=yi

qi(y) ≤ α, which is precisely the definition of the label ambiguity set Qαi = {p ∈ P (Y) : P  

> y̸=yi

p(y) ≤ α} of Lienen and Hüllermeier [29]. The objective therefore becomes 

1

n

> n

X

> i=1

inf 

> qi∈Q αi

Ey∼qi [ℓ(h(xi), y )] ,

which is exactly the label relaxation objective. 

Proposition 3 (Recovery of DRO) . Consider the robust framework (1) instantiated with (i) the empirical distribution Pref = Pn, (ii) an ambiguity set defined via a statistical distance ∆ (e.g. Wasserstein) with radius ϱ:

U(Pref ) = {Q ∈ P (X × Y ) : ∆( Q, P n) ≤ ϱ},

and (iii) the pessimistic (supremum) aggregation. Then the resulting optimization exactly recovers distributionally robust optimization [4]: 

ˆhDRO = arg min 

> h∈H

sup 

> Q∈U (Pn)

E(x,y )∼Q[ℓ(h(x), y )] .

Proof. Direct substitution of Pref = Pn, U(Pref ) as defined above, and Φ = sup into the unified framework (1) immediately yields the DRO objective. 

Proposition 4 (Recovery of DFO) . Consider the robust framework (1) instantiated with (i) the empirical distribution Pref = Pn, (ii) an ambiguity set defined via a statistical distance ∆ with radius 

ϱ as in Proposition 3, and (iii) the optimistic (infimum) aggregation. Then the resulting optimization exactly recovers distributionally favorable optimization [22]: 

ˆhDFO = arg min 

> h∈H

inf  

> Q∈U (Pn)

E(x,y )∼Q[ℓ(h(x), y )] .

19 Proof. Direct substitution of Pref = Pn, U(Pref ) as in Proposition 3, and Φ = inf into the unified framework (1) immediately yields the DFO objective. 

Proposition 5 (Recovery of Mixup) . Consider the robust framework (1) instantiated with (i) the reference distribution set to the Mixup vicinal distribution, 

Pref = ˜Pmix = 1

n2

> n

X

> i,j =1

νλ( · | (xi, y i), (xj , y j )) ,

where νλ generates Mixup interpolations (˜ x, ˜y) = λ(xi, y i) + (1 − λ)( xj , y j ) with λ ∼ Beta( α, α ),and (ii) a singleton ambiguity set U(Pref ) = {Pref }. Then the resulting optimization exactly recovers training with Mixup [48]: 

ˆhMixup = arg min 

> h∈H

E(x,y )∼ ˜Pmix [ℓ(h(x), y )] .

Proof. Since U(Pref ) = {Pref } is a singleton, the aggregation trivializes as in Proposition 1: 

Φ 

> Q∈{ ˜Pmix }

E(x,y )∼Q[ℓ(h(x), y )] = E(x,y )∼ ˜Pmix [ℓ(h(x), y )] .

Expanding the expectation over ˜Pmix :

E(x,y )∼ ˜Pmix [ℓ(h(x), y )] = 1

n2

> n

X

> i,j =1

Eλ∼Beta( α,α )

h

ℓ h(λx i + (1 −λ)xj ), λy i + (1 −λ)yj

i

,

which is exactly the Mixup empirical risk of Zhang et al. [48]. 

Proposition 6 (Recovery of VRM) . Consider the robust framework (1) instantiated with (i) the reference distribution set to the vicinal distribution, 

Pref = ˜Pvic = 1

n

> n

X

> i=1

ν( · | xi, y i),

and (ii) a singleton ambiguity set U(Pref ) = {Pref }. Then the resulting optimization exactly recovers vicinal risk minimization [8]: 

ˆhVRM = arg min 

> h∈H

E(x,y )∼ ˜Pvic [ℓ(h(x), y )] .

Proof. Since U(Pref ) = {Pref } is a singleton, the aggregation trivializes as in Proposition 1: 

Φ 

> Q∈{ ˜Pvic }

E(x,y )∼Q[ℓ(h(x), y )] = E(x,y )∼ ˜Pvic [ℓ(h(x), y )] ,

which is exactly the VRM objective of Chapelle et al. [8]. 

D.2 Special cases 

We now show that for linear models many seemingly disparate robust learning methods reduce to empirical risk minimization with an additional regularization term. 

D.2.1 Regression 

We consider a supervised regression problem with data {(xi, y i)}ni=1 with xi ∈ Rd, y i ∈ R. We study linear models of the form fw(x) = w⊤x, where w ∈ Rd denotes the parameter vector. The learning objective is based on the squared loss 

ℓ(fw(x), y ) = ( y − w⊤x)2.

The corresponding ERM objective is 

RERM (w) = 1

n

> n

X

> i=1

(yi − w⊤xi)2.

20 Proposition 7. Shown by [ 8]. Assume the vicinal distribution is defined by ˜x = x+ϵ, ϵ ∼ N (0 , σ 2I).

Then the VRM objective is equivalent to 

RVRM (w) = RERM (w) + σ2∥w∥2.

Proof. The VRM objective minimizes the expected loss under the vicinal distribution: 

RVRM (w) = 1

n

> n

X

> i=1

Eϵ

(yi − w⊤(xi + ϵ)) 2.

Expanding the square yields 

(yi − w⊤xi − w⊤ϵ)2 = ( yi − w⊤xi)2 − 2( yi − w⊤xi)w⊤ϵ + ( w⊤ϵ)2.

Taking expectations with respect to ϵ, the cross term vanishes since E[ϵ] = 0 . Thus, 

E(yi − w⊤(xi + ϵ)) 2 = ( yi − w⊤xi)2 + E[( w⊤ϵ)2].

Since ϵ ∼ N (0 , σ 2I),

E[( w⊤ϵ)2] = w⊤E[ϵϵ ⊤]w = σ2w⊤w = σ2∥w∥2.

Substituting into the VRM objective yields 

RVRM (w) = 1

n

> n

X

> i=1

(yi − w⊤xi)2 + σ2∥w∥2,

which proves the claim. 

Proposition 8. Let the Mixup samples be defined as ˜x = λx i +(1 −λ)xj , ˜y = λy i +(1 −λ)yj , where 

(i, j ) are drawn independently and uniformly from [n] and λ ∼ Beta( α, α ). Let cα = E[λ(1 − λ)] .Then the Mixup objective satisfies 

RMixup (w) = (1 − 2cα) RERM (w) + 2 cα

 ¯y − w⊤ ¯x2,

where ¯x = 1

> n

P 

> i

xi and ¯y = 1

> n

P 

> i

yi. In particular, whenever the hypothesis class contains an intercept, RMixup and RERM share the same minimizers. Proof. Let ri = yi − w⊤xi. Then 

˜y − w⊤ ˜x = λr i + (1 − λ)rj ,

and using the identity (λa + (1 − λ)b)2 = λa 2 + (1 − λ)b2 − λ(1 − λ)( a − b)2,

(˜ y − w⊤ ˜x)2 = λr 2 

> i

+ (1 − λ)r2 

> j

− λ(1 − λ)( ri − rj )2.

Since E[λ] = E[1 − λ] = 1 /2 and E[λ(1 − λ)] = cα, taking expectation over λ and (i, j ) gives 

RMixup (w) = RERM (w) − cα Ei,j 

(ri − rj )2 .

Because i and j are independent and identically distributed, 

Ei,j 

(ri − rj )2 = 2 Ei[r2 

> i

] − 2 ( Ei[ri]) 2 = 2 RERM (w) − 2 (¯ y − w⊤ ¯x)2,

where the second equality uses ¯r = ¯ y − w⊤ ¯x. Substituting, 

RMixup (w) = (1 − 2cα) RERM (w) + 2 cα (¯ y − w⊤ ¯x)2.

For the second claim, with intercept b the second term becomes (¯ y − w⊤ ¯x − b)2 ≥ 0, vanishing iff ¯r = 0 , which is the first-order condition of RERM in b. Both terms are therefore minimized simultaneously by any minimizer of RERM . Since cα < 1/2 for any Beta( α, α ), the coefficient of 

RERM is strictly positive, so under standard non-degeneracy of the augmented feature matrix the minimizer is unique and coincides with that of RERM .21 Remark 2. Under the generative model yi = w⊤ 

> ⋆

xi + εi with E[εi] = 0 , Var( εi) = σ2, and ε ⊥ x,taking expectation over the noise yields the equivalent form 

Eε[RMixup (w)] = Eε[RERM (w)] − cα (w − w⋆)⊤Σx(w − w⋆) + const ,

with Σx = 1

> n2

P 

> i,j

(xi − xj )( xi − xj )⊤. The negative sign means Mixup contributes a concave bonus centered at w⋆ rather than a convex penalty, and the expected Mixup minimizer remains w⋆.This recovers the observation of [ 7] that Mixup has no asymptotic effect on the linear least-squares minimizer. 

Proposition 9. Let ϱ be the radius of the Wasserstein-1 ambiguity set with transport cost ∥x − x′∥q ,and let q∗ denote the conjugate exponent. For sufficiently small ϱ,

RDRO (w) = RERM (w) + 2 ϱ ∥w∥q∗ · 1

n

> n

X

> i=1

|yi − w⊤xi| + o(ϱ).

The leading-order regularizer is the product of ∥w∥q∗ and the empirical mean absolute residual; both factors depend on w.Proof. By Theorem 1 of [11], for sufficiently small ϱ,

RDRO (w) = 1

n

> n

X

> i=1

ℓ(hw(xi), y i) + ϱ · 1

n

> n

X

> i=1

∥∇ xℓ(hw(xi), y i)∥q∗ + o(ϱ).

For the squared loss, ∇xℓ(hw(x), y ) = −2( y − w⊤x)w, so 

∥∇ xℓ(hw(x), y )∥q∗ = 2 |y − w⊤x| ∥ w∥q∗ .

Since ∥w∥q∗ does not depend on the index i, it factors out of the sum: 

ϱ · 1

n

> n

X

> i=1

∥∇ xℓ(hw(xi), y i)∥q∗ = 2 ϱ ∥w∥q∗ · 1

n

> n

X

> i=1

|yi − w⊤xi|.

The mean absolute residual 1

> n

P 

> i

|yi − w⊤xi| is a w-dependent quantity and cannot be absorbed into a w-independent constant. 

D.3 Classification 

We consider a supervised binary classification problem with data {(xi, y i)}ni=1 , xi ∈ Rd, yi ∈{− 1, +1 }. We study linear classifiers fw(x) = w⊤x trained with the squared loss ℓ(fw(x), y ) = (y − w⊤x)2, which corresponds to least-squares classification (e.g. Fisher’s LDA). Since yi ∈{− 1, +1 } implies y2 

> i

= 1 , the squared loss coincides with the squared-margin loss (1 − yiw⊤xi)2.The corresponding ERM objective is 

RERM (w) = 1

n

> n

X

> i=1

(yi − w⊤xi)2.

We assume linear models include an intercept, incorporated by augmenting features with a constant. 

Proposition 10 (VRM) . Under the vicinal distribution ˜x = x + ϵ, ϵ ∼ N (0 , σ 2I),

RVRM (w) = RERM (w) + σ2∥w∥2.

Proof. The squared loss takes the same form as in the regression setting (Appendix D.1) with the constraint yi ∈ {− 1, +1 }, which is not used in the proof of Proposition 7. The same argument applies verbatim. 

Proposition 11 (Mixup) . Let the Mixup samples be defined as ˜x = λx i + (1 − λ)xj , ˜y = λy i +(1 − λ)yj with (i, j ) drawn independently and uniformly from [n] and λ ∼ Beta( α, α ). Let cα =

E[λ(1 − λ)] . Then 

RMixup (w) = (1 − 2cα) RERM (w) + 2 cα (¯ y − w⊤ ¯x)2,

or equivalently RMixup (w) − RERM (w) = −2cα Var n(y − w⊤x). With intercept, RMixup and 

RERM share the same minimizers. 

22 0 50 100 

> Epoch
> 0.6
> 0.7
> Loss

ERM 0 100 

> Epoch
> 0.70
> 0.75
> Loss

DRO   

> Train
> Val
> Train (clean) 050 100
> Epoch
> 0.65
> 0.70
> Loss

DFO Figure 8: Training and validation learning curves for ERM, DRO, and DFO. ERM exhibits classical overfitting, with the validation loss diverging significantly from the training loss. DRO shows a similar but less pronounced effect. DFO does not overfit, with training and validation losses remaining closely aligned throughout training. For DRO and DFO, the clean training loss is also shown. 

Proof. Under the Mixup convex combination, ˜y ∈ [−1, +1] even when yi, y j ∈ {− 1, +1 }. The squared-loss form (˜ y − w⊤ ˜x)2 = ( λr i + (1 − λ)rj )2 remains valid because ˜y enters only through the residual ri = yi − w⊤xi, and the algebraic identity used in the proof of Proposition 8 does not depend on the range of yi. The same argument applies verbatim. 

Proposition 12 (W-DRO) . Let ϱ be the radius of the Wasserstein-1 ambiguity set with transport cost 

∥x − x′∥q , and let q∗ denote the conjugate exponent. For sufficiently small ϱ,

RDRO (w) = RERM (w) + 2 ϱ ∥w∥q∗ · 1

n

> n

X

> i=1

|yi − w⊤xi| + o(ϱ).

Proof. The gradient ∇xℓ(fw(x), y ) = −2( y − w⊤x)w has the same form for y ∈ {− 1, +1 } as for 

y ∈ R, so the proof of Proposition 9 applies verbatim. 

Proposition 13 (Label Smoothing under squared loss) . For binary classification with yi ∈ {− 1, +1 },let label smoothing replace each label by ˜yi = (1 − α) yi for some α ∈ [0 , 1] . Under the squared loss, 

RLS (w) = (1 − α)2 RERM 

 w/ (1 − α),

and consequently the minimizer satisfies w⋆ 

> LS

= (1 − α) w⋆

> ERM

.Proof. Substituting ˜yi = (1 − α)yi,

RLS (w) = 1

n

> n

X

> i=1

 (1 − α)yi − w⊤xi

2.

Factoring (1 − α) from each term and writing u = w/ (1 − α),

RLS (w) = 1

n

> n

X

> i=1

(1 − α)2 yi − u⊤xi

2 = (1 − α)2 RERM (u).

The minimizer of RLS in w is therefore characterized by ∇uRERM (u) = 0 at u = w/ (1 − α), i.e. 

w/ (1 − α) = w⋆

> ERM

, giving w⋆ 

> LS

= (1 − α) w⋆

> ERM

.

E Additional Experimental Results 

Learning curves. Figure 8 illustrates how the choice of disambiguation principle affects training dynamics. Under ERM, the validation loss diverges from the training loss, exhibiting classical overfitting. DRO mitigates this effect by emphasizing worst-case perturbations, resulting in more conservative updates and a reduced generalization gap. In contrast, DFO shows little divergence between training and validation losses. This behavior arises because the optimistic aggregation selectively down-weights hard or potentially corrupted samples, effectively focusing the model on regions of the data that are easier to fit. While this can improve robustness to noise, it may also reduce sensitivity to challenging but informative examples. 23 10 −1                

> 0
> 2
> 4
> 6
> 8
> 10
> # settings
> VRM σ
> 10 −2 10 −1
> 0
> 5
> 10
> 15
> W-DRO ρ
> 10 −2 10 −1
> 0.0
> 2.5
> 5.0
> 7.5
> 10.0
> 12.5
> W-DFO ρ
> 10 −2 10 −1 10 0
> 0
> 2
> 4
> 6
> 8
> 10 KL-DRO η
> 10 −1 10 0
> 0
> 5
> 10
> 15
> # settings
> KL-DFO η
> 10 −1
> 0
> 2
> 4
> 6
> 8
> LS α
> 10 −1
> 0
> 2
> 4
> 6
> 8
> 10
> LR α
> Tabular Image Reward

Figure 9: Single-axis baselines: distribution of best-trial hyperparameter magnitudes per method, stacked by modality. Each panel pools the selected value across all (dataset, metric) settings. 

Per-baseline HPO choices. For comparison, Figure 10 shows the same analysis for each single-axis baseline. Since each baseline exposes a single hyperparameter, the matrix has one column per method (with the relevant symbol). Three patterns are noteworthy. First, KL-DFO saturates at τ = 5 (the upper search bound) on nearly every setting, suggesting that the optimistic KL direction prefers the maximum permitted temperature; this is consistent with KL-DFO’s relative weakness on tail risk. Second, W-DRO and W-DFO converge to overlapping ρ ranges, indicating that the two stances disagree on direction more than on magnitude. Third, VRM σ is the most modality-stable choice (∼ 0.05 –0.15 across the board), making it a sensible default. Figure 9 summarizes these per-method distributions. 24 VRM 

(σ)

W-DRO 

(ρ)

W-DFO 

(ρ)

KL-DRO 

(η)

KL-DFO 

(η)

LS 

(α)

LR 

(α)

ASSIST · Acc 

ASSIST · CVaR 

ASSIST · Brier 

Hypert. · Acc 

Hypert. · CVaR 

Hypert. · Brier 

Diabetes · Acc 

Diabetes · CVaR 

Diabetes · Brier 

College · Acc 

College · CVaR 

College · Brier 

Hospital · Acc 

Hospital · CVaR 

Hospital · Brier 

HELOC · Acc 

HELOC · CVaR 

HELOC · Brier 

Lead · Acc 

Lead · CVaR 

Lead · Brier 

CelebA · Acc 

CelebA · CVaR 

CelebA · Brier 

Waterbirds · Acc 

Waterbirds · CVaR 

Waterbirds · Brier 

RLHF · Acc 

RLHF · CVaR 

RLHF · Brier 

RLHF · WC-Acc 

0.05 0.1 0.05 2 4.9 0.19 0.17 

0.066 0.1 0.1 0.1 5 0.3 0.27 

0.05 0.05 0.1 1 5 0.089 0.21 

0.15 0.05 0.098 2 2.9 0.17 0.13 

0.096 0.1 0.05 0.1 2.3 0.26 0.3 

0.13 0.05 0.1 3 2 0.089 —

0.05 0.05 0.077 1 2.7 0.29 0.23 

0.12 0.1 0.05 3.1 0.25 0.29 0.3 

0.15 0.05 0.1 1.8 4.6 0.065 0.089 

0.05 0.05 0.069 2.2 5 0.2 0.2 

0.076 0.05 0.093 0.1 4.7 0.3 0.3 

0.083 0.05 0.1 2.7 4.9 0.065 0.089 

0.075 0.05 0.054 1.5 2.9 0.2 0.2 

0.2 0.071 0.05 0.22 5 0.3 0.3 

0.056 0.05 0.05 5 5 0.1 0.11 

0.05 0.1 0.05 0.15 1.2 0.27 0.29 

0.2 0.1 0.05 0.1 5 0.3 0.24 

0.2 0.1 0.05 0.55 5 0.3 0.3 

0.05 0.1 0.05 0.1 0.1 0.05 0.05 

0.14 0.05 0.1 2 0.61 0.3 0.26 

0.073 0.1 0.05 2.2 0.25 0.052 0.065 

0.035 0.014 0.01 2.5 3.9 0.14 0.29 

0.13 0.094 0.01 0.01 4.9 0.3 0.3 

0.028 0.012 0.01 5 4.9 0.05 0.052 

0.065 0.044 0.063 0.073 2.8 0.089 0.29 

0.045 0.071 0.01 0.011 1.4 0.3 0.3 

0.03 0.037 0.01 0.26 5 0.053 0.05 

0.05 0.05 0.076 0.013 0.041 0.28 0.28 

0.2 0.1 0.099 0.01 5 0.3 0.3 

0.05 0.05 0.061 5 0.064 0.098 0.065 

0.05 0.05 0.079 0.016 0.048 0.28 0.29 

> Tabular Image Reward

Figure 10: Single-axis baselines: hyperparameter values selected by HPO, one row per (dataset, metric) setting and one column per method (with the method’s primary HP indicated). Cells are colored by log-magnitude within each column; "—" denotes a setting without a result for that method. KL-DFO consistently saturates at τ = 5 (the upper search bound). 25